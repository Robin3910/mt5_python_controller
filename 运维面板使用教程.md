# 节点控制台使用教程

> 面向在 **Windows 节点机**上操作面板的人：第一次导入节点、日常启停、看日志、改配置、更新客户端。
>
> 打包与实现要点见 [`使用与部署文档.md`](./使用与部署文档.md) §6 / §7.3–§7.5；架构决策见 [`技术实现方案.md`](./技术实现方案.md) §18.13。
>
> **最后更新：2026-09-07**

---

## 1. 它做什么、不做什么

节点控制台是装在 **MT5 所在 Windows 电脑**上的桌面程序（窗口标题：`节点控制台 v…`）。一台机器上的多个 `node_client` 实例，都由它负责启停、守护、看健康与日志、从后端拉安装包更新。

| | 运维面板（本机） | 管理后台（浏览器） |
| --- | --- | --- |
| 管什么 | 本机进程：导入、启停、守护、日志、`.env`、客户端版本 | 交易规则、分组/策略、手数、风控、远程平仓、上传安装包 |
| 连哪里 | 本机 `127.0.0.1` 状态口 + 只读地问后端「现在该装哪个版本」 | 后端 API / WebSocket |
| 不下什么 | **不下单、不算手数、不过滤信号** | 信号分发与规则都在后端 + 节点端 |

一句话：后台决定「跟不跟、跟多少」；面板只保证「节点程序还活着、版本对、日志能看」。

```
管理后台（浏览器）
  上传安装包 / 设发布版本 / 启用节点 / 配规则
        │  NODE_TOKEN 只读通道（查版本、下安装包）
        ▼
本机运维面板  ──启停/守护──▶  node_client.exe（与 MT5 同目录）
        │                         │
        └── 本机 127.0.0.1:端口    └── WebSocket ──▶ 后端 /ws/node
            /health /status /stop
```

---

## 2. 安装与第一次打开

### 2.1 拿到程序

向管理员要分发压缩包 `node_client_dashboard-<版本>.zip`（由 `nodel_client_dashboard/build_exe.bat` 打出）。解压后会得到一层目录，里面是 `node_client_dashboard.exe`。

建议把整个目录放到固定位置，例如 `D:\tools\node_client_dashboard\`，以后不要随便挪——实例列表里记的是本机绝对路径。

不要把别人已经跑过的目录整份拷过来：里面可能带着他的 `panel_config.json`（含节点令牌）和 `instances.json`（他机器上的路径）。

### 2.2 启动

双击 `node_client_dashboard.exe`。窗口标题会带版本号，排障时把这个版本报给管理员即可。

开发环境（有源码时）也可以：

```bat
cd nodel_client_dashboard
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

### 2.3 三条运行约定

1. **只允许开一个面板**。再双击一次会提示「节点控制台已在运行」，并尽量把已有窗口拉到前台。
2. **点窗口关闭 / 最小化 = 进系统托盘，不是退出。** 守护仍在跑，已启动的节点也不会被杀掉。托盘与任务栏使用程序 `logo.ico`；右键「显示主窗口」或「退出」。真正退出走托盘「退出」。
3. **面板自己崩了会自动拉起**（默认间隔 5 秒、最多 3 次；连续稳定跑满 60 秒后计数清零）。监督日志在面板目录 `logs/dashboard_supervisor.log`。托盘里点「退出」属于正常退出，不会被看门狗重启。

重新打开面板时，会按上次记下的本机状态口 **自动接管**仍在跑的客户端，不必把节点停掉再启一遍。

---

## 3. 界面怎么看

窗口默认约 1200×760，分三块：

```
┌─────────────────────────────────────────────────────────────┐
│ 节点控制台        [版本更新] [连接配置]  仅本机回环 …        │  ← 顶栏
├──────────────┬──────────────────────────────────────────────┤
│ 实例列表      │  选中实例的名称 / 路径                       │
│              │  徽章：在线 · 健康 · 连接 · 端口 · 版本 · 守护 │
│  [导入节点]   │  [启动] [停止] [节点配置] [刷新健康] [打开目录] │
│  [全部启动]   │  [守护进程 开关]                              │
│  [全部停止]   │  摘要：路径、进程号、MT5 账号、错误 …         │
│  [全部开守护] │  策略任务                                     │
│  [全部关守护] │  运行日志                    [清空显示]       │
│  [手工添加]   │                                              │
│  [手工替换全部]│                                              │
└──────────────┴──────────────────────────────────────────────┘
```

### 3.1 顶栏

| 按钮 | 作用 |
| --- | --- |
| **连接配置** | 填后端 API 地址与节点令牌，供「导入节点 / 版本更新」访问后端。可「测试连接」。 |
| **版本更新** | 检测、批量更新、指定版本、本机回滚。有待更新实例时按钮会变成橙色，例如 `有新版 v1.2.0（2）`，括号里是待更新台数。 |

右侧三个徽章只是提醒：状态口仅本机回环、不要绑公网、节点连后端可用 TLS/WSS。

### 3.2 左侧实例列表

每一张卡片显示：标签、短路径、状态行（版本 · 运行中/已停止 · 健康 · 是否开了守护）。

点卡片选中；卡片上的 **编辑** 改标签 / exe 路径 / 工作目录，**移除** 只从面板列表拿掉（运行中会先停），**不会删除** MT5 目录里的客户端文件。

### 3.3 右侧详情徽章

| 徽章 | 含义 |
| --- | --- |
| 在线 / 离线 | 本机进程在不在（不是后台「节点是否启用」） |
| 健康 正常 / 启动中 / 已停止 / 错误 / 未知 | 本机 `/health` 的结果 |
| 连接 已鉴权 / 连接中 / 重连中 / … | 节点连后端 WebSocket 的状态。要真正跟单，这里应到 **已鉴权**，且后台该节点已启用 |
| 端口 | 面板启动时注入的本机状态口 |
| 版本 | 正在跑的 `node_client` 版本 |
| 守护已开 | 子进程退出后会按间隔自动拉起（默认 5 秒） |
| 启动中 / 停止中 | 正在启停，此时不要去覆盖 exe |

---

## 4. 第一次把节点跑起来（推荐流程）

前提（通常由管理员在服务器上先做完）：

- 管理后台能打开，且「客户端版本」页已经上传并 **设为发布版本**；
- 已从后台「配置 → 节点令牌」复制到 `NODE_TOKEN`；
- 这台 Windows 上 MT5 已安装，**已经登录交易账户**，算法交易已打开。

然后在节点机上按下面做。

### 第 1 步 打开面板

双击 `node_client_dashboard.exe`。

### 第 2 步 填连接配置

点顶栏 **连接配置**：

1. **API 地址**：管理后台的 HTTP 根，例如 `https://hub.example.com` 或 `http://159.75.33.185`（不要写成 `wss://…/ws/node`）。
2. **节点令牌**：后台复制的那份 `NODE_TOKEN`。勾「显示」可核对。
3. 点 **测试连接**。成功会显示后端当前发布版本；失败则改地址/令牌，或检查这台机器能否访问后台。
4. 点 **保存**。配置写在 exe 同目录的 `panel_config.json`。

两项都留空时，面板会尝试从已有实例的 `.env` 自动发现（`MANAGER_WS_URL=ws://host/ws/node` → `http://host`，令牌取 `NODE_TOKEN`）。第一次还没有实例，必须手工填。

这个令牌 **只能**查版本、下安装包，不能用来上传、发布、删包——那些仍要管理员登录后台。

### 第 3 步 导入节点

点左侧 **导入节点**：

1. **MT5 目录**：选含 `terminal64.exe` 或 `terminal.exe` 的安装目录。选错会当场拦住。选对后会提示终端名，以及该目录是否已有客户端 / `.env`。
2. **实例标签**：给这台节点起个好认的名字（如 `模拟盘-A`）；留空则用目录名。
3. **客户端版本**：点「刷新列表」，默认就是当前发布版本，也可以挑历史版本。旁边有包体大小与更新说明。
4. 勾选 **自动写入 `.env`**（推荐）：用连接配置反推 `MANAGER_WS_URL`（`https://host` → `wss://host/ws/node`）并写入 `NODE_TOKEN`。目录里 **已经有 `.env` 时会整份覆盖**；想保留旧文件请取消勾选。确认框会再写一遍将覆盖哪些内容。
5. **导入后立即启动** 建议先不要勾。后台启动无法弹出「输入 MT5 账号」的窗口，终端没登录时进程会起不来。
6. 点 **开始导入**，确认框里核对目录、版本、终端名后执行：下载 → 校验 sha256 → 解压到 MT5 目录 → 写 `.env` → 加入实例列表。

同一目录已经在列表里时，会提示改用顶栏「版本更新」，避免重复添加。

### 第 4 步 核对配置

选中刚导入的实例，点 **节点配置**。常用字段页确认：

- `后端网关地址` 是 `ws://` 或 `wss://` 开头（最常见的错是填成了 `http://`）；
- `节点接入令牌` 与后台一致；
- 真实交易时 `使用模拟 MT5` 为关。

改完点保存。`LOCAL_STATUS_PORT` 不会出现在表单里——启动时由面板注入，写进 `.env` 只会误导。

### 第 5 步 确认 MT5 已登录，再启动

回到主界面点 **启动**。节点 **无窗口后台**运行，不会弹出黑色控制台。

几秒后看右侧徽章：

- 健康 → **正常**
- 连接 → **已鉴权**（若一直停在「连接中 / 重连中」，见 [§12](#12-常见问题)）

### 第 6 步 到管理后台启用节点

`node_client` 用尚未入库的 `MT5_LOGIN` 第一次连上时，后台会自动注册，名称形如 `node-<账号>`，默认 **禁用**。到「节点管理」启用后才会真正接入分发。

需要改名称、按币种手数、账户风控时，也在后台节点详情里改，不在运维面板里改。

### 第 7 步 打开守护（建议）

右侧打开 **守护进程**，或左侧 **全部开守护**。之后进程意外退出会按约 5 秒间隔自动拉起。开关会写入 `instances.json`，下次开面板仍然有效。

到这里，这台节点的本机侧就完成了。

---

## 5. 日常启停

| 操作 | 在哪点 | 说明 |
| --- | --- | --- |
| 启动一台 | 选中 → **启动** | 已在跑则跳过；会注入空闲的本机状态口 |
| 停止一台 | 选中 → **停止** | 先 `POST /stop` 优雅退出，超时再结束进程 |
| 全部启动 / 全部停止 | 左侧按钮 | 有确认框；已符合状态的会跳过，结束后弹出逐台结果 |
| 开/关守护 | 右侧开关，或左侧批量按钮 | 只影响「挂了是否自动拉起」，不会立刻启停 |
| 刷新健康 | **刷新健康** | 立刻打一次本机 `/health` `/status` |
| 打开工作目录 | **打开目录** | 资源管理器打开该实例的 cwd（一般就是 MT5 目录） |

批量启停、更新时，若某台显示「忙碌中，已跳过」，等它启停完成再操作，不要同时去覆盖同一个 exe。

---

## 6. 看健康、任务与日志

### 6.1 摘要区

选中实例后，中间等宽文本会列出标签、版本、程序路径、工作目录、状态口、进程号、健康、WebSocket 状态、节点编号、MT5 账号、运行时长、最后一次错误。排障时把这块整段复制出来即可。

### 6.2 策略任务

来自本机 `/status` 的 runners 快照，例如任务 id、魔术号、品种、模式、方向、是否停止中、模版 id。没有任务时显示「当前无运行中的策略任务」。

这里只是本机观测；分组任务的完整历史仍以管理后台「分组 → 信号」为准。

### 6.3 运行日志

节点的 stdout/stderr **实时**写到面板目录：

```
<面板目录>\logs\<标签>_<实例id前8位>\YYYY-MM-DD.log
```

右侧文本框显示当日（或最近一份）日志的尾部。点 **清空显示** 会清空 **当天这个文件的内容**（文件还在），便于盯新日志；不是删历史、也不是清后台交易记录。

---

## 7. 改节点配置（`.env`）

选中实例 → **节点配置**：

- **常用字段**：后端地址、令牌（可切换显示）、模拟 MT5、观察列表、滑点、魔术号、心跳/上报/重连间隔、策略采样间隔、日志级别等。有类型校验；`MANAGER_WS_URL` 必须是 `ws://` 或 `wss://`。
- **原始文本**：整份 `.env`。表单没列出的键在这里改。
- **两页都改过时以原始文本为准**（无法自动合并）。保存后若走了这条路径会提示。
- 保存是就地改对应行：注释、空行、顺序、自定义键都保留。表单里留空的可选项不会写进文件。
- `.env` 不存在时，保存会创建。

改完必须 **重启实例** 才生效（配置在进程里一次性加载）。若当时在跑，保存后会问要不要立刻重启。

令牌在后台被重置后：每台节点都要在这里改 `NODE_TOKEN` 并重启，否则连接徽章会停在失败/重连，后台事件是 `invalid_token`。

---

## 8. 更新客户端版本

面板是更新代理：Windows 上正在运行的 exe 不能自己覆盖自己，所以必须由面板「先停 → 备份 → 覆盖 → 再启」。

管理员在后台「客户端版本」上传 zip、点「设为发布版本」之后，各节点机上的面板才会看到新版本。

### 8.1 顶栏橙色提示

面板启动约 3 秒后查一次后端，之后每 1 分钟再查；装完新版、增删实例、改过连接配置也会立刻重算。

- 有实例版本低于发布版本 → 按钮变橙：`有新版 v1.2.0（2）`
- 已经最新、后端还没发布、或后端连不上 → 仍显示「版本更新」
- **检测失败不弹窗**（只是提示，不能打断本机运维）
- 版本号读不出来的实例 **不计入** 待更新数，避免提示一直亮着没人看

### 8.2 更新到发布版本（最常用）

1. 点 **版本更新**（没有实例时会提示先添加）。
2. 需要的话在对话框顶部改 API 地址 / 令牌，点 **保存连接配置**（与顶栏入口是同一份 `panel_config.json`）。
3. 点 **检测更新**。每行显示 `当前版本 → 目标版本` 和判定：**需更新 / 已最新 / 将降级**。
4. 勾选要更新的实例（可全选 / 全不选）。
5. 点 **更新到发布版本**，确认后：下载 → 校验 sha256 → 解压 → 逐台停 → 备份到 exe 同级 `.backups/<当前版本>/` → 按文件树覆盖 → 自动再启。

`.env` **永远不会被覆盖**。

### 8.3 更新到指定版本

用于灰度或临时回到某个已上传的历史包。下拉列出后端全部版本（新的在前，默认当前发布版本），旁白有大小、是否发布版本、更新说明。同样做 sha256 校验。

目标版本低于当前版本时，确认之后还会再弹一次 **降级二次确认**，并列出受影响实例。

### 8.4 回滚到本机备份

不依赖网络。下拉列出 **所选实例都有的共同备份版本**，从 `.backups/<版本>/` 恢复。

备份是第一次通过面板做「版本更新」之后才有的。从来没更新过、或各实例备份集合对不上，会提示没有共同备份。

exe 与 `version.txt` 会一起备份，避免回滚后界面上的版本号和真正在跑的程序对不上。

### 8.5 和「手工替换全部」怎么选

| 场景 | 用哪个 |
| --- | --- |
| 后台已发布新包，要更新部分或全部实例，还要能回滚 | 顶栏 **版本更新** |
| 没有后端通道，手里只有一个 `node_client.exe` | 左侧 **手工替换全部** |
| 只想换其中几台、或要保留多份历史备份 | 不要用手工替换 |

手工替换会对 **列表里全部实例** 生效（不能挑几台）：运行中先停 → 把旧 exe 备份为同目录 `node_client.exe.bak`（只有这一槽，下次再替换会被盖掉）→ 覆盖 exe / `version.txt` → 自动再启。源目录建议带着 `version.txt`。

---

## 9. 手工添加（本机已有客户端时）

目录里已经放好 `node_client.exe` 和 `.env`、只是还没进面板时：

1. 点 **手工添加**，选中那个 `node_client.exe`（工作目录默认取其同目录）。
2. 会弹出 **编辑实例**：可改标签、exe 路径、工作目录。
3. 再按 [§4 第 4–7 步](#第-4-步-核对配置) 核对配置、启动、去后台启用、开守护。

客户端必须和 `terminal64.exe` 在同一目录，否则节点发现不了 MT5。

一台电脑多个 MT5（多个安装目录 / 多个账户）：每个目录导入（或手工添加）成一个实例，用标签区分。同一 `MT5_LOGIN` 不能同时在线。

---

## 10. 关闭窗口 ≠ 退出

| 你做了什么 | 实际结果 |
| --- | --- |
| 点窗口右上角关闭、或最小化 | 进托盘；面板还在，守护还在，节点还在跑 |
| 托盘右键 → 显示主窗口 | 把窗口叫回来 |
| 托盘右键 → 退出 | 面板进程结束；**不会强杀**已启动的 `node_client` |
| 再打开面板 | 按 `instances.json` 里保存的端口接管仍在跑的节点 |

所以「晚上想让节点继续跟单」：关窗口即可，不要点托盘「退出」，也尽量别把面板进程结束掉——关了守护就不会在节点挂掉后自动拉起。

无托盘依赖的异常环境下，点关闭会直接退出面板（同样不强杀节点）。

---

## 11. 本机文件落在哪

都在 **`node_client_dashboard.exe` 同目录**（开发跑源码时则在 `nodel_client_dashboard/`）：

| 路径 | 内容 |
| --- | --- |
| `instances.json` | 本机实例列表：标签、exe 路径、工作目录、状态口、是否守护 |
| `panel_config.json` | 后端 API 地址与 `NODE_TOKEN` |
| `logs/<节点标识>/YYYY-MM-DD.log` | 各节点运行日志 |
| `logs/dashboard_supervisor.log` | 面板看门狗日志 |
| `.dashboard.lock` | 单实例锁，不要手工乱删（面板异常退出后一般会自行处理） |

各 **节点工作目录**（通常即 MT5 安装目录）里还有：

| 路径 | 内容 |
| --- | --- |
| `node_client.exe`、`version.txt` | 正在跑的客户端 |
| `.env` | 该节点身份与连接（令牌、网关地址等），更新时受保护 |
| `.backups/<版本>/` | 面板「版本更新」留下的版本化备份，供本机回滚 |
| `node_client.exe.bak` | 仅「手工替换全部」留下的单槽备份 |

`panel_config.json` 和 `.env` 里都有令牌，不要发到聊天工具、不要打进分发包。

---

## 12. 常见问题

| 现象 | 怎么处理 |
| --- | --- |
| 导入节点提示未配置地址或令牌 | 先做顶栏「连接配置」并「测试连接」 |
| 版本列表空 / 提示后台没有任何安装包 | 让管理员到后台「客户端版本」上传 zip 并设为发布版本 |
| 选 MT5 目录被拦住 | 目录里必须有 `terminal64.exe` 或 `terminal.exe`，不要选数据目录或桌面快捷方式 |
| 点启动后马上变成已停止 / 健康错误 | 看运行日志。常见原因：MT5 没登录（后台启动无法交互输入账号）、exe 不在终端同目录、`.env` 缺失 |
| 健康正常，但连接一直「连接中 / 重连中」 | 核对 `MANAGER_WS_URL`（必须 `ws://` 或 `wss://`，生产一般是 `wss://域名/ws/node`）、令牌是否最新、本机能否访问该地址、Nginx 是否放行 `/ws/` |
| 连接已鉴权，后台仍显示离线或拒绝 | 到「节点管理」**启用**该节点；`already_online` 表示同一账号已有连接；`disabled` 即未启用 |
| 徽章「健康 未知」、版本也没有 | 状态口没通：进程没起来，或面板记下的端口已过期。试一次「停止」再「启动」让面板重新注入端口 |
| 顶栏「版本更新」不变橙 | 本来就已最新、后端连不上（失败静默）、或该实例版本号读不出来（不计入待更新） |
| 更新时提示忙碌中已跳过 | 等该台「启动中 / 停止中」结束再更新 |
| 回滚提示没有共同备份 | 先用「版本更新」成功更新过至少一次；多实例回滚时只出现在 **大家都有的** 备份版本里 |
| 改了 `.env` 好像没生效 | 配置只在启动时加载，必须重启实例 |
| 重复打开面板 | 正常，只允许单实例；应切到已有窗口 |
| 关了窗口节点还在、再开面板也能管 | 正常：关窗口进托盘；即便退出面板也不强杀节点，再开会按保存的端口接管 |
| 面板反复自己重启 | 看 `logs/dashboard_supervisor.log`。稳定跑满约 60 秒后崩溃计数会清零 |

节点侧更多现象（令牌拒绝、Webhook、策略跳过等）见 [`使用与部署文档.md`](./使用与部署文档.md) §12。

---

## 13. 建议的日常习惯

1. 节点机开机后先开 MT5 并确认已登录，再开运维面板（若节点已在跑，面板会接管）。
2. 生产实例打开 **守护进程**；关窗口进托盘即可，不要点托盘「退出」。
3. 顶栏变橙时，先「检测更新」看清哪些台，再勾选更新；更新前确认没有正在开仓的关键操作窗口（更新会短暂停止进程）。
4. 令牌重置、换域名之后：先改连接配置并测试，再对每台「节点配置」核对 `MANAGER_WS_URL` / `NODE_TOKEN`，最后重启。
5. 排障时准备三样东西：面板窗口标题里的版本、右侧摘要全文、对应 `logs/<节点>/当天.log` 的尾部。
