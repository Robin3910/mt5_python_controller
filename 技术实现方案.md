# MT5 多节点信号分发交易系统 — 技术实现方案


| 项目   | 说明                                                                                  |
| ---- | ----------------------------------------------------------------------------------- |
| 文档版本 | v0.6.3（2026-08-07：tpl_3 网格修复见 §18.8；v0.6.2 见 §18.7） |
| 编写日期 | 2026-06-25                                                                          |
| 最后更新 | 2026-08-17                                                                          |
| 技术栈  | Python 3.10+ / FastAPI / Redis / WebSocket / MetaTrader5                            |
| 参考实现 | [Robin3910/mt5_python_connector](https://github.com/Robin3910/mt5_python_connector) |
| 适用范围 | 管理后台（中心服务） + MT5 节点客户端 + Web 管理端（PC + 手机 H5）                                        |


---

## 目录

1. [背景与目标](#1-背景与目标)
2. [总体架构](#2-总体架构)
3. [技术选型](#3-技术选型)
4. [核心概念与数据模型](#4-核心概念与数据模型)
5. [数据存储设计](#5-数据存储设计)
6. [节点接入鉴权与自动注册](#6-节点接入鉴权与自动注册)
7. [WebSocket 长连接与节点管理（表 1 / 表 2）](#7-websocket-长连接与节点管理表-1--表-2)
8. [信号接收与解析（与库代码保持一致）](#8-信号接收与解析与库代码保持一致)
9. [信号分发引擎（表 3~8）](#9-信号分发引擎表-38)
10. [MT5 操作规则（与库代码保持一致）](#10-mt5-操作规则与库代码保持一致)
11. [远程平仓（表 10）](#11-远程平仓表-10)
12. [Web 管理端（表 9）](#12-web-管理端表-9)
13. [API 接口设计](#13-api-接口设计)
14. [安全设计](#14-安全设计)
15. [部署方案](#15-部署方案)
16. [项目目录结构](#16-项目目录结构)
17. [开发计划与里程碑](#17-开发计划与里程碑)
18. [设计决策确认](#18-设计决策确认)
19. [测试方案](#19-测试方案)

---

## 1. 背景与目标

参考库 `mt5_python_connector` 是一个**单机版** Flask 应用：在一台装有 MT5 的 Windows 机器上接收 TradingView Webhook 信号，并直接在本机 MT5 上下单。

本方案要将其扩展为**一拖 N 的多节点跟单/信号分发系统**：

- **一个中心管理后台**（FastAPI + Redis）：统一接收信号、管理所有 MT5 节点、按规则分发信号、实时监控所有账户、提供 Web 管理界面。
- **N 个 MT5 节点客户端**：每个节点运行在一台（云）电脑上，本地连接自己的 MT5 终端，通过 WebSocket 长连接接入后台，负责上报状态、接收并执行交易指令。
- **Web 管理端**：PC 与手机 H5 自适应，用于配置、监控、远程平仓。

> **关键约束（需求）**
>
> 1. 节点接入需要鉴权——采用**所有节点共享一个全局 `NODE_TOKEN`**（管理员可在「配置」页查看/重置），节点身份由 `MT5_LOGIN` 唯一识别；客户端用同一 `MT5_LOGIN` 首次接入时若库中尚未注册会按默认配置自动入库（也支持管理员手动预先创建）。
> 2. **信号接收规则、解析逻辑、MT5 操作规则**与参考库代码保持一致（本方案在第 8、10 章逐条对齐）。

### 1.1 与参考库的职责拆分

参考库的三块核心逻辑被拆分到分布式架构的不同位置，但**逻辑本身保持不变**：


| 参考库模块                   | 原职责               | 新架构中的位置                             |
| ----------------------- | ----------------- | ----------------------------------- |
| `tradingview_parser.py` | 解析 Webhook 信号     | **管理后台**（信号入口处解析一次）                 |
| `app.py` 的持仓判断逻辑        | 开仓前检查是否已有持仓       | **管理后台分发引擎**（基于全局实时持仓快照决策） + 节点本地兜底 |
| `mt5_client.py`         | 连接/下单/平仓/查询       | **MT5 节点客户端**（在本地 MT5 执行）           |
| `config.py`             | 符号映射 / 关键字 / 默认参数 | 后台与节点共享的公共配置模块                      |


---

## 2. 总体架构

```
                          TradingView (Pine Script Alerts)
                                    │  Webhook (HTTP POST)
                                    ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │                     管理后台  (FastAPI + Redis)                     │
   │                                                                    │
   │  ┌─────────────┐   ┌─────────────┐   ┌──────────────────────────┐ │
   │  │ Webhook 接收 │──▶│ 信号解析器   │──▶│         分发引擎          │ │
   │  │  /webhook    │   │ (TV Parser) │   │  持仓过滤 / 区间过滤 /     │ │
   │  └─────────────┘   └─────────────┘   │  手数计算 / 同步|轮询模式  │ │
   │                                       └────────────┬─────────────┘ │
   │  ┌─────────────┐   ┌─────────────┐   ┌─────────────▼────────────┐  │
   │  │ 节点注册管理 │   │ 账户监控    │    │     WebSocket 网关         │  │
   │  │ /api/nodes  │   │ (Monitor)   │    │  鉴权握手/心跳/上报/下发    │  │
   │  └──────┬──────┘   └──────┬──────┘   └─────────────┬────────────┘  │
   │         └─────────────────┼───────────────────────┘               │
   │                           ▼                                        │
   │                    ┌──────────────┐                                │
   │                    │    Redis     │  节点注册表/Token、在线状态(TTL)、│
   │                    │              │  账户&持仓快照、信号队列(轮询)、  │
   │                    │              │  Pub/Sub(多实例路由)、配置项     │
   │                    └──────────────┘                                │
   └───────────────────────────┬────────────────────────────────────────┘
              WebSocket 长连接   │  (wss://.../ws/node，首包 auth 鉴权)
        ┌─────────────────────┬─┴───────────────────┬─────────────────┐
        ▼                     ▼                      ▼                 ▼
  ┌────────────┐       ┌────────────┐         ┌────────────┐    ┌────────────┐
  │ MT5 节点 #1 │       │ MT5 节点 #2 │   ...   │ MT5 节点 #N │    │ Web 管理端  │
  │ 全局TOKEN+ │       │ 全局TOKEN+ │         │ 全局TOKEN+ │    │ PC + 手机H5 │
  │ MT5_LOGIN  │       │ MT5_LOGIN  │         │ MT5_LOGIN  │    │ (REST + WS) │
  │ MetaTrader5 │       │ MetaTrader5 │         │ MetaTrader5 │    └────────────┘
  │  terminal   │       │  terminal   │         │  terminal   │
  └────────────┘       └────────────┘         └────────────┘
```

### 2.1 数据流（开仓信号为例）

1. TradingView 触发 Alert → `POST /webhook` 到达管理后台。
2. 后台校验来源（Token / IP 白名单），用 `TradingViewParser` 解析为标准 `TradingSignal`。
3. 分发引擎读取 Redis 中各节点的**实时持仓快照**与**配置**，按规则筛选目标节点：
  - 多区间方向过滤（表 4）
  - 有持仓禁止再开仓 / 无持仓才可接收（表 5、6）
  - 计算每个节点的下单手数（统一 / 独立，表 3）
  - 根据当前模式分发：**全员同步**（广播）或 **轮询领取**（队列，表 7、8）
4. 通过 WebSocket 把交易指令下发到目标节点。
5. 节点用 `MT5Client`（逻辑同参考库）在本地 MT5 执行，回传执行结果。
6. 后台记录结果、刷新账户/持仓快照，并通过 WebSocket 推送给 Web 管理端实时展示。

---

## 3. 技术选型


| 层         | 选型                                                                               | 说明                                                           |
| --------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Web 框架    | **FastAPI** + Uvicorn                                                            | 原生异步、内置 WebSocket、自动 OpenAPI 文档                              |
| 异步运行时     | asyncio                                                                          | 单进程内管理大量 WS 长连接                                              |
| 缓存/状态/消息  | **Redis 7**                                                                      | 节点状态、账户快照、信号队列、Pub/Sub、分布式锁                                  |
| Redis 客户端 | `redis-py`（asyncio 模式）                                                           | 异步读写与 Pub/Sub                                                |
| 节点 ↔ 后台   | **WebSocket (wss)**                                                              | 长连接、双向、低延迟                                                   |
| MT5 接入    | `MetaTrader5`（官方 Python 包）                                                       | 仅运行在节点侧（Windows）                                             |
| 数据校验      | Pydantic v2                                                                      | 请求/消息模型校验                                                    |
| 持久化       | **MySQL 8**（SQLAlchemy 2.0 + asyncmy 异步驱动；Alembic 迁移）                            | 节点账号、操作审计、历史信号（Redis 仅存实时态）                                  |
| 鉴权        | JWT（管理员）/ Token（节点）/ Bearer+IP（Webhook）                                          | 见第 14 章                                                      |
| 前端        | **Vue 3 + TypeScript** + Vite + Pinia + Vue Router（UI 库 Element Plus / Naive UI） | `<script setup lang="ts">` + Composition API；响应式，PC/H5 同一套代码 |
| 部署        | 后台 Linux/Windows 均可；节点必须 Windows                                                 | 节点依赖 MT5 终端                                                  |


> 节点客户端复用参考库的 `mt5_client.py`、`tradingview_parser.py`（解析在后台）、`config.py`，保证"MT5 操作规则与信号规则一致"。

---

## 4. 核心概念与数据模型

### 4.1 节点（Node）

代表一台运行 MT5 的客户端机器（一个 MT5 账户）。**`mt5_login` 是节点的业务唯一键**——既是接入鉴权的识别依据，也是 Web 列表 / 持仓快照的归并 key；管理端不再保存每节点 token（鉴权改为全局共享，见 §6 与 §4.5）。

```python
class Node(BaseModel):
    node_id: str            # 系统生成的唯一 ID（如 nd_xxxxxxxx）
    name: str               # 人工备注名（留空时自动生成 "node-<mt5_login>"）
    enabled: bool = True    # 是否启用（禁用后不分发、不允许连接）
    status: str = "offline" # online / offline
    # —— 按币种分发配置（filters 字典，键为品种代码）——
    # 每项可含：follow_sync, follow_poll, lot_mode, lot, poll_order
    filters: dict | None = None
    # —— MT5 绑定（自 v0.2 起，mt5_login 创建后不可修改，且全局唯一）——
    mt5_login: int                # 必填，> 0
    mt5_server: str | None = None # 由节点 hello 消息上报后回填
    created_at: float
    last_seen: float | None = None
```

> **自动注册默认值**：`name="node-<login>"`、`enabled=False`（须管理员启用后才可接入）；`filters` 从中控台已登记品种**自动生成**（每个品种一条：`follow_sync=True`、`follow_poll=True`、`lot_mode=fixed`、`lot=0.01`、`poll_order=0`）。见 §6 与 `node_service.default_node_filters_from_global`。
>
> **v0.4 变更**：节点**必须**在 `filters` 中为信号品种配置按币种条目，否则该节点拒收（`skip_reason`：`节点未配置：{sym}未在节点按币种配置中，拒收`）；不再对未配置品种回退节点级默认策略。
>
> **v0.3 变更**：原节点级 `lot_mode` / `follow_sync` / `follow_poll` / `poll_order` 已下沉到 `filters` 的**按币种条目**；库表仍保留旧列作兼容，运行期以 `filters` 为准。

### 4.2 账户快照（AccountSnapshot）

节点周期性上报，存于 Redis，供监控与分发决策使用。

```python
class Position(BaseModel):
    ticket: int
    symbol: str
    type: str          # BUY / SELL
    volume: float
    price_open: float
    price_current: float
    profit: float
    magic: int
    comment: str
    time: float

class AccountSnapshot(BaseModel):
    node_id: str
    login: int
    server: str
    balance: float
    equity: float
    margin: float
    free_margin: float
    leverage: int
    positions: list[Position]
    updated_at: float
```

### 4.3 交易信号（TradingSignal）

**与参考库 `tradingview_parser.py` 的 `TradingSignal` 完全一致**，仅新增系统调度字段：

```python
class TradingSignal(BaseModel):
    action: str                 # BUY / SELL / CLOSE
    symbol: str                 # 已映射为 MT5 符号
    volume: float
    stop_loss: float | None = None
    take_profit: float | None = None
    order_type: str | None = "market"
    comment: str = ""
    allow_position: bool = False   # 与库一致：为 True 时允许已有持仓仍开仓
    # —— 系统新增（调度用，不影响 MT5 下单语义）——
    signal_id: str                 # 后台生成，幂等/追踪/容错用
    received_at: float
```

### 4.4 交易指令（Command，后台 → 节点）

```python
class Command(BaseModel):
    cmd: str                     # open / close / config / query / ping
    signal_id: str | None = None
    # open 时：
    action: str | None = None    # BUY / SELL
    symbol: str | None = None
    volume: float | None = None
    stop_loss: float | None = None
    take_profit: float | None = None
    comment: str = ""
    magic: int | None = None
    # close 时：
    close_target: str | None = None   # all / symbol / ticket
    close_symbol: str | None = None
    close_ticket: int | None = None
```

### 4.5 系统级配置（SystemSetting，v0.2 新增）

以 key/value 形式持久化系统级配置。当前唯一用途：保存全局节点接入令牌 `node_token`。

```python
class SystemSetting(BaseModel):
    key: str            # e.g. "node_token"
    value: str          # 明文存储（节点令牌需要复制到每个 .env，因此不哈希）
    updated_at: float
```

> 设计权衡：与单节点 token 不同，**全局令牌需要管理员能随时查看以复制到节点 `.env`**（同一令牌可能分发到 N 台节点），因此采用「明文持久化 + Redis 缓存 + 管理端可显示/重置」策略，而非"仅哈希、只在生成时显示一次"。读取与重置端点都受管理员 JWT 鉴权保护（见 §13.3）。

---

## 5. 数据存储设计

> **Redis** 负责**实时态**（节点在线状态、账户快照、信号队列、Pub/Sub、分布式锁）；**MySQL** 负责**持久化权威源**（节点注册与配置、操作审计、历史信号与逐节点执行结果）。Redis 中的节点元数据视为 MySQL 的热缓存，服务启动时从 MySQL 预热。

### 5.1 Redis 键设计（实时态）


| Key                         | 类型              | 内容                                                              | TTL             |
| --------------------------- | --------------- | --------------------------------------------------------------- | --------------- |
| `node:{node_id}`            | String(JSON)    | 节点元数据 + 交易配置（见 Node 模型）                                         | 持久              |
| `node:by_login:{mt5_login}` | String          | → `node_id`（WS 握手时按 `mt5_login` 反查节点；v0.2 起替代了原 `node:token`） | 持久              |
| `nodes`                     | Set             | 全部 `node_id`                                                    | 持久              |
| `node:online:{node_id}`     | String          | `"1"`，心跳续期，作为"在线"判据                                             | 心跳间隔 × 3（如 90s） |
| `node:account:{node_id}`    | String(JSON)    | 最新 `AccountSnapshot`                                            | 持久（被覆盖）         |
| `signal:{signal_id}`        | Hash            | 信号原文 + 状态（pending/dispatching/done/failed）                      | 24h             |
| `signal:poll:pending`       | List            | 轮询模式：待处理信号队列（信号之间串行排队）                                          | 持久              |
| `signal:poll:{signal_id}`   | String(JSON)    | 轮询信号处理结果：信号快照 + 领取状态 `status`(pending/done/unconsumed) + 领取节点 `consumer` | 24h             |
| `signal:poll:rotation:{symbol}` | String(JSON) | **按品种的轮转顺序**：`node_id` 有序列表；每次被领取的节点移到队尾，实现循环轮转（round-robin） | 持久              |
| `config:lot:global`         | String          | 全局统一手数配置（启用/数值）                                                 | 持久              |
| `config:filters`            | String(JSON)    | **按品种全局规则**（准入清单 + 方向总开关 + 分发模式/持仓范围 + 区间过滤；见 9.2） | 持久              |
| `config:node_token`         | String          | 全局节点接入令牌（明文缓存；权威源在 MySQL `system_setting` 表，见 §4.5 与 §5.2）  | 持久              |
| `cmd:{node_id}`             | Pub/Sub Channel | 多实例部署时，向某节点路由指令                                                 | —               |
| `lock:dispatch:{signal_id}` | String(NX/EX)   | 分发幂等锁，防重复处理                                                     | 30s             |


> **单实例 vs 多实例**：单实例部署时，WebSocket 连接对象可直接保存在进程内字典 `connections[node_id]`，分发即直接 `await ws.send_json(...)`。多实例水平扩展时，每个实例订阅自己持有连接的 `cmd:{node_id}` 频道，分发方 `PUBLISH` 指令，由持有连接的实例转发——以此解耦"分发逻辑"与"连接归属"。

### 5.2 MySQL 表结构（持久化）

> 引擎 InnoDB，字符集 `utf8mb4`；事件类表（审计 / 历史 / 执行）默认保留 **90 天**（定时清理或按天分区）。SQLAlchemy 2.0 + asyncmy，迁移用 Alembic。

```sql
-- 节点注册与配置（权威源；Redis 为热缓存）
-- v0.2 起：移除 token_hash；mt5_login 升级为 NOT NULL + UNIQUE，作为节点的业务唯一键
CREATE TABLE nodes (
    node_id      VARCHAR(32)   PRIMARY KEY,
    name         VARCHAR(64)   NOT NULL,
    enabled      TINYINT(1)    NOT NULL DEFAULT 1,
    lot_mode     VARCHAR(16)   NOT NULL DEFAULT 'global',-- global / fixed / signal
    lot          DECIMAL(10,2) NULL,
    follow_sync  TINYINT(1)    NOT NULL DEFAULT 1,
    follow_poll  TINYINT(1)    NOT NULL DEFAULT 1,
    poll_order   INT           NOT NULL DEFAULT 0,       -- 轮询消费顺序，默认按 created_at
    filters_json JSON          NULL,                     -- 节点级区间过滤(未配置回落全局)
    mt5_login    BIGINT        NOT NULL,
    mt5_server   VARCHAR(64)   NULL,
    created_at   DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at   DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    UNIQUE KEY uk_mt5_login (mt5_login)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 系统级配置（v0.2 新增）：当前唯一用途是保存全局节点接入令牌 node_token
CREATE TABLE system_setting (
    `key`       VARCHAR(64)  PRIMARY KEY,
    `value`     TEXT         NOT NULL,
    updated_at  DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 操作审计（管理端敏感操作）
CREATE TABLE audit_log (
    id          BIGINT       PRIMARY KEY AUTO_INCREMENT,
    ts          DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    operator    VARCHAR(64)  NOT NULL,
    action      VARCHAR(64)  NOT NULL,                   -- create_node/rotate_node_token/close_all/update_config...
    target      VARCHAR(64)  NULL,                       -- node_id / 全局
    params_json JSON         NULL,
    result      VARCHAR(16)  NOT NULL,                   -- ok / fail
    ip          VARCHAR(45)  NULL,
    KEY idx_ts (ts), KEY idx_operator (operator)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 历史信号（webhook 接收与解析结果）
CREATE TABLE signal_history (
    signal_id     CHAR(32)      PRIMARY KEY,
    received_at   DATETIME(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    source_ip     VARCHAR(45)   NULL,
    raw_payload   TEXT          NULL,                    -- 原始 webhook 内容
    action        VARCHAR(8)    NULL,                    -- BUY / SELL / CLOSE
    symbol        VARCHAR(32)   NULL,
    volume        DECIMAL(10,2) NULL,
    sl            DECIMAL(18,5) NULL,
    tp            DECIMAL(18,5) NULL,
    comment       VARCHAR(128)  NULL,
    parsed_ok     TINYINT(1)    NOT NULL DEFAULT 0,
    dispatch_mode VARCHAR(8)    NULL,                    -- sync / poll
    status        VARCHAR(16)   NOT NULL DEFAULT 'pending', -- pending/dispatching/done/partial/failed
    KEY idx_recv (received_at), KEY idx_symbol (symbol)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 逐节点执行结果（一条信号 × 一个节点 = 一行；同步/轮询通用，用于对账）
CREATE TABLE signal_dispatch (
    id            BIGINT        PRIMARY KEY AUTO_INCREMENT,
    signal_id     CHAR(32)      NOT NULL,
    node_id       VARCHAR(32)   NOT NULL,
    decided_vol   DECIMAL(10,2) NULL,                    -- 实际下单手数(9.4)
    gate_result   VARCHAR(16)   NOT NULL,                -- passed / skipped
    skip_reason   VARCHAR(64)   NULL,                    -- has_position/filtered/offline...
    status        VARCHAR(16)   NOT NULL DEFAULT 'pending', -- pending/done/failed/skipped
    retcode       INT           NULL,                    -- MT5 retcode(10.7)
    order_ticket  BIGINT        NULL,
    deal          BIGINT        NULL,
    error         VARCHAR(255)  NULL,
    dispatched_at DATETIME(3)   NULL,
    finished_at   DATETIME(3)   NULL,
    KEY idx_signal (signal_id), KEY idx_node (node_id), KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

> 账户实时态（balance/equity/持仓）存于 Redis；如需历史曲线/留痕对账，可另建 `account_snapshot` 时序表（数据量大，建议按天分区或转时序库，按需开启）。

#### v0.6 新增：分组策略账本与账户风控

与上面的 `signal_dispatch`（`model=normal`）隔离；ORM 定义见 `backend/app/orm.py`。启动时由 `db.py` 内迁移函数补列（如 `nodes.risk_json`、`nodes.trend_json`、`node_group.strategy_id`、`node_group.trend_risk_enabled`）。

| 表 | 用途 |
| --- | --- |
| `node_group` | 节点分组；`dispatch_mode`、一对一 `strategy_id`、可选 `trend_risk_enabled` |
| `node_group_member` | 分组成员与组内轮询序 |
| `trading_strategy` | 策略实例（模版 id/名、品种、`config_json` 规则） |
| `group_signal_task` | strategy 信号主任务 + 策略快照 |
| `group_task_dispatch` | 节点子任务（独立 `magic`、运行快照、`hold_when_empty`） |
| `group_task_event` | 子任务事件流（含 `detail_json`） |

`nodes` 表另增 `risk_json`（账户级风控配置，结构见 §9.9）。趋势面板参数存 `system_setting.trend_config`（全局共享，见 §18.10）；`nodes.trend_json` 仅作旧数据迁移种子。

---

## 6. 节点接入鉴权与自动注册

> **核心规则（v0.2）：所有节点共享一个全局 `NODE_TOKEN`（在「配置」页查看/重置），节点身份由 `MT5_LOGIN` 唯一标识；首次接入的 `MT5_LOGIN` 自动按默认配置入库。** Token 不正确、缺少 `MT5_LOGIN`、节点被禁用或重复登录的连接均被拒绝。

### 6.1 与 v1.0（每节点一 token）的差异

| 项 | v1.0（已废弃） | v0.2（当前） |
| --- | --- | --- |
| 鉴权令牌 | 每节点一份，明文仅创建时显示一次，DB 存 sha256 | 全局共享一份，明文持久化，可在「账户设置」随时查看/重置 |
| 节点身份识别 | token → `node_id` 反查 | 校验全局 token → 用 `mt5_login` 反查/自动创建 |
| 节点入库 | 仅管理员手动 `POST /api/nodes` | 手动 `POST /api/nodes`（可选）+ node_client 首次登录自动注册 |
| `mt5_login` 约束 | 可空、可重复 | NOT NULL、全局 UNIQUE |
| API 端点 | `POST /api/nodes/{id}/rotate-token` | `GET/POST /api/config/node-token[/rotate]` |
| 节点 `.env` | 各自不同的 token | 同一个全局 token + 各自的 `MT5_LOGIN` |

### 6.2 全局令牌生命周期

```
首次启动后端
   │
   ▼
ensure_node_token(): SELECT system_setting WHERE key='node_token'
   │  存在  → 把 value 同步到 Redis 缓存
   │  缺失  → secrets.token_urlsafe(32) 写库 + 缓存
   ▼
管理员 GET  /api/config/node-token             → 返回 {token, updated_at}（管理员 JWT 鉴权）
管理员 POST /api/config/node-token/rotate      → 重新生成 + 写库 + 刷缓存 + 落审计
                                              ↳ 所有节点的旧 .env 立即失效，需要同步更新
```

要点：
- **存明文**：节点 `.env` 需要这份 token，管理员必须能多次复制；同时端点受 JWT 保护，未登录无法读取。
- **常量时间比较**：握手用 `hmac.compare_digest(client_token, expected_token)`，与参考库 `verify_auth` 同源的安全实践。
- **DB 权威 + Redis 缓存**：DB 是权威源，启动 / 重置时刷新缓存；缓存失效时回源 DB。
- **审计**：`rotate_node_token` 记录到 `audit_log`（操作人 / IP / 时间）。

### 6.3 节点身份识别与自动注册

```
node_client 启动
   │ 读 .env: NODE_TOKEN, MT5_LOGIN, MT5_PASSWORD...
   ▼
建立 wss://hub/ws/node 连接 → ws.accept()
   │
   ▼ 首包
   ws.send {"type":"auth","data":{"token":"<NODE_TOKEN>","mt5_login":<int>}}
   │
   ▼ 后台校验顺序
   1. 拉取全局 token（Redis → DB 回源）；hmac.compare_digest 比较
        ✗  → auth_fail(invalid_token) + close 4401
   2. 解析 mt5_login（必须为正整数）
        ✗  → auth_fail(missing_mt5_login) + close 4401
   3. 反查节点：Redis `node:by_login:{login}` → 命中即取节点；未命中再 DB 直查兜底
   4. 仍不存在 → node_service.auto_register(login)
        ├─ 默认配置：name=f"node-{login}", enabled=False（入库后本轮鉴权仍会因禁用被拒 4403）
        ├─ filters：从中控台已登记品种批量生成（每品种 follow_sync/poll=True, lot_mode=fixed, lot=0.01, poll_order=0）
        └─ 写库 + 刷缓存 + broadcast_admin({type:"node_registered",...})
   5. 节点 enabled=False  → auth_fail(disabled) + close 4403
   6. 同 node_id 已在线   → auth_fail(already_online) + close 4409
   7. 通过 → register_connection + send auth_ok({node_id, heartbeat})
```

要点：
- **token 不放 URL**：query string 会进入 Nginx/反代访问日志，是敏感信息泄露面。统一用**连接后首包 `auth`**。
- **首包超时**（`AUTH_FIRST_PACKET_TIMEOUT` 默认 5s）→ 关闭码 `4400`，防止未鉴权连接占用资源。
- **关闭码约定**：`4400` 握手超时/格式错误 / `4401` 鉴权失败（invalid_token / missing_mt5_login）/ `4403` 节点被禁用 / `4409` 重复连接 / `4500` 自动注册失败（DB 异常）。
- **重复连接**：同一 `node_id` 已有存活连接时拒绝新连接（`already_online`）；旧连接断开后新连接才能接入——避免互相踢线导致连接雪崩。

参考实现：`backend/app/ws_gateway.py::node_ws` + `backend/app/node_service.py::auto_register` + `backend/app/system_settings.py::get_node_token`。

### 6.4 客户端配置

节点客户端 `.env`：

```ini
MANAGER_WS_URL=wss://your-domain.com/ws/node
# 从管理后台「配置 → 节点令牌」复制（所有节点共享同一令牌）
NODE_TOKEN=<全局节点令牌>
# 账号/密码/服务器启动时交互输入；将本程序放入 MT5 安装目录以自动发现 terminal64.exe
HEARTBEAT_INTERVAL=15
ACCOUNT_REPORT_INTERVAL=5
```

---

## 7. WebSocket 长连接与节点管理（表 1 / 表 2）

> 对应表格第 1 条（心跳检测、信息上报、长连接建立、状态管理、传输通道）与第 2 条（实时监控所有账户、上报与解析）。

### 7.1 消息协议

统一 JSON 信封：`{ "type": "...", "data": {...}, "ts": 1719... }`

**节点 → 后台**


| type           | 说明               | data 关键字段                              |
| -------------- | ---------------- | -------------------------------------- |
| `auth`         | 首包鉴权，声明身份与版本      | token, mt5_login, client_version（旧客户端不带，按未知处理） |
| `heartbeat`    | 心跳               | ts                                     |
| `account`      | 账户 + 持仓快照上报      | AccountSnapshot                        |
| `trade_result` | 指令执行结果           | signal_id, success, order, deal, error |
| `ack`          | 轮询模式领取/完成确认      | signal_id, stage(claimed/done)         |


**后台 → 节点**


| type                    | 说明                 | data 关键字段                 |
| ----------------------- | ------------------ | ------------------------- |
| `auth_ok` / `auth_fail` | 鉴权结果               | node_id                   |
| `command`               | 交易/平仓/查询指令         | Command 模型                |
| `config`                | 下发交易配置（手数/区间/模式开关） | lot_mode, lot, filters... |
| `pong`                  | 心跳回应               | ts                        |


### 7.2 心跳与状态管理

- 节点每 `HEARTBEAT_INTERVAL`（默认 30s）发送 `heartbeat`；后台收到后 `SET node:online:{id} 1 EX 90`（3 倍间隔容错）。
- 后台同时回 `pong`，节点据此判断后台可达性；连续丢失则触发重连（指数退避）。
- "在线"判据：`node:online:{id}` 存在即在线；TTL 到期自动判定离线。
- 后台维护一个 watchdog 协程，定期扫描在线集合变更，将 `online/offline` 变化通过 WS 推送给 Web 管理端。
- 连接断开（`WebSocketDisconnect`）：立即清理进程内连接表，并删除/缩短 `node:online` 标记。

### 7.3 账户实时监控（表 2）

- 节点每 `ACCOUNT_REPORT_INTERVAL`（默认 5s）调用本地 MT5 获取 `account_info()` + `positions_get()`，组装 `AccountSnapshot` 上报。
- 后台解析后写入 `node:account:{id}`，并增量推送到 Web 管理端。
- 该快照是**分发引擎做持仓过滤的依据**（表 5、6），因此上报频率要兼顾实时性与负载；下单/平仓等关键动作后，节点应**立即触发一次额外上报**以减小决策延迟窗口。

### 7.4 重连与可靠性

- 节点侧：断线后自动重连（退避 1s→2s→…→30s 上限），重连成功后立即补发一次账户快照。
- 后台侧：指令下发使用 `signal_id` 幂等；节点对重复 `signal_id` 去重，避免重连导致重复下单。

---

## 8. 信号接收与解析（与库代码保持一致）

> 本章逐条对齐参考库 `tradingview_parser.py` 与 `config.py`，保证"信号接收规则和逻辑"一致。解析在**管理后台 `/webhook`** 完成。

### 8.1 接收入口

`POST /webhook`，请求体兼容三类（与参考库 `app.py` 一致）：

- `application/json` → 取 JSON
- `application/x-www-form-urlencoded` → 取 form
- 其他 → 取原始文本，作为 `{"text": "..."}`

并执行与参考库相同的安全校验：`Authorization: Bearer <token>`（`ENABLE_AUTH`）与 IP 白名单（`ENABLE_IP_WHITELIST`，支持 `X-Forwarded-For`）。

### 8.2 支持的信号格式（与库一致）

**格式 1：结构化 JSON**

```json
{ "action": "buy", "symbol": "EURUSD", "volume": 0.1, "sl": 1.0800, "tp": 1.0900, "comment": "Pine Signal" }
```

**格式 2：简化 JSON / 文本字段**

```json
{ "signal": "buy EURUSD 0.1 sl=1.0800 tp=1.0900" }
```

（或 `text` / `message` / `body` 字段承载纯文本）

**格式 3：纯文本**

```
buy EURUSD 0.1
EURUSD buy 0.1
```

### 8.3 字段解析规则（与库一致）


| 目标字段           | 接受的别名（大小写不敏感）                                           | 规则                                                          |
| -------------- | ------------------------------------------------------- | ----------------------------------------------------------- |
| action         | `action` / `signal` / `direction` / `cmd`               | 关键字匹配；**CLOSE 优先**，其次 BUY，其次 SELL                           |
| symbol         | `symbol` / `ticker` / `s` / `sym`                       | 转大写，经 `SYMBOLS` 映射表解析；文本用正则提取 6 位符号或 `EUR/USD` 形式           |
| volume         | `volume` / `lotsize` / `lot` / `v` / `q`                | 缺省 `DEFAULT_LOT=0.1`；`<=0` 用缺省；**上限 `MAX_LOT_SIZE=1.0`** 截断 |
| sl             | `sl` / `stoploss` / `stop_loss` / `stop`                | 可选；`>0` 才生效                                                 |
| tp             | `tp` / `takeprofit` / `take_profit` / `target`          | 可选；`>0` 才生效                                                 |
| order_type     | `type` / `ordertype`                                    | 缺省 `"market"`                                               |
| allow_position | `allow_position` / `allowPosition` / `position_allowed` | `int(value)==1` 才为真；默认 `False`                              |


**动作关键字表（含中文，与库一致）：**

```python
BUY_KEYWORDS   = ["buy", "long", "做多", "买入", "多"]
SELL_KEYWORDS  = ["sell", "short", "做空", "卖出", "空"]
CLOSE_KEYWORDS = ["close", "exit", "平仓", "平", "close_all"]
```

**符号映射表（TradingView → MT5，与库一致）：**

- 外汇：EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, NZDUSD, EURGBP, EURJPY, GBPJPY
- 贵金属：XAUUSD, XAGUSD ；原油：USOIL, UKOIL ；指数：US100, US30, GER40

### 8.4 校验规则（与库 `validate_signal` 一致）

- `action`、`symbol` 必填；`volume > 0`；若给定 `sl/tp` 必须 `> 0`，否则判为非法信号并返回 400。

### 8.5 解析后落地

解析成功后，后台为信号分配 `signal_id`，写入 `signal:{signal_id}` 与 **`signal_history`（含 `raw_payload` 原始请求体）**，随后交给**分发引擎**（第 9 章）。解析失败时亦尽力落库 `raw_payload` 供事件页追溯。注意：参考库是"解析后立即在本机下单"，本系统改为"解析后进入分发"，这是唯一的流程差异，**信号语义不变**。

---

## 9. 信号分发引擎（表 3~8）

分发引擎是新增的核心模块，输入一条 `TradingSignal`，输出"对哪些节点、用什么手数、用什么模式下发什么指令"。

### 9.1 总体决策流程

```
            ┌─────────────────────────────────────────────┐
   signal ─▶            │ 0) 品种准入：signal.symbol 须在中控台 filters 中登记且 enabled=true │
            │     未登记 / 已禁用 → 拒收（webhook status=rejected；含 CLOSE） │
            │ 1) 候选节点 = 在线 ∧ enabled ∧ 节点 filters 已配该品种 │
            │     ∧ 该币种参与对应模式（未配品种 → skipped 并记日志）│
            │ 2) 方向总开关 + 多区间方向过滤（表4）          │
            │ 3) 持仓过滤（表5/6，scope 按品种配置）          │
            │ 4) 计算每节点手数（表3，按节点 filters 币种项）  │
            │ 5) 按该品种 dispatch_mode 分发：               │
            │     - sync(表7): 过滤后节点并发同时执行         │
            │     - poll(表8): 按轮转顺序选 1 个节点领取     │
            └─────────────────────────────────────────────┘
```

**分发模式与持仓范围**（v0.3）：不再使用全局 `config:dispatch` API，改为写入 `config:filters` 的**每个品种条目**（`dispatch_mode`、`position_scope`）。环境变量 `DISPATCH_MODE` / `POSITION_SCOPE` 仅作该字段缺省时的回退默认。

### 9.2 多区间方向过滤（表 4）

**目的**：在不同价格区间内只允许特定方向开仓（例如：金价 2300–2350 只允许做空，2250–2300 只允许做多），用于风控与策略过滤。

配置结构（`config:filters`，**按品种为键**；未登记或 `enabled=false` 的品种 webhook 直接拒收，含 CLOSE；后台手动平仓不受影响）：

```json
{
  "XAUUSD": {
    "enabled": true,
    "allow_buy": true,
    "allow_sell": true,
    "dispatch_mode": "sync",
    "position_scope": "symbol",
    "default_action": "block",
    "intervals": [
      { "low": 2300, "high": 2350, "allow": ["SELL"] },
      { "low": 2250, "high": 2300, "allow": ["BUY"] }
    ]
  }
}
```

判定逻辑（开仓信号才参与区间/持仓过滤；CLOSE 不受区间/持仓过滤，但仍须品种已登记且 `enabled=true`）：

0. **品种启用开关**：`enabled=false` 时拒收该品种全部信号（含 CLOSE）。
1. **方向总开关**（v0.3）：`allow_buy` / `allow_sell` 为 `false` 时，直接拦截对应方向。
2. 取该符号当前价格——**以该节点实时价格为准**（后台下发前向该节点查询当前 tick，或将区间规则随指令下发、由节点执行前本地校验），避免用陈旧快照误判。
3. 找到价格落入的区间；若该区间 `allow` 不含本次方向 → **拦截**。
4. 不落入任何区间时按 `default_action` 处理。

> **配置层级（已确认）**：
> - **中控台（全局）**：`config:filters` 登记品种并配置分发模式、持仓范围、方向总开关与区间规则；同时作为 Webhook **准入清单**。
> - **节点级**：`node.filters` 按币种配置 `follow_sync` / `follow_poll` / `lot_mode` / `lot` / `poll_order`；**未单独配置的币种对该节点拒收**（v0.4；自动注册时从中控台已有品种批量生成默认条目）。
> - 区间过滤在节点侧与全局**字段级合并**（`effective_filters`）：节点可覆盖全局同名品种的 `enabled`、区间等字段。

### 9.3 持仓过滤：有持仓禁止再开仓 / 无持仓才接收（表 5、6）

**与参考库 `app.py::execute_trade` 的语义对齐**：库中在 BUY/SELL 前，若 `allow_position=False` 且该 **symbol 已有持仓**，则跳过。

本系统把该判断**前移到后台**（因为后台掌握所有节点的实时持仓快照），实现"无持仓账户才下发、有持仓账户不下发"：

```python
def position_gate(node_id, signal, snapshot, scope="symbol"):
    if signal.action not in ("BUY", "SELL"):
        return True               # 平仓不受此限
    if signal.allow_position:
        return True               # 与库一致：显式允许则放行
    held = snapshot.positions
    if scope == "symbol":         # 默认：与库一致，按 symbol 判断
        held = [p for p in held if p.symbol == resolve(signal.symbol)]
    # scope == "account": 账户级——只要有任意持仓就拦截
    return len(held) == 0
```

- `**scope="symbol"`（默认）**：完全对齐参考库（同一品种有持仓才拦截）。
- `**scope="account"`**：对齐表格字面"有持仓账户禁止再次开仓"（只要账户有任意持仓即拦截）。
- **（已确认）两种范围都实现**，由 `config:dispatch.position_scope` 在管理端运行时切换，**默认 `symbol`**（对齐参考库）。
- **节点侧兜底**：节点执行开仓前再次本地校验持仓（库原逻辑保留），双保险防止快照延迟导致的重复开仓。

### 9.4 手数管理：统一 / 独立（表 3）

每个节点的实际下单手数由**该品种**在 `node.filters` 中的 `lot_mode` 决定（v0.4 起：未在 `filters` 配置该品种的节点在 9.1 候选筛选阶段即被跳过，不会进入手数计算）：


| lot_mode | 取值来源                | 说明                 |
| -------- | ------------------- | ------------------ |
| `global` | `config:lot:global` | 全局统一手数（中控台）       |
| `fixed`  | `node.filters[symbol].lot` | 该节点该品种独立手数   |
| `signal` | `signal.volume`     | 跟随信号自带手数         |


```python
def resolve_volume(node, signal_volume, global_lot, symbol):
    sf = node.filters.get(symbol)  # 按币种查找，兼容券商后缀
    mode = sf.lot_mode if sf else node_default_lot_mode  # 默认 fixed
    ...
```

- 轮询顺序同理：`node.filters[symbol].poll_order`，缺省为 0。
- 是否参与 sync/poll：读 `node.filters[symbol].follow_sync` / `follow_poll`，缺省为 `True`。

### 9.5 全员同步模式（表 7）

- 语义：一条信号让**所有**通过过滤的节点**并发同时**执行（广播）。与轮询模式的区别：同步是"所有节点都执行"，轮询是"只有轮到的 1 个节点执行"。
- 实现：分发引擎遍历过滤后的目标节点，逐一下发 `command`（进程内直接 `ws.send_json`；多实例下 `PUBLISH cmd:{node_id}`）。
- 仅 `node.filters[symbol].follow_sync=True`（缺省 True）的节点参与。

```python
async def dispatch_sync(signal):
    targets = await filter_targets(signal)        # 9.1~9.3
    for node in targets:
        cmd = build_command(node, signal)         # 含 9.4 手数
        await send_to_node(node.node_id, cmd)
```

### 9.6 轮询轮转领取模式（表 8）

- **语义（已确认）**：所有参与该品种轮询的节点**按固定顺序排成一个轮转队列**；一条信号到来后，**只被轮转队首第一个能真正成交的节点领取消费**（一个信号 = 一个节点开仓）。领取成功后，该节点被**移动到队尾**，其余节点保持原相对顺序；其他节点等待**下一条**新信号按序领取，如此**循环轮转**（round-robin 派单）。
- **默认初始顺序**按 `poll_order` 升序、其次 `created_at` 升序，顺序可在管理端调整。
- 与全员同步的区别：同步是"所有节点**并发同时**下单"；轮询轮转是"每条信号**只有轮到的 1 个节点**下单"——用于把信号/持仓**分摊**到不同账户轮流承接（分散风险、错峰建仓、规避同一时刻同向下单被风控识别）。
- 排队与推进（队列保证信号有序，轮转保证节点公平轮流）：

```
入队:   新信号 LPUSH signal:poll:pending（多条信号之间串行处理）
建任务: 取出信号，读取该品种轮转顺序 signal:poll:rotation:{symbol}，
        与“当前参与节点”对齐（剔除已失效、追加新节点到队尾）
选人领取:
        for node in 轮转顺序:
            若节点离线/被禁用            → 顺延下一个（保持其位置）
            向该节点下发 command，等待 trade_result/ACK（带超时/重试）
            被 9.2/9.3 过滤 skipped      → 顺延下一个（保持其位置）
            重试后仍失败 failed          → 顺延下一个（保持其位置）
            成功 done                    → 记为 consumer，跳出
收尾:   consumer 移动到队尾并持久化轮转顺序；signal.status = done
        （整轮无人可成交 → status = unconsumed，不下单）
```

设计要点：

- **轮转范围**：**按品种独立**维护轮转顺序（`signal:poll:rotation:{symbol}`），各品种互不影响。
- **只移动消费者**：仅**真正开仓成功**的节点移到队尾；被跳过/离线/未轮到的节点保持原位（离线节点因此会保留其轮转位置，恢复在线后优先被轮到）。
- **顺延容错**（表 8"队列错误容错处理"）：队首节点若离线、被 9.2/9.3 过滤、或失败/超时按 `POLL_MAX_RETRY` 指数退避重试后仍未成交 → **顺延给下一个候选节点**，直到有节点成交；确保"一个信号尽量被一个节点承接"。
- **逐节点实时判定**：每个节点轮到时，仍按 9.2/9.3 用该节点**最新快照**实时判定区间与持仓，保证"无持仓才开仓"等规则持续生效。
- **参与资格**：仅 `node.filters[symbol].follow_poll=True`（缺省 True）且已配置该品种、已启用的节点进入轮转队列。

> 模式切换：按品种在 `config:filters[symbol].dispatch_mode` 设置 `sync | poll`（中控台 UI）；不再提供全局 `/api/config/dispatch` 端点。

### 9.7 一致性、幂等与并发控制

多节点实盘最大的风险是**重复下单**与**竞态**。采用"全链路 at-least-once 投递 + 幂等去重"达到 effectively-once 效果：

- **信号去重**：对 webhook 原文（或 `action+symbol+volume+来源+时间窗` 指纹）计算 key，`SET dedup:{fingerprint} 1 NX EX 5` 命中即丢弃，吸收 TradingView 的重发；通过后才分配 `signal_id`。
- **指令幂等**：每条 `command` 携带 `signal_id`；节点本地保存近期已处理 `signal_id`（LRU + TTL），重连或重发时去重，**绝不因重连而重复下单**。
- **执行串行锁**：对 `(node_id, symbol)` 加分布式锁 `lock:exec:{node_id}:{symbol}`（`SET NX PX`），在锁内完成"读快照 → 持仓判定(9.3) → 下发 → 等回执"，消除"判定无持仓"与"实际成交"之间的竞态。
- **陈旧窗口处理**：开/平仓后节点**立即补报**快照（7.3）；后台在锁内对该 `(node, symbol)` 置 `pending-hold` 直到回执或超时，期间拒绝同标的再次开仓，弥补约 5s 快照间隔的空窗。
- **状态机**：信号 `pending → dispatching → done | partial | failed`；单节点结果 `done | failed | skipped`，全部持久化（见 5.2），便于追踪与对账。
- **节点重启**：节点重连后先补报快照、再继续接收指令；轮询轮转顺序 `signal:poll:rotation:{symbol}` 持久化于 Redis，后台重启后延续同一轮转进度（9.6）。

### 9.8 策略模版与 `model=strategy` 分组链路

Webhook 的 `model` 字段决定走哪条分发链路：`normal`（默认）走 9.1~9.7 的按币种分发；`strategy` 走**分组分发**（`group_dispatcher.py`）。两条链路的规则与账本完全隔离——分组链路不读中控台品种配置，不做区间方向 / 持仓过滤，也不读节点的按币种手数策略；落库走 `group_signal_task` / `group_task_dispatch` / `group_task_event`，与 `signal_dispatch` 互不读写。

#### 分组、策略绑定与任务模型

| 概念 | 说明 |
| --- | --- |
| 节点分组 `node_group` | strategy 信号的分发单元；自带 `dispatch_mode`（`sync` 全员 / `poll` 组内轮转），不按币种区分 |
| 成员 `node_group_member` | 一个节点可加入多个分组；`sort_order` 决定组内轮询初始顺序 |
| 策略实例 `trading_strategy` | 由内置模版创建，绑定品种；**与分组一对一绑定**（`node_group.strategy_id` UNIQUE）——同一策略不能挂到多个分组；未绑定策略的分组不参与分发 |
| 主任务 `group_signal_task` | 一条 strategy 信号 × 一个分组的分发记录；触发时快照策略规则（`strategy_snapshot_json`），运行期不受策略后续编辑影响 |
| 子任务 `group_task_dispatch` | 主任务 × 节点的实际执行单元；`magic = NODE_TASK_MAGIC_BASE + dispatch.id`，每节点独立魔术号 |
| 事件 `group_task_event` | 开仓 / 加仓 / 网格平移 / 平仓等状态变更；`detail_json` 可带计算依据（手数反推、网格线等） |

并发与生命周期要点：

- **节点级互斥**：子任务非终态时，`(group_id, node_id)` 在 Redis 占位（TTL=`NODE_BUSY_TTL`），**同一分组内**一个节点同时只跑一个策略子任务；不同分组各自独立，同一节点可并行承接多个分组。
- **网格空仓常态**：`hold_when_empty=True` 时，账户快照对账不会因无持仓强制收口；任务只由止损 / 止盈 / `CLOSE` / 手动停策略结束。
- **断线恢复**：节点重连后按未收口子任务续跑；网格运行态（`runtime_json`：触发态 / 平移量 / 当前网格线）随非心跳进度落库，`strategy_resume` 原样还原。若子任务已是 `closing` 或写过 `stop_requested_at`，改发 `strategy_stop`，避免把「决定要停」的任务又恢复成继续跑。
- **残仓非终态**：平仓复查仍有持仓、或 `close_on_stop=false` 脱离监控时，子任务落 `stop_failed` / `detached` / `faulted`（`SUBTASK_STUCK`），**不释放占位**，等仓位真正清零或对账确认后再收口。
- **手动信号**：管理端 `POST /api/console/manual-signal` 可对指定分组触发 BUY / SELL / CLOSE，走同一条分组分发链路（便于联调与补单）。
- **定向筛选**：信号可带两个数组，在品种匹配之上再筛一层——`template_ids` 按绑定的策略模版收窄（`group_rules.template_reject_reason`），`group_ids` 按分组 ID 点名（`group_rules.group_targeted`）；缺省 / 空数组不限制，两者同时给出时取交集。开仓走 `_candidate_groups`（分组/策略须启用且过准入）；**CLOSE 独立按活动子任务 + 开仓快照筛选**，分组或策略后来禁用仍可终止在跑任务。两个字段只对 `model=strategy` 有意义，非策略信号携带一律 400（`webhook._validate_targeting`），并计入去重指纹。
- **定向的落选说明**：未登记的模版 ID 在入口层 400（模版是代码里的封闭注册表）；不存在的分组 ID 不报 400，只在分发时汇总成一条 `指定的分组不存在：…`（`group_rules.missing_group_ids`），因为分组是可随时增删的业务数据。被点名却禁用的分组会单独写明原因，没被点名的分组则完全不进落选说明。

策略实例由内置**策略模版**创建：选模版时把默认规则复制到实例，之后按实例独立调整，不影响模版。规则以 `rules` 列表（每条带 `type`）存进 `trading_strategy.config_json`，随 `strategy_start` 下发给节点。

#### 规则类型与扩展方式

| type | 名称 | 所属模版 | 首单手数来源 |
| --- | --- | --- | --- |
| 1 | 逆势加仓 | 策略模版1（`tpl_1`） | 信号 `volume` |
| 2 | 顺势加仓 | 策略模版1（`tpl_1`） | 信号 `volume` |
| 3 | 以损定量趋势单 | 策略模版2（`tpl_2`） | 按风险金额反推 |
| 4 | 网格交易 | 策略模版3（`tpl_3`） | 规则 `lot_per_grid` |

各 type 的字段集合不同，`strategy_templates.normalize_rule` 按 type 分派到对应的 normalizer（注册表 `_RULE_NORMALIZERS`），只输出该类型自己的字段。新增模版只需登记一个 normalizer，既有 type 的落库结构不受影响；未知 type 一律按逆势加仓处理。

模版1 的逆势 / 顺势规则另有 `batch_action`（`all` / `buy` / `sell`）：分批加仓时独立指定监控方向，可与首单 `action` 不同。

模版1 偏离基准按规则类型分开（节点 `strategy_rules`）：**顺势**取最近一笔开仓价；**逆势**取持仓中不利方向最深的开仓价（多单最低 / 空单最高，无更深逆势仓时即首仓）。因此先顺势加仓后，不会在顺势仓的小幅回撤上误触发逆势加仓，须先回到首仓不利侧达到阈值。

#### 策略模版2：以损定量趋势单

核心是「先定亏多少，再算开多少手」：

```
止损距离     = |止损触发侧报价 - 信号止损价|   # BUY 看 bid、SELL 看 ask
每手止损亏损 = 止损距离 / tick_size × tick_value
总手数       = 风险金额 / 每手止损亏损         # 按 volume_step 向下取整
```

底仓按 `base_ratio` 市价成交（**不挂止盈**），剩余仓位按 `add_batches` **等分**成多笔分散仓，与底仓一并市价打出，订单数固定是 `1 + add_batches`。**所有订单共用信号那一个止损价**。开启 `breakeven_enabled` 后，浮盈达到「止损距离 × `breakeven_times`」时把该魔术号全部持仓的止损移到加权均价（`once` 只触发一次 / `loop` 可反复）。

分散仓的止盈是一条**等分阶梯**，不是同一个价：

```
第 i 笔止盈 = 开仓侧报价 + 止损距离 × rr_ratio × i / add_batches
```

满盈亏比只落在最远一档，前面各档按比例提前兑现——价格一路走高时逐档落袋，回头时已兑现的部分不再吐回去。底仓不设止盈，靠保本止损与趋势跑。

约定：

- **止损距离按触发侧算、阶梯锚在开仓侧**：多单在 ask 成交、止损由 bid 触发，两者差一个点差；分开取值才能同时对齐 MTcommander 的手数与止盈价。拿不到点差时退化为同一个价。
- **分散仓严格等手数，余量不开**：每笔 `⌊剩余 ÷ 单数⌋`，除不尽的部分放弃下单，因此实下总手数会略少于反推值。阶梯下最远一档最难兑现，把余量并到末笔等于让最重的仓去赌最难到达的那一档。剩余不够让每笔都到最小手数时自动减少单数。
- **阶梯以真实成交价为锚**：手数必须在下单前算出，只能用当前报价预估；底仓成交后用真实成交价把整条阶梯重锚一次（`risk_sizing.anchor_to_fill`，点差随开仓价平移），否则盈亏比不再是配置值。这是与 MTcommander 唯一的有意偏差——它用的是下单前报价。
- **断线恢复按备注认档**：分散仓 MT5 备注为 `R3B{index}`；重连用最大已见批次推断 `filled`，**不用持仓笔数**（中间档止盈离场后笔数变少，按笔数会重复开仓）。
- **开仓 burst 可中断**：底仓后打齐分散仓的循环每笔前检查 `_stopping`；收到 `strategy_stop` / CLOSE 后停手，由外层平仓收口。
- **限价开仓**：`entry_mode=limit` 时底仓与分散仓全部挂在信号入场价（同一点位），止损距离与手数与市价同一口径；阶梯止盈仍锚该入场价。挂单 GTC 等成交。见 §18.15 / §18.24。

#### 策略模版3：网格交易

信号只负责「启动这组网格」；区间 / 格数 / 每格手数 / 止损止盈均来自策略规则，**不要求**信号携带 `sl`，也**忽略**信号 `volume` / `tp`。

| 参数 | 说明 |
| --- | --- |
| `price_lower` / `price_upper` | 网格区间 |
| `grid_count` | 格数（2–200） |
| `grid_mode` | `arithmetic` 等差 / `geometric` 等比 |
| `grid_side` | `long` 只做多 / `short` 只做空 |
| `lot_per_grid` | 每格手数；若配置了 `total_lot_limit`，须 `total_lot_limit >= lot_per_grid` |
| `trigger_price` | 触发价；`0` = 立即启动；否则现价**触及**（穿越或落到）该价才启动，与多空方向无关 |
| `stop_lower` / `stop_upper` | 下沿 / 上沿终止价；`0` = 不设。多头：下沿=止损、上沿=止盈；空头相反 |
| `prefill_enabled` | 初始建仓：多头买入卖出价仍高于现价的格；空头开空平仓价仍低于现价的格 |
| `close_on_stop` | `true`：止损/止盈/终止时清仓收口；`false`：停止交易但保留持仓，子任务进 `detached` 直至仓位被外部平光 |
| `trailing_up` / `trailing_max` | 向上追踪：突破区间外沿时平移整网（含止损止盈）；`trailing_max=0` 表示不限平移次数 |
| `assist_*` | **配置期试算助手**（默认 `assist_enabled=false`）：`assist_timeframe` / `assist_atr_mult` / `assist_spacing` / `assist_max_loss`。只记录 `grid_count` / `lot_per_grid` 是怎么算出来的，**节点执行不读**（见 §18.14） |

方向准入在分发前由 `group_rules.entry_reject_reason` 校验（无启用规则、区间非法、`long` 拒收 `SELL` 等）。创建/更新策略时 `validate_rules_for_template` 拒收模版-type 错配与空启用集。节点侧由 `grid_trading.py` + `strategy_runner.py` 执行；模版与快照规则对不上时 **fail-closed**（不回退加仓路径）；空仓是正常运行态；仅 hedging 账户可跑网格。

#### 分层：为什么手数在节点侧算

反推手数 / 网格切线需要品种的 `tick_value` / `volume_step` / 点值等合约规格，只有 MT5 终端能给。服务端只下发风险或网格参数，节点负责取规格、算手数、下单：

| 层 | 职责 | 文件 |
| --- | --- | --- |
| 服务端 · 准入 | 模版2 缺止损、模版3 方向不相容等在分发前挡下；`template_ids` / `group_ids` 定向筛分组 | `group_rules.entry_reject_reason` / `template_reject_reason` / `group_targeted` |
| 服务端 · 下发 | `strategy_start` 携带 `entry` 与策略规则快照；分配节点魔术号 | `group_dispatcher` |
| 节点 · 计算 | 模版2 反推手数 / 模版3 切网格线与追踪平移（纯逻辑） | `risk_sizing.py` / `grid_trading.py` |
| 服务端 · 配置期试算 | 模版3 借节点 K 线与合约规格推荐格数 / 每格手数（只读，不参与分发） | `grid_sizing.py` / `grid_sizing_api.py` |
| 节点 · 执行 | 读规格、下单、改单、上报事件与 `detail` | `strategy_runner.py` |

节点侧按策略快照里启用的规则类型选执行路径（模版1 加仓 / 模版2 以损定量 / 模版3 网格互斥）。断线恢复时：模版3 优先用 `runtime_json` 还原网格，无 runtime 再按持仓备注与现价猜测平移；模版2 按 `R3B*` 备注还原已开档位；无法还原则降级为只监控到全平。

### 9.9 账户级风控（节点侧常驻）

与信号分发正交：配置权威存 `nodes.risk_json`，登录成功与保存时经 WS 同步到节点，由节点本地循环监控并自动平仓。规则类型：

| 规则键 | 作用 |
| --- | --- |
| `float_pl_ratio` | 浮盈/浮亏相对余额达到阈值后清仓 |
| `equity_min` | 净值低于指定 USD 后清仓 |
| `symbol_pl_orders` | 品种盈亏金额 + 订单数条件 → 按动作平仓（可多条） |
| `symbol_pl_protect` | 品种浮盈亏保护状态机（触发后收窄阈值） |
| `lot_pl_tiers` | 按持仓手数分档 + 盈亏金额平仓 |

公共字段含 `enabled`、`monitor_mode`（`loop` 循环 / `times` 限次）、`max_times` / `remaining_times`。规范化与校验见 `backend/app/risk_control.py`；节点执行见 `node_client` 风控模块。

---

## 10. MT5 操作规则（与库代码保持一致）

> 本章逐条对齐参考库 `mt5_client.py`，节点客户端**直接复用**该模块，保证"MT5 操作规则一致"。

### 10.1 连接管理

- `mt5.initialize()` 初始化；失败记录 `mt5.last_error()`。
- 连接后读取 `account_info()`（login/server/balance/equity/margin/free_margin/leverage）。
- `is_connected()` = `connected and terminal_info() is not None`。

### 10.2 符号解析 `resolve_symbol`（与库一致）

- 处理券商后缀：TradingView 的 `XAUUSD` 可能对应 MT5 的 `XAUUSD.m` / `XAUUSD+`。
- 先大小写不敏感精确匹配；再前缀/去后缀部分匹配；多匹配时取最短名（基础符号）。

### 10.3 开仓 BUY / SELL（与库一致）

- 解析符号 → `symbol_select(symbol, True)`。
- 取价：**BUY 用 ask，SELL 用 bid**。
- 体积校验：低于 `volume_min` 或高于 `volume_max` 直接报错返回。
- 下单请求参数（与库完全一致）：

```python
request = {
    "action": mt5.TRADE_ACTION_DEAL,         # 市价成交
    "symbol": symbol,
    "volume": volume,
    "type": mt5.ORDER_TYPE_BUY,              # 或 ORDER_TYPE_SELL
    "price": price_info["ask"],              # SELL 用 bid
    "deviation": Config.DEFAULT_SLIPPAGE,    # 默认 10
    "magic": magic or Config.DEFAULT_MAGIC_NUMBER,  # 默认 20240615
    "comment": comment or "TV Signal",
    "type_filling": filling_mode,            # 自动探测：FOK→RETURN→IOC
}
# 仅当 sl/tp > 0 时才加入 request["sl"] / request["tp"]
```

- **填充模式探测**（与库一致）：读 `symbol_info.filling_mode` 优先选 FOK，否则 RETURN，否则 IOC；取不到时回退 RETURN。
- 结果判定：`retcode == TRADE_RETCODE_DONE` 为成功，返回 deal/order/volume/price。

### 10.4 平仓（与库一致）

- `close_position(ticket, volume=None)`：按持仓反向市价单平仓，`position=ticket`，BUY 持仓用 SELL 平、SELL 持仓用 BUY 平，价格相应取 bid/ask；默认平全部手数。
- `close_all` / `close_symbol` / `close_by_magic`：批量平仓一律**先撤挂单再平仓**。市价平仓采用「爆发式首轮（`max_retry=0`）+ 瞬时失败再退避重试」；对冲账户的全平/按品种平仓会先用 `TRADE_ACTION_CLOSE_BY` 把同品种多空对敲（一次请求锁两笔），剩余再市价平。Python `MetaTrader5` 包非线程安全，仍在节点专用单线程执行器上串行 `order_send`，不并行开线程。
- `close_by_magic(magic)`：按魔术号批量平仓（**不对敲**，避免误平其它任务的反向仓）。
- **CLOSE 信号语义（与库 `execute_trade` 一致）**：收到 `action=CLOSE` 即对该 `symbol` 执行 `close_all_positions(symbol)`。

### 10.5 查询

- `get_positions(symbol=None)`、`get_position_by_ticket(ticket)`、`get_symbol_info(symbol)`、`get_current_price(symbol)`——字段与库一致，用于上报与平仓。

### 10.6 默认参数（与库 `config.py` 一致）


| 参数                     | 默认值      |
| ---------------------- | -------- |
| `DEFAULT_LOT`          | 0.1      |
| `DEFAULT_SLIPPAGE`     | 10       |
| `DEFAULT_MAGIC_NUMBER` | 20240615 |
| `MAX_LOT_SIZE`         | 1.0      |
| `MAX_POSITIONS`        | 5        |

> 注：参考库中 `MAX_POSITIONS` 已定义但**未在代码中启用**；本系统在风控/分发层显式使用（如需要可配合 9.3 限制单账户最大持仓数）。

### 10.7 错误码与重试策略

节点对 `order_send` 的返回 `retcode` 分类处理，**重试只在节点本地针对同一 `signal_id` 进行**（不回传后台重发，避免重复单）：

| 类别 | 典型 retcode | 处理 |
| --- | --- | --- |
| 成功 | `DONE`(10009) / `DONE_PARTIAL`(10010) | 记录 deal/order，回执 success |
| 可重试（瞬时） | `REQUOTE`(10004)、`PRICE_CHANGED`(10020)、`PRICE_OFF`(10021)、`TIMEOUT`(10012)、`TOO_MANY_REQUESTS`(10024)、`CONNECTION`(10031) | **刷新报价**后退避重试（默认 3 次：200ms / 500ms / 1s） |
| 填充模式不支持 | `INVALID_FILL`(10030) | 按 FOK→RETURN→IOC 切换 `type_filling` 重试（配合 `_get_supported_filling_mode`） |
| 业务致命（不重试） | `NO_MONEY`(10019)、`INVALID_VOLUME`(10014)、`INVALID_STOPS`(10016)、`INVALID_PRICE`(10015)、`MARKET_CLOSED`(10018)、`TRADE_DISABLED`(10017)、`INVALID`(10013) | 立即判 `failed`，回执错误并**告警**到管理端 |

- **刷新报价**：可重试类在每次重试前重新 `get_current_price`（BUY 用 ask / SELL 用 bid），必要时放宽 `deviation`。
- **结果为 None**：`order_send` 返回 `None` 时读取 `mt5.last_error()` 记录，按连接类错误处理（重连 MT5 后重试）。
- **重试边界**：超过最大次数仍失败 → `failed` + 告警；轮询模式下按 9.6 跳过该节点、继续下一个，不阻塞整体。
- **审计**：每次 `order_send` 的 `retcode/comment/deal/order/price/volume` 落 `signal_dispatch`（5.2），用于对账与定位。


---

## 11. 远程平仓（表 10）

支持**单账户**与**全局**远程平仓，由 Web 管理端触发，经后台下发 `command(cmd=close)`。


| 操作        | 触发            | 下发                                      |
| --------- | ------------- | --------------------------------------- |
| 单账户—全部平仓  | 选定节点          | `close_target=all`                      |
| 单账户—按品种平仓 | 选定节点 + 品种     | `close_target=symbol, close_symbol=XXX` |
| 单账户—指定单平仓 | 选定节点 + ticket | `close_target=ticket, close_ticket=N`   |
| 全局—全部平仓   | 一键全局          | 向所有在线节点广播 `close_target=all`            |
| 全局—按品种平仓  | 全局 + 品种       | 向所有在线节点广播 `close_target=symbol`         |


- 节点收到后调用对应的 `close_position` / `close_all` / `close_symbol`（第 10.4），回传逐单结果。批量全平与按品种平仓在节点侧走爆发式连发，对冲锁仓会先对敲；**不是**多线程并行 `order_send`（官方 Python 包非线程安全，且券商交易通道本身串行）。
- **账户级全平**（`close_target=all`）会先对该节点所有未收口的策略子任务下发 `strategy_stop`，节点在执行 `close_all` 前也会 `request_stop` 全部 runner，避免网格/限价监控把仓补回来。按品种、按订单平仓不终止策略任务。
- 全局平仓为高危操作：Web 端二次确认 + 操作审计日志（记录操作人、时间、范围、结果）。

---

## 12. Web 管理端（表 9）

PC 与手机 H5 自适应，同一套前端代码（响应式布局）。

### 12.1 功能模块


| 模块    | 功能                                       |
| ----- | ---------------------------------------- |
| 总览    | 节点在线数、总权益/浮盈、活跃持仓、分发/回报事件流（实时）               |
| 节点管理  | 新增/编辑/启用禁用/删除节点、按币种分发配置（手数/参与开关/轮询序）、**账户级风控** |
| **策略** | 基于内置模版创建/编辑策略实例（模版1 逆势顺势、模版2 以损定量、模版3 网格）；`/strategies` |
| **分组** | 创建分组、绑定唯一策略、维护成员与组内分发模式；查看分组信号与子任务事件；`/groups` |
| **中控台** | **按品种准入清单**、方向总开关、分发模式/持仓范围、多区间过滤、全局手数；**手动发信号**；**清理交易日志** |
| 配置    | 修改管理员密码、2FA、**节点令牌（NODE_TOKEN）**：显示/隐藏、复制、重置 |
| 账户监控  | 各节点 balance/equity/margin、持仓列表（实时刷新，表 2） |
| 远程平仓  | 单账户/全局平仓操作面板（表 10）                       |
| **事件** | Webhook 信号历史：原始请求体 + 解析字段 + 各节点分发/执行明细（`/events`） |
| 审计    | 管理端敏感操作审计（`/audits`） |
| 信号与日志 | 节点详情内信号/成交回报 Tab；分组信号详情页 |


### 12.2 实时性

- 管理端通过 `wss://.../ws/admin`（管理员 JWT 鉴权）订阅实时事件：节点上下线、账户快照更新、信号分发与执行结果、轮询队列告警。
- 后台事件 → 进程内事件总线 → 推送给所有已连接的 admin WS。

### 12.3 前端技术

- **Vue 3 + TypeScript**：统一使用 `<script setup lang="ts">` + Composition API；Vite 构建。
- UI 组件库 Element Plus（或 Naive UI，二者均自带完整 TS 类型）；状态管理 **Pinia**，路由 **Vue Router**，HTTP 用 **Axios**（按后台接口封装请求/响应的 TS 类型）。
- WebSocket 封装为带类型的 composable（如 `useAdminSocket()`），其事件模型与后台 `/ws/admin` 推送的消息结构共用一份 TS 类型定义（`types/`），保证前后端契约一致。
- 全局开启 `strict` 类型检查；提交前 `vue-tsc --noEmit` 做类型校验。
- `vw/vh` + 媒体查询实现 PC/H5 自适应；可由 FastAPI 以 `StaticFiles` 托管打包产物，单服务部署。

---

## 13. API 接口设计

### 13.1 信号入口（兼容参考库）


| 方法   | 路径         | 说明                                 |
| ---- | ---------- | ---------------------------------- |
| POST | `/webhook` | 接收 TradingView 信号（Bearer + IP 白名单） |


### 13.2 节点管理（管理员鉴权）


| 方法     | 路径                                  | 说明                 |
| ------ | ----------------------------------- | ------------------ |
| POST   | `/api/nodes`                        | 手动注册节点（返回 `NodeOut`，不含 token；`mt5_login` 重复返回 409） |
| GET    | `/api/nodes`                        | 节点列表（含在线状态）        |
| GET    | `/api/nodes/{id}`                   | 节点详情 + 最新账户快照      |
| PATCH  | `/api/nodes/{id}`                   | 修改备注/手数模式/参与开关/风控/趋势参数（`mt5_login` 创建后不可改）|
| DELETE | `/api/nodes/{id}`                   | 删除节点               |
| GET    | `/api/nodes/{id}/trend`             | 趋势面板数据：向该节点取 K 线并算 EMA/RSI 与综合得分（见 §18.10）|

> 启用/禁用通过 `PATCH /api/nodes/{id}` 的 `enabled` 字段切换；不再提供独立的 `/enable` `/disable` 端点。每节点 `rotate-token` 已废弃，由 §13.3 的全局 `/api/config/node-token/rotate` 替代。


### 13.3 配置


| 方法      | 路径                              | 说明                           |
| ------- | ------------------------------- | ---------------------------- |
| GET/PUT | `/api/config/lot`               | 全局统一手数（表 3）                  |
| POST    | `/api/nodes/lot`                | 批量设置节点手数（表 3）                |
| GET/PUT | `/api/config/filters`           | 按品种规则：准入 + 方向总开关 + 分发/持仓范围 + 区间过滤（表 4、7、8） |
| GET/PUT | `/api/config/trend`             | 趋势面板全局参数（EMA/RSI；全后台共享，落 `system_setting`） |
| GET     | `/api/config/node-token`        | 获取全局节点接入令牌（明文；管理员 JWT）       |
| POST    | `/api/config/node-token/rotate` | 重置全局节点接入令牌（旧令牌立即失效；落审计）      |


### 13.4 账户与持仓


| 方法  | 路径                          | 说明            |
| --- | --------------------------- | ------------- |
| GET | `/api/accounts`             | 全部节点账户快照（表 2） |
| GET | `/api/nodes/{id}/positions` | 指定节点持仓        |


### 13.5 信号事件（v0.4 新增）


| 方法  | 路径                    | 说明                                                         |
| --- | --------------------- | ---------------------------------------------------------- |
| GET | `/api/events/signals` | 分页列出 Webhook 信号事件（`page`/`page_size`，默认 1/20，上限 100） |

响应 `PaginatedSignalEvents`：每条 `SignalEventRecord` 含 `signal_id`、`received_at`、`source_ip`、**`raw_payload`**（Webhook 原始 JSON/文本）、解析字段（`action`/`symbol`/`volume`/`sl`/`tp` 等）、`dispatch_mode`、`status`，以及嵌套 `dispatches[]`（各节点的 `decided_vol`/`skip_reason`/`status`/`retcode`/`order`/`deal`/`error` 等）。**不含**手动平仓记录（`dispatch_mode=manual`）。


### 13.6 远程平仓（表 10）


| 方法   | 路径                      | 说明                       |
| ---- | ----------------------- | ------------------------ |
| POST | `/api/nodes/{id}/close` | 单账户平仓（all/symbol/ticket） |
| POST | `/api/close-all`        | 全局平仓（all/symbol，二次确认）  |
| POST | `/api/close-batch`      | 多节点批量平仓（指定 node_ids 子集）|


### 13.7 策略与分组（v0.6）


| 方法     | 路径 | 说明 |
| ------ | --- | --- |
| GET    | `/api/strategies/templates` | 内置策略模版列表（`tpl_1` / `tpl_2` / `tpl_3`） |
| GET/POST | `/api/strategies` | 策略实例列表 / 创建 |
| GET/PATCH/DELETE | `/api/strategies/{id}` | 查询 / 更新 / 删除策略 |
| GET/POST | `/api/groups` | 分组列表 / 创建（可绑策略、成员） |
| GET/PATCH/DELETE | `/api/groups/{id}` | 查询 / 更新 / 删除分组 |
| GET    | `/api/groups/{id}/signals` | 分组策略信号分页 |
| GET    | `/api/groups/{id}/dispatches/{dispatch_id}/events` | 节点子任务事件流（含 `detail_json`） |
| POST   | `/api/console/manual-signal` | 手动对分组发 BUY/SELL/CLOSE（走 strategy 链路） |
| POST   | `/api/console/purge-trade-logs` | 清理交易相关日志（需确认短语；会尝试停止运行中策略） |


### 13.8 WebSocket


| 路径                   | 用途              |
| -------------------- | --------------- |
| `/ws/node` | 节点长连接（首包 `auth` 含 `token`+`mt5_login`，未注册的 `mt5_login` 自动入库，见第 6 章）；可下发 `strategy_start` / `strategy_stop` / 风控配置 / `market_probe`（只读行情探针，回包 `market_probe_result`，含 `bars` / `quote` / `spec`；`spec` 读不到时省略而非报错）|
| `/ws/admin`（JWT）     | 管理端实时数据推送，事件含 `snapshot/node_status/node_registered/node_rejected/account/dispatch/trade_result` |


> 兼容性：参考库的 `/positions`、`/position/{ticket}`、`/symbol/{symbol}`、`/health`、`/connect`、`/disconnect` 在新系统中演变为"针对某节点"的操作（`/api/nodes/{id}/...`），保留 `/health` 作为后台自身健康检查。

---

## 14. 安全设计


| 面向      | 机制                                                                                             |
| ------- | ---------------------------------------------------------------------------------------------- |
| Webhook | `Authorization: Bearer <token>`（`secrets.compare_digest`）+ IP 白名单（支持 `X-Forwarded-For`），与参考库一致 |
| 节点连接    | **全局 `NODE_TOKEN`**（明文 + 常量时间比较，可在管理端查看/重置）+ `mt5_login` 唯一识别 + 首次自动注册；仅 `enabled=True` 节点可在线 |
| 管理端     | 管理员账号 + JWT + 可选 TOTP；敏感操作（全局平仓、**重置节点令牌**、改配置）二次确认 + 审计日志                                    |
| 传输      | 全链路 TLS（wss/https），建议 Nginx 反代 + 证书                                                            |
| 速率/防护   | `/webhook` 与登录接口限流；异常来源告警                                                                      |
| 审计      | 关键操作（注册/平仓/改配置/**rotate_node_token**）落库，含操作人/时间/参数/结果                                          |


---

## 15. 部署方案

### 15.1 拓扑

- **管理后台**：Linux 或 Windows 均可（不依赖 MT5），Uvicorn + Nginx(反代/TLS) + Redis + MySQL 8；**生产推荐用 Docker Compose 一键部署（见 15.5）**。
- **MT5 节点**：每台 Windows 云电脑安装 MT5 终端 + 节点客户端（Python），在 `.env` 配置全局 `NODE_TOKEN` + 本机 `MT5_LOGIN` 后开机自启；新 MT5 账户首次接入会自动注册。

### 15.2 后台运行（裸机 / 调试）

> 生产推荐直接用 **15.5 Docker 部署**；本节为裸机或本地调试运行方式。

```bash
# 多 worker 时需用 Redis Pub/Sub 路由（见第5章），WS 连接有状态，建议先单 worker 起步
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
# 生产：Nginx 反代 443 → 127.0.0.1:8000，配置 wss 升级头
```

### 15.3 Nginx（WebSocket 反代要点）

```nginx
location /ws/ {
    proxy_pass http://127.0.0.1:8000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 3600s;            # 长连接保活
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

### 15.4 节点客户端

```bat
:: run_node.bat（开机自启 / 任务计划程序）
python node_client.py
```

- 节点需在 MT5 中开启自动交易（"算法交易"按钮），登录目标账户。
- 建议节点客户端内置断线重连、异常自重启（如配合 `nssm` 注册为 Windows 服务）。
- 打包用 `build_exe.bat`（onefile）/ `build_exe_onedir.bat`（onedir）；版本号在构建时自动生成为 `${数字版本号}-${年月日时分秒}`，规则见 §18.17。

### 15.5 服务端 Docker 部署（推荐）

> **仅服务端容器化**：管理后台 + Redis + MySQL + Nginx 用 Docker Compose 编排。**MT5 节点不纳入 Docker**——节点依赖 Windows 上的 MT5 终端（GUI/算法交易），仍按 15.4 在 Windows 部署。后台镜像不含 `MetaTrader5` 依赖。

#### 15.5.1 后台镜像 `backend/Dockerfile`

```dockerfile
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 PIP_NO_CACHE_DIR=1 TZ=Asia/Shanghai
WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends gcc curl \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY app ./app
COPY alembic ./alembic
COPY alembic.ini .

EXPOSE 8000
HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
    CMD curl -fsS http://localhost:8000/health || exit 1

# WS 长连接有状态：单进程起步（横向扩展见第 5 章 Pub/Sub 路由）
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]
```

#### 15.5.2 编排 `docker-compose.yml`

```yaml
services:
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      TZ: Asia/Shanghai
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [appnet]

  redis:
    image: redis:7-alpine
    restart: always
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 10
    networks: [appnet]

  backend:
    build: ./backend
    restart: always
    env_file: .env
    environment:
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      MYSQL_DSN: mysql+asyncmy://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:3306/${MYSQL_DATABASE}
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }
    expose: ["8000"]          # 仅内网暴露，由 nginx 对外
    networks: [appnet]

  nginx:
    image: nginx:1.27-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/nginx/certs:/etc/nginx/certs:ro
    depends_on: [backend]
    networks: [appnet]

volumes:
  mysql_data:
  redis_data:

networks:
  appnet:
```

#### 15.5.3 编排环境变量 `.env`

```ini
# ---- MySQL ----
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=mt5hub
MYSQL_USER=mt5
MYSQL_PASSWORD=change_me
# ---- Redis ----
REDIS_PASSWORD=change_me_redis
# ---- 应用（与 backend 共用）----
ENABLE_AUTH=True
AUTH_TOKEN=your_webhook_bearer_token
JWT_SECRET=your_jwt_secret
LOG_LEVEL=INFO
```

#### 15.5.4 Nginx `deploy/nginx/default.conf`

```nginx
upstream backend { server backend:8000; }

server {                       # 80 跳转 443（生产）
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name your-domain.com;
    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    location /ws/ {                       # 节点/管理端 WebSocket
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;          # 长连接保活
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

#### 15.5.5 启动与运维

```bash
# 1) 准备 .env 与证书：deploy/nginx/certs/{fullchain,privkey}.pem
# 2) 构建并后台启动
docker compose up -d --build
# 3) 首次/升级数据库迁移
docker compose exec backend alembic upgrade head
# 4) 查看状态与日志
docker compose ps
docker compose logs -f backend
# 5) 发布新版本（仅重建后台）
docker compose build backend && docker compose up -d backend
# 6) 数据备份（定时任务）
docker compose exec mysql sh -c 'mysqldump -u$MYSQL_USER -p$MYSQL_PASSWORD $MYSQL_DATABASE' > backup_$(date +%F).sql
```

要点：
- **WS 有状态**：`backend` 默认单副本。需多副本时先落地第 5 章 Redis Pub/Sub 路由，再在 Nginx 对 `/ws/` 做连接级会话保持。
- **网络隔离**：仅 `nginx` 暴露 80/443；`backend`/`redis`/`mysql` 只在内网 `appnet`，不对外开放端口。
- **数据持久化**：`mysql_data`、`redis_data` 具名卷；Redis 开启 AOF（`--appendonly yes`）。
- **自愈与顺序**：`restart: always` + `healthcheck`；`depends_on: condition: service_healthy` 保证 MySQL/Redis 就绪后再起 backend。
- **密钥管理**：密码仅放 `.env`（加入 `.gitignore`）；更高安全可改用 Docker secrets。
- **时区/编码**：容器统一 `Asia/Shanghai`，MySQL `utf8mb4`。

---

## 16. 项目目录结构

```
robin-project-2026/
├─ backend/                      # 管理后台 (FastAPI + Redis)
│  ├─ app/
│  │  ├─ main.py                 # FastAPI 入口、路由注册、生命周期
│  │  ├─ config.py               # 公共配置（符号映射/关键字/默认参数，对齐参考库）
│  │  ├─ webhook.py              # /webhook 接收 + 鉴权(Bearer/IP)
│  │  ├─ parser.py               # TradingViewParser（移植自参考库，逻辑一致）
│  │  ├─ ws_gateway.py           # /ws/node 网关：鉴权握手、心跳、上报、下发
│  │  ├─ dispatcher.py           # normal 分发引擎：过滤/区间/手数/同步与轮询
│  │  ├─ group_dispatcher.py     # strategy 分组分发：strategy_start/stop、准入
│  │  ├─ group_service.py        # 分组 CRUD / 策略绑定校验
│  │  ├─ group_persist.py        # 分组主任务/子任务/事件持久化与对账
│  │  ├─ group_rules.py          # 魔术号、状态汇总、模版准入拒绝原因
│  │  ├─ groups.py / strategies.py  # 分组 / 策略 REST API
│  │  ├─ strategy_templates.py   # 内置模版1/2/3 与规则规范化
│  │  ├─ strategy_service.py     # 策略实例持久化
│  │  ├─ risk_control.py         # 账户级风控规范化与下发
│  │  ├─ trend_indicators.py     # 趋势面板纯函数：EMA/RSI、综合得分、参数规范化
│  │  ├─ market_probe.py         # 只读行情探针 RPC：req_id 等待表 + 缓存 + 请求合并
│  │  ├─ trend_api.py            # GET /api/nodes/{id}/trend
│  │  ├─ grid_sizing.py          # 模版3 配置期试算纯函数：ATR→格距→格数→每格手数
│  │  ├─ grid_sizing_api.py      # GET /api/nodes/{id}/grid_sizing
│  │  ├─ console_api.py          # 手动信号、清理交易日志等
│  │  ├─ poll_queue.py           # 轮询轮转队列 + 顺延容错
│  │  ├─ nodes.py / accounts.py / close.py / events.py
│  │  ├─ orm.py                  # 含 node_group / trading_strategy / group_* 表
│  │  └─ ...
│  ├─ alembic/
│  └─ tests/                     # 含 test_group_* / test_strategy_api 等
│
├─ node_client/                  # MT5 节点客户端 (Windows)
│  ├─ node_client.py             # WS 连接/鉴权/心跳/上报/收指令执行
│  ├─ strategy_runner.py         # 策略任务执行（模版1/2/3）
│  ├─ risk_sizing.py             # 模版2 以损定量纯逻辑
│  ├─ grid_trading.py            # 模版3 网格切线 / 追踪平移
│  ├─ mt5_client.py              # MT5 操作（对齐参考库）
│  └─ ...
│
├─ frontend/                     # Web 管理端 (Vue 3 + TypeScript)
│  └─ src/views/                 # 总览/节点/策略/分组/事件/中控台/配置/审计
│
├─ deploy/ · docker-compose.yml · .env.example
└─ 使用与部署文档.md / WEBHOOK.md / 生产部署WSS操作文档.md / 技术实现方案.md
```

---

## 17. 开发计划与里程碑


| 阶段       | 内容                                        | 主要交付                  |
| -------- | ----------------------------------------- | --------------------- |
| M1 基础框架  | FastAPI 骨架、Redis 封装、配置/模型、Webhook + 解析器移植 | 信号能被正确接收并解析（对齐参考库）    |
| M2 节点接入  | 全局 NODE_TOKEN + mt5_login 自动注册、`/ws/node` 鉴权握手、心跳、状态管理 | 节点能自动入库并稳定在线（表 1、需求 1）  |
| M3 监控上报  | 账户/持仓上报与存储、`/ws/admin` 实时推送               | 后台实时看到所有账户（表 2）       |
| M4 分发引擎  | 持仓过滤、区间过滤、手数计算、全员同步模式                     | 单条信号广播执行（表 3/4/5/6/7） |
| M5 轮询队列  | 轮询轮转领取、队列、ACK、超时重试 + 顺延容错              | 轮询轮转可用且可容错（表 8）       |
| M6 平仓与前端 | 远程单/全局平仓、Web 管理端 PC+H5                    | 完整可视化操作（表 9、10）       |
| M7 安全与上线 | JWT/审计/限流、TLS、压测与灰度                       | 生产可用                  |
| M8 策略分组 | 节点分组、策略模版1/2/3、账户风控、手动信号与任务账本 | `model=strategy` 全链路可用（§9.8 / §18.5） |


---

## 18. 设计决策确认

> 第一轮评审已逐条确认如下设计点（作为开发依据）：

1. **持仓过滤范围（表 5/6）**：✅ 两种范围都实现，做成可配置项 `config:dispatch.position_scope` 运行时切换，**默认 `symbol` 级**（同品种有持仓才禁止再开仓，与参考库一致；`account` 级为账户有任意持仓即禁止）。
2. **多区间方向过滤的配置层级（表 4）**：✅ 由人工在管理端配置，支持**全局配置**与**节点级配置**两级；**节点未单独配置时回落到全局配置**。判定价格通过取该节点实时获取品种价格。
3. **轮询轮转语义（表 8）**：✅ 参与该品种轮询的节点排成轮转队列，一条信号**只由队首第一个能成交的节点领取消费**（一个信号 = 一个节点开仓），领取后该节点移到队尾循环轮转；队首离线/被过滤/失败则**顺延**下一个节点。**默认初始顺序按 `poll_order`、其次 `created_at` 升序**，可在管理端调整；区别于全员同步的"所有节点并发同时执行"。
4. **手数回落（表 3）**：✅ 节点为 `global` 但全局手数未启用时，回落到信号自带手数 `signal.volume`，并受 `MAX_LOT_SIZE` 约束。
5. **CLOSE 与过滤**：✅ 平仓信号不参与区间/持仓/方向总开关过滤，但仍须品种已在中控台登记；对持有该品种的在线节点广播平仓。
6. **历史持久化**：✅ 实时态用 Redis；审计与历史信号落 **MySQL**，默认保留 **90 天**。
7. **后台部署形态**：✅ 初期**单实例**起步（WS 连接进程内管理）；如需高可用多实例，再按第 5 章接 Redis Pub/Sub 路由。
8. **节点与账户关系**：✅ **一个节点 = 一个 MT5 账户**（一对一）。

### 18.25 导入节点时覆盖已有 `.env`（2026-08-26）

§18.16 决策 21 规定导入时「已存在的 `.env` 一律不覆盖」，避免误伤另一个节点的身份。实际运维里更常见的是：目录里留着上一份过期令牌或别人拷来的 `.env`，导入后看起来成功、节点却连不上。导入本身就会二次确认，再跳过这份文件只会让操作者误以为已经写好了。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 导入遇到已有 `.env` | 继续跳过；只合并地址/令牌；默认跳过、另加「覆盖」勾选 | **勾选「自动写入 .env」时整份覆盖**：与首次导入同一条路径（包内 `.env.example` + 面板连接配置），不再分叉。想保留旧文件就取消勾选。确认框会写明将覆盖、自定义键不会保留 |
| 2 | 版本更新要不要一起改 | 更新时也覆盖 `.env` | **不改**：`apply_package` 仍把 `.env` 放进 `PROTECTED_NAMES`。更新是给已在跑的节点换程序，覆盖身份等于把节点搞下线 |

### 18.24 模版2 限价改为同价挂入场（2026-08-17）

实盘核对后，趋势策略的限价开仓不再沿「入场价 → 止损价」铺入场阶梯。底仓与分散仓全部挂在信号入场价；TP/SL 挂法保持（共用信号止损、分散仓按该入场价铺阶梯止盈）。手数因此回到与市价相同的单值止损距离口径。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 分散仓挂在哪 | 维持 §18.15 的入场阶梯 | **全部挂在信号入场价**：趋势单要的是回调到计划点位后一起进，不是沿途捡更优价；入场阶梯把分散仓挂到更靠近止损处，与「趋势」语义不符 |
| 2 | 手数怎么算 | 继续用加权递减距离 | **单值止损距离（与市价相同）**：同价入场后各档离止损一样远，再按加权距离算会把仓开大、一打止损就超预算 |
| 3 | TP/SL 要不要跟着改 | 各档按自己的挂单价重锚 TP / 拉开 SL | **不改**：SL 本来就共用信号价；TP 本来就锚底仓入场价。同价入场后这两套挂法与市价模式完全对齐 |
| 4 | 风险金额语义 | 维持「全档成交的最坏亏损」文案 | **回到与市价相同的「打到止损 ≈ 风险金额」**：同价挂单通常一起成交，不再用递减距离放大手数 |
| 5 | `ladder_step` 字段 | 删掉 | **保留、恒为 0**：后台明细与前端本来就按 falsy 隐藏，避免改协议 |

### 18.23 运维面板更新检查节奏（2026-08-16）

顶栏「版本更新」提示灯原先启动 3 秒后查一次、之后每 30 分钟复检。30 分钟间隔下，管理员刚设好发布版本，节点机上的橙色提示要等很久才亮。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 复检间隔 | 维持 30 分钟；改成数秒级 | **启动约 3 秒后查一次，之后每 1 分钟复检**：请求只打只读的 `current` 接口，失败仍静默；1 分钟足够让本机尽快看到新发布，又不至于把版本检查打成心跳 |

### 18.22 分组信号关联订单时间对齐 MT5 终端（2026-08-16）

后台事件 `created_at` 是真实 UTC，管理端 `toLocaleString()` 按浏览器时区（北京）显示，IC Markets 终端则是 GMT+2/+3，网格订单列表会比终端快 5～8 小时。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 显示哪套钟 | 一律北京时间；一律 UTC | **按该节点 MT5 服务器时区**：关联订单是拿来跟终端对票的，应对齐券商钟面 |
| 2 | 偏移从哪来 | 写死 IC Markets | **节点用活 tick.time 与 `time.time()` 的差推断**，随账户快照上报 `server_time_offset`；推断不出时 IC Markets 按欧洲夏令时兜底，其它券商仍显示北京时间 |

### 18.21 总览全平同时终止该节点策略任务（2026-08-16）

账户级 `close_all` 会清掉策略仓，但原先不停 runner，网格会重新挂格、未成交限价单会被补挂，后台子任务仍占 busy。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 在哪停 | 只改节点 `_do_close`；只改后台标 done | **后台复用 `strategy_stop` + 节点先 `request_stop`**：账本要标 `closing`、重连才能补发终止而不是 resume；节点必须在 `close_all` 前停监控，否则 asyncio 下 runner 会在全平之后立刻再下单 |
| 2 | 停哪些 | 只停当前品种；只停一个分组 | **该节点上所有未收口子任务（跨分组）**：全平是整账户，留任何一个 runner 都会把仓补回来 |
| 3 | 按票/按品种 | 一样停 | **不停**：那两级不是清账户，策略任务可能还持有其它仓 |
| 4 | 离线节点 | 强制收口 | **在线才停**：离线强制收口却没平仓，会放出 busy、留下没人管的仓。全平接口对离线本就是 409 |

### 18.20 远程全平要「一瞬间」清仓（2026-08-16）

管理端「平掉该节点全部」走账户级 `close_all`。客户期望终端上像一键全平那样几乎同时消失，但 Python `MetaTrader5` 没有 `OrderSendAsync`，包也非线程安全；节点本来就把所有 MT5 调用排进 `max_workers=1` 的专用线程，并行 `order_send` 会串 `last_error`、和策略监控抢同一批仓，并更容易吃到 10024。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 怎么变快 | 多线程并行 `order_send`；写 MQL5 EA 用 `OrderSendAsync` | **爆发式首轮 + 失败再重试**：首轮 `max_retry=0`、失败单不 sleep，把请求尽快打进终端；requote / 10024 等瞬时错误再带退避重试。观感接近终端自带的一键全平，仍走现有节点通道，不必另挂 EA |
| 2 | 对冲锁仓 | 多空都走反向市价 | **全平 / 按品种先 `TRADE_ACTION_CLOSE_BY`**：一次请求锁两笔、无滑点；券商拒绝对敲则整波改回市价。按魔术号收口**不对敲**，避免误平其它任务的反向仓 |
| 3 | 填充模式 | 每笔都从 FOK 试起 | **记住该品种上次成功的 filling**：批量平仓时少一次 10030 往返 |
| 4 | 10024 | 当致命错误丢掉 | **并入 `TRANSIENT`**：爆发式连发时券商常见，第二轮重试才能平干净 |

### 18.19 运维面板的版本号与分发压缩包（2026-08-13）

`build_exe.bat` 除 exe 外再打一份 `packages\node_client_dashboard-<版本>.zip`，面板自身也有了版本号（形态与 node_client 一致，见 §18.17），窗口标题会显示它。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 版本号怎么定 | 只给 zip 名加时间戳，不引入版本体系 | **完整复用 §18.17 那套**：面板管着一批客户端，排障时两个版本号常并列出现，格式不统一就没法比对；`_build_info.py` 冻进 exe、`version.txt` 进包，两个落点与 node_client 同口径 |
| 2 | 冻进 exe 的版本往哪显示 | 不显示，纯粹作标识 | **写进窗口标题**：版本冻进去却无处可看等于没有；标题栏是报障时最容易截到的地方 |
| 3 | 包内结构 | 平铺，与 node_client 安装包一致 | **带一层 `node_client_dashboard-<版本>\` 目录**：node_client 的包要被 `apply_package` 按文件树覆盖安装所以必须平铺，面板这个没有自动安装通道、纯靠人解压，平铺会把文件铺一地 |
| 4 | 哪些不能入包 | 只排除日志与缓存 | **`panel_config.json` 与 `instances.json` 必须排除**：前者存后端地址与 `NODE_TOKEN`，后者是本机实例的绝对路径清单。两者就写在 exe 同目录，只要在 `dist\` 里跑过一次面板就会生成 —— 这与 node_client 排除 `.env` 是同一条红线 |
| 5 | 何时校验泄露 | 分发前人工检查 | **构建期自检**：必须含主程序、必须不含上述两个文件，否则删产物并让构建失败。包发出去之后才发现令牌跟着走了就已经晚了 |

`.bat` 有两条硬约束，这次是踩了才补上的（脚本头部已写明）：**必须 CRLF 行尾**，cmd 按 CRLF 切行，用 LF 会把 `REM` 注释和紧随其后的命令连成一行，现象是满屏 `'rtual' 不是内部或外部命令` 这类被截半的命令名；**注释必须 ASCII**，cmd 先解析重定向再处理 `REM`，注释里一个裸的 `<` 就会被当成输入重定向而报「系统找不到指定的文件」，UTF-8 中文在系统代码页下双字节错位解码同样可能暴露出这类字节。

### 18.18 面板对话框的遮挡与显示（2026-08-13）

四个对话框（版本更新 / 连接配置 / 导入节点 / 节点配置）原本都用「`withdraw()` 遮住构建过程 → `after(20)` 里算好居中位置再 `deiconify()` + `grab_set()`」这套写法。在 customtkinter 6.x 下这会**卡死整个面板**：`CTkToplevel.__init__` 自己就会 withdraw 窗口去设置暗色标题栏，并挂一个延时回调把窗口恢复到「之前的状态」；我们在这个窗口期里再调一次 `withdraw()`，那个回调记下的「之前状态」就变成了 `withdrawn`，于是在对话框已经显示并抓取输入之后，又把它按回隐藏。现象是对话框一闪而过，而输入权仍握在这个看不见的窗口上，主窗口连最小化和关闭都点不动，只能强杀进程。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 构建期怎么遮挡 | 继续 `withdraw()`，显示前把 customtkinter 的内部标记复位 | **改用 `-alpha` 透明度**：窗口状态自始至终是 `normal`，那套恢复逻辑拿到的「之前状态」永远正确，不必去碰库的私有字段（版本一升就会失效） |
| 2 | 放哪 | 四份各自就地改 | **抽 `dialog_window.py` 共用**：原本四处是逐字复制的同一段代码，分头改必然漏掉一处，而漏掉的那处症状是主窗口死锁 |
| 3 | 要不要兜底 | 只改遮挡方式 | **额外在 `<Unmap>` 上释放 grab**：隐藏窗口持有输入权的代价是整个面板失去响应、只能强杀，这一行保证任何路径下窗口一旦不可见就交还输入权 |
| 4 | 居中计算 | 留在各对话框里 | **提成纯函数 `centered_origin`**：父窗口贴边、比对话框还小、被拖出屏幕外这几种情况都要夹紧坐标，脱开 Tk 才好单测（`test_dialog_window.py`）|
| 5 | 「更新到指定版本」「回滚到本机备份」怎么选版本 | 沿用 `CTkInputDialog` 手输版本号 | **新增 `version_picker.py` 下拉候选清单**：版本号形如 `1.1.0-20260813090507`，手抄错一位要等到下载 404 或「版本不可用」才发现；`CTkInputDialog` 也不受 `dialog_window` 管，位置由库决定、不居中于父窗口。改成下拉后还顺带补上了完整性校验 —— 清单里带 sha256，手输那条路径拿不到校验和只能跳过 |
| 6 | 两个入口数据源不同（后端清单 / 本机备份）怎么共用 | 各写一个对话框 | **候选项由 `loader` 在后台线程提供**，远端拉取与本机枚举共用同一套 UI；详情区按字段存在性渲染，所以缺 size / sha256 的本机备份也能正常显示。模态语义收敛在 `_ask()` 里：子窗口的 `grab_set` 会顶掉父窗口的，关闭后必须交还输入权，否则父对话框失去模态、主窗口变成可点 |

同一轮修掉 `ImportNodeDialog._register` 的重名：`tkinter.Misc._register` 是内部方法，`Toplevel` 初始化时用它注册 `WM_DELETE_WINDOW` 回调，子类定义同名方法会让对话框在 `super().__init__()` 里就构造失败（「导入节点」必崩）。已改名为 `_register_instance`。

另修掉一处**面板范围内的系统性错误**：后台线程里 `except ... as e:` 紧接 `self.after(0, lambda: ...str(e))`。`e` 在 except 块结束时会被解绑，而 lambda 是延迟执行的，跑起来只会抛 `NameError`，**所有异步操作的失败提示因此都显示不出来**（GUI 形态下 stderr 也看不见，表现为静默失败或界面一直停在「正在…」）。`version_dialog`、`import_dialog`、`connection_dialog`、`version_picker`、`app_ui` 五处 `_run_async` 一律改成先 `msg = str(e)` 再交给 lambda。往面板里新增异步操作时注意别再写回原样。

「版本更新」入口同时从左栏挪到顶栏右上角，与「连接配置」并列，并兼作更新提示灯：启动 3 秒后查一次后端发布版本、之后每 1 分钟复检（见 §18.23），有实例落后就把按钮变成橙色的「有新版 vX.Y.Z（待更新数）」。几点取舍：网络请求走后台线程，后端入口在主线程解析好再传进去（Tcl 非线程安全，与 §18.16 决策 9 同一约束）；**检测失败一律静默**，一个连不上的后端不该弹窗打断本机运维；待更新数由纯函数 `count_pending_upgrades` 算，**版本号读不出来的实例不计入**，否则提示会常亮到没人再看；装完新版或增删实例后只用缓存的后端版本重算，不再发请求。

### 18.17 客户端构建版本号自动生成（2026-08-13）

打包脚本在构建时自动生成版本号 `${数字版本号}-${年月日时分秒}`（如 `1.1.0-20260813090507`）：`version.py` 只留手工维护的 `BASE_VERSION`，时间戳由 `build_version.py` 追加，一份冻结进 exe、一份写成产物目录的 `version.txt`。

动因来自 §18.16 那套版本管理的一处空档：版本号同时是主键、发布指针、磁盘文件名与升降级判据，唯独它自己还靠人手改常量。同一个 `1.1.0` 反复打包会得到内容不同而版本号相同的多个安装包——上传第二个直接 409，装到节点上也分不清谁是谁；而每次手工递增又必然有人忘。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 怎么让版本号自增 | 自动递增构建号（`1.1.0.42`） | **追加 `-${年月日时分秒}`**：后端 `compare_versions` 抽数字段逐位比较，时间戳天然单调，晚构建的一定判为更新；递增构建号要么把计数写回被跟踪的源码（每次打包脏一次工作区），要么依赖 CI 计数器，而这里全靠人在 Windows 机器上手工跑 bat，没有 CI |
| 2 | 生成的版本号存哪 | 构建时改写 `version.py` 的常量 | **常量只存数字版本号，时间戳落到生成物 `_build_info.py`**：改写被 git 跟踪的源码会让每次打包都弄脏工作区，也容易连着构建号一起误提交；生成物进 `.gitignore` |
| 3 | exe 怎么知道自己的版本 | 只靠旁路 `version.txt` | **`_build_info.py` 在 PyInstaller 分析之前生成，随包冻结**：单独分发的 exe 一旦没带上 `version.txt`，就会退回裸的 `1.1.0` 上报，被后端判成比任何构建产物都旧，面板则会把它当成「需更新」反复推 |
| 4 | 两个落点谁优先 | 内置版本优先 | **旁路 `version.txt` 优先**：与面板 `read_version_near()` 及 §18.16 决策 8 的备份口径一致（备份里 exe 与 `version.txt` 是一起存取的），改成内置优先会让面板显示与节点上报在「只换 exe」的场景下对不上 |
| 5 | 超长怎么办 | 等后端上传时拒收 | **`make_version` 当场抛错、构建失败**：后端 `_VERSION_RE` 限死 32 字符，留到上传才暴露时包已经打完；反算下来数字版本号最长 17 位 |
| 6 | onedir 形态 | 维持现状 | **补上 `version.txt` 写出**：原本只有 onefile 脚本写，onedir 产物打成 zip 上传时后端读不到版本号（要手填），面板也无从核对已装版本 |
| 7 | 源码直跑怎么报版本 | 运行时现凑一个时间戳 | **上报裸 `BASE_VERSION`**：源码跑本就没有「构建」这回事，现凑时间戳会让没打包的开发机看起来比正式发布还新；裸版本号在补零比较下排在所有构建产物之前，正好表达「更旧」|

### 18.16 客户端版本管理与远程更新（2026-08-13）

后端新增客户端安装包的上传、版本清单与发布指针；本机运维面板作为更新代理，从后端拉包后批量更新各实例，并支持降级与本机回滚；node_client 在鉴权首包上报版本，让后台看到全网实际运行的版本分布。

这项改造的约束来自 Windows：**正在运行的 exe 无法覆盖自己**。所以"谁执行替换"决定了整体形态——面板本就持有停止、备份、重启的完整能力，还有守护线程会在进程退出后自动拉起，让 node_client 自更新等于和面板抢进程控制权。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 谁执行替换 | node_client 自更新（下载后释放 updater 子进程，自己退出后由它覆盖并重启） | **面板代理执行**：Windows 文件锁决定了自更新必须引入独立 updater 进程，而面板的守护线程会在 node_client 退出后按 `restart_delay_s` 自动拉起**旧 exe**，两者必然打架；面板侧 `batch_replace` 的「停 → 等句柄释放 → 覆盖 → 按需再启」流程已验证可用，直接复用 |
| 2 | 面板怎么连后端 | 面板内用管理员账号密码换 JWT | **复用节点 `NODE_TOKEN`**：面板是本机运维工具，在桌面端存管理员凭据会把后台权限下放到每台节点机；`NODE_TOKEN` 本就明文躺在同机的 `node_client/.env` 里，面板读它不扩大暴露面 |
| 3 | `NODE_TOKEN` 的授权范围 | 顺带允许它改发布版本 | **只授权 `current` 与 `download` 两个只读接口**：该令牌自 v0.2 起全局共享一份，给它写权限等于任何一台节点机都能改全网的发布版本；上传 / 发布 / 降级 / 删除一律留在管理员 JWT，并有专门用例守住这条线 |
| 4 | 后端地址从哪来 | 只允许手工配置 / 只从 .env 自动发现 | **手工配置优先，缺项回落实例 `.env`**：新装机器上还没有实例时 `.env` 无从读起，多个实例也可能连不同后端；地址与令牌各自独立回落，覆盖「只换了域名、令牌沿用」这个常见场景 |
| 4a | 配置入口放哪 | 只放在版本更新对话框里 | **顶栏独立「连接配置」为主入口，版本更新对话框内保留就地编辑**：连接配置是面板级设置而非某个实例的属性，埋在更新弹窗里不易发现，且新装机器需要「先配好再更新」；两处读写同一份 `panel_config.json`。入口不要求已有实例——恰恰是没有实例时最需要手工填 |
| 4b | 配置怎么验证 | 保存后到版本更新里试 | **「测试连接」直接调 `current` 接口**：地址、令牌、网络三者任一不对都会卡住更新，而错误只在点更新时才暴露；单独区分「连通但尚未发布任何版本」与「连不上／令牌错」，前者不是故障 |
| 4c | 自动发现的值要不要回显进输入框 | 只在下方列出发现结果，输入框保持空 | **回显**：输入框空着而下方显示「已发现 http://…」，用户无法判断是否还要手工抄一遍。回显只在打开时做一次且仅填空字段，「清空」后不再回填——否则清空按钮失效，也就没法退回「跟随 .env」模式 |
| 5 | 包体存哪 | 存 MySQL BLOB | **存磁盘 + 表里只记元数据与 sha256**：包体几十 MB，进库会把备份与复制代价放大；代价是必须挂卷，已在两份 compose 里配 `backend_data` 卷并注入 `CLIENT_PACKAGE_DIR` |
| 6 | 「当前发布版本」放哪 | 在 `client_version` 表上加 `is_current` 布尔列 | **复用 `system_setting.client_release` 单值指针**：布尔列要靠事务保证「只有一行为真」，而指针天然唯一；指针里的 `previous` 顺带成了服务端回滚的落点 |
| 7 | 降级与回滚的关系 | 合并成一个「切到某版本」动作 | **两条独立入口**：服务端降级是「把发布指针指向更旧的版本」，要联网、影响全网；本机回滚是「从备份目录就地恢复」，不依赖网络、只影响本机。故障现场往往正是网络或后端不可用的时候，回滚必须能脱网执行 |
| 8 | 备份粒度 | 沿用既有的单槽 `.bak` | **改为 `.backups/<版本>/` 版本化目录，且 exe 与 `version.txt` 一起备份**：原实现只保留上一版且不备份 `version.txt`，恢复 `.bak` 会得到「旧程序 + 新版本号」，面板与后台显示的版本与实际运行的程序对不上 |
| 9 | 覆盖粒度 | 只换 exe | **按文件树整体覆盖**：onedir 形态的实例（exe + `_internal/` 依赖树）没法只换 exe，Python 运行时与扩展 DLL 必须配套；原 `replace_exe_file` 保留不动，供「只有一个 exe」的场景使用 |
| 10 | `.env` 怎么处理 | 一并覆盖，让用户重配 | **永不覆盖**（`PROTECTED_NAMES`）：它承载 `NODE_TOKEN` 与 MT5 账号，覆盖即掉线；打包时误把 `.env` 塞进 zip 是很容易犯的错，所以防线放在覆盖侧而不是打包侧 |
| 11 | 版本上报走哪条消息 | 新增 `version_report` 消息类型 | **扩 `auth` 首包的 `client_version`**：协议表（§ WS 上行）早就预留了这个字段，只是从未实现；旧节点不带该字段必须按「未知版本」放行，绝不能因此拒绝接入 |
| 12 | 版本落库还是只放 Redis | 只当实时态放 Redis | **落 `nodes` 表**：管理员需要在节点离线时也能看到它最后运行的版本；但版本没变就不写库，避免每次断线重连都白写一次，因此 `client_version_at` 的语义是「该版本首次上报的时间」 |
| 13 | 版本比较实现 | 面板直接 import 后端的 `client_version` | **两处各一份纯函数**：面板要独立打成单文件 exe，不共享后端代码；沿用模版3 试算与 `risk_sizing` 的既有做法，两边模块头互相标注、口径必须同步改 |
| 14 | 上传怎么落盘 | `await file.read()` 一次读完再写 | **分块读写到 `.part` 临时文件，校验通过才改名**：后端是单 worker 部署，整包进内存会占死进程；半个损坏的包若直接落成正式文件，会被当成可发布版本 |
| 15 | zip 安全 | 相信包是自己打的 | **解压与校验两侧都过滤绝对路径、盘符与 `..`**（zip slip），版本号也收窄字符集——它会拼进磁盘文件名，`../` 能写到包目录之外 |
| 16 | 删除版本的连带影响 | 只删包和记录 | **若删的正是回滚落点，一并清空 `previous`**：否则回滚指向一个已不存在的包，必然失败；当前发布版本则直接禁止删除 |
| 17 | 并发安全 | 沿用 `batch_replace` 现状 | **补上 `busy` 跳过**：`start_all` / `stop_all` 都有这道判断，只有 `batch_replace` 漏了，并发点替换会撞上正在启停的实例；覆盖写入同时加 `WinError 32` 退避重试，文件占用在 Windows 上是常态 |
| 18 | 上传体积限制 | 只依赖 Nginx | **应用层也设 `CLIENT_PACKAGE_MAX_MB`（默认 300，超出 413）**：Nginx 的 `client_max_body_size` 全局是 `1m`，这次只给 `/api/client-versions` 单开 location 放宽，避免把 webhook 等入口一起敞开。该 location **必须带 `^~`**：Nginx 里正则 location 优先于普通前缀 location，少了它就会被 `~ ^/(api\|webhook\|...)` 抢走，放宽形同虚设，上传在 0% 处直接 413 |
| 19 | 面板怎么拿版本清单 | 让面板用管理端 `GET /api/client-versions` | **新增 `GET /api/client-versions/available`（节点令牌鉴权）**：管理端那个接口带 `uploaded_by` 等后台操作痕迹，而节点令牌是全局共享的运维凭据，不该看到谁在什么时候传了包；available 只给下载所需字段。它仍是只读的，符合第 3 条划定的授权边界 |
| 20 | 导入新节点的落点 | 让用户自己拷 exe 再「添加」 | **选 MT5 目录一键部署**：客户端**必须**与 `terminal64.exe` 同目录（`mt5_discover` 的硬约束），放错位置的表现是启动即 `SystemExit`，对新手很不友好；所以目录校验前置到选择那一刻，不合规直接拦住 |
| 21 | 导入时要不要生成 `.env` | 只部署程序，配置留给用户 | **用面板的连接配置自动生成**（`http://host` 反推 `ws://host/ws/node` + 填入 `NODE_TOKEN`）：这两项正是新节点唯一必填的东西，而它们的值面板已经有了，让用户再手抄一遍纯属多余。当时约定「已存在的 `.env` 一律不覆盖」；**导入侧已由 §18.25 改为勾选自动写入时整份覆盖**，版本更新仍不覆盖 |
| 22 | `.env` 模板从哪来 | 面板内嵌一份模板 | **取安装包内的 `.env.example`**：内嵌会多一个必须跟着 node_client 同步维护的副本；取不到时退化成只写两个必填键，其余靠 `NodeSettings` 的内置默认值，仍可正常运行 |
| 23 | `.env` 怎么写回 | 按表单值整份重写 | **就地合并 `update_env_text`**：`.env.example` 的中文注释是节点配置的主要说明来源，用户也可能自己加了键，整份重写会全部丢掉。只改目标键所在那一行，新键追加到末尾 |
| 24 | 表单与原始文本冲突 | 尝试合并两边的改动 | **原始文本被改过就以它为准**：两边同时改无法可靠合并，猜错会丢用户手写的编辑；保存后在状态栏明示以哪边为准。表单里留空的可选项不写进文件，避免把内置默认值固化成显式配置 |
| 25 | 哪些键放进表单 | 把 `.env` 全部键都列出来 | **`LOCAL_STATUS_PORT` 刻意排除**：它由面板启动实例时用环境变量注入，pydantic-settings 的优先级是环境变量高于 `.env`，写进文件不生效反而误导 |
| 26 | 改完配置要不要自动重启 | 静默重启 / 完全不管 | **询问后重启**：`get_settings()` 带 `@lru_cache`，配置在进程内是冻结的，不重启改了也白改；但重启会中断正在跑的策略监控，不能替用户决定 |
| 27 | 安装包谁来打 | 让发版的人手工压缩产物目录 | **构建脚本自动打**（`build_package.py`）：手工压缩每次都要记住"排除 `.env`、带上 `version.txt`"，漏一次就是令牌泄露或版本对不上。逻辑写成 Python 而不是在 .bat 里拼 PowerShell，是为了能用测试守住排除规则 |
| 28 | `.env` 排除怎么保证 | 只靠打包时的排除列表 | **排除 + 打包后自检双保险**：`verify_package` 校验"必须含 `node_client.exe`、必须不含 `.env`"，不满足就删掉产物并让构建失败。安装包会上传到后端、任何持有节点令牌的机器都能下载，这个错误的代价是令牌泄露，值得多一道闸 |
| 29 | zip 输出目录 | 放在 `dist\packages\` | **放 `dist` 之外的 `packages\`**：输出目录若在产物目录内部，下一次构建递归收集产物时会把上一次的 zip 收进新包，包体逐次翻倍。`build_package` 里也加了断言拦住这种传参 |

### 18.15 模版2 限价开仓与挂单可见性（2026-08-12）

模版2 规则新增一行「开仓方式」开关（默认 `市价`，行为不变）。改成 `限价` 后，底仓挂在信号给的入场价，分散仓在「入场价 → 止损价」之间等分挂阶梯限价，GTC 等成交。

这项改造真正的难点不在下单，而在**让挂单被看见**：改造前全仓库没有任何挂单能力，两条收口链路（节点侧 `market_hub` 的 `GONE`、服务端 `reconcile_node_positions` 的账户快照对账）都只看持仓。若只加下单不动这两处，挂单成功会被当成成交写入 `opened_at`，下一轮对账发现「无持仓」就把子任务判完成、释放节点占位，那张限价单成了没人监控的孤儿——成交后既没有保本移动，也没有分散仓推进和收口。所以实施上先打通可见性、再接下单入口。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 开关放哪 | 新登记一个 `RULE_TYPE`（模版4） | **在 `type=3` 上加 `entry_mode` 字段**：用户诉求就是「趋势策略加一个切换开关」，两种方式共用风险金额 / 盈亏比 / 分散仓 / 保本这整套语义，拆模版会让用户重建策略、也会把同一套参数维护两遍 |
| 2 | 挂单价从哪来 | 配置里写死绝对价 / 相对现价的点数偏移 | **由信号携带**（`limit_price` / `price` 等别名）：策略绑的是分组不是价位，配置里写绝对价每换一次行情就得改一次配置 |
| 3 | 分散仓怎么挂 | 全部挂在同一个价 / 只有底仓挂限价 | **阶梯限价**：分散仓的本意就是分散成本，全挂同一价与底仓无异；分母取「单数 + 1」，末档因此不会正好落在止损价上（落在止损上等于一成交就触及止损） |
| 4 | 手数怎么算 | 沿用底仓那一档的止损距离 | **按仓位比例加权的平均止损距离**：各档开仓价不同、止损价共用，止损距离是一组递减值，用单值算会让实际亏损超出风险金额 |
| 5 | 风险金额的语义 | 维持「确定亏损」 | **改为「全部档位都成交时的最坏亏损」**：部分成交就打到止损时实际亏损更小。这是限价模式绕不开的语义变化，前端与文档都必须显式说明，否则用户会误以为仓位变重了是 bug |
| 6 | 取整偏差怎么办 | 相信向下取整不会超 | **切完按实际分布复核一轮，超预算就收缩重切**：`volume_min` 托底会让底仓占比被顶高，而底仓那一档止损距离最大，偏离方向不保证有利 |
| 7 | 挂单未成交时的存活判据 | 复用网格的 `hold_when_empty` | **`GONE` 判据改成「无持仓且无挂单」**，服务端对账口径改成「持仓 ∪ 挂单魔术号」：`hold_when_empty` 只挡住节点侧，服务端快照对账那一侧仍会收口；且它的语义是「空仓是常态」，与「在等成交」不是一回事 |
| 8 | `seen` 的含义 | 维持「曾观察到持仓」 | **扩成「曾观察到持仓或挂单」**：否则挂单被手工撤掉后永远等不到 `GONE`，占位与魔术号一直泄漏 |
| 9 | 挂单采样开销 | 每轮都读 `orders_get` | **只对 `track_orders` 的订阅读**：市价链路一笔挂单都不会有，多一次终端调用纯属浪费 |
| 10 | 读不到挂单时 | 当作没有挂单 | **整轮退化为 `STALE`**：与持仓读取失败同一口径，「读不到」绝不等于「没有」 |
| 11 | 挂单成功怎么回报 | 复用 `success` 即算成交 | **回报带 `pending`，落 `group_task_dispatch.pending_orders` 新列**：`opened_at` 照写（对账已能看见挂单），但持仓数不能凭空加一，否则前端显示有仓实际没有 |
| 12 | 收口要不要撤单 | 只平持仓 | **`close_by_magic` / `close_symbol` / `close_all` 一律先撤单再平仓**：顺序反了的话，两步之间价格触及挂单价会当场又开一笔；账户级风控清仓同理，留着挂单等于清完仓又埋一颗雷 |
| 13 | 未成交策略 | 配超时自动撤 / 用 MT5 订单过期 | **GTC 一直挂**（用户拍板）：期间该节点该品种不接新信号，占位由子任务持有 |
| 14 | 阶梯止盈的锚 | 各档以自己的挂单价为锚 | **仍以底仓价为锚**：止盈价位分布与市价模式保持一致，各档因开仓价更有利而实际盈亏比更高，这是限价的红利而不是要抹平的差异 |
| 15 | 成交后要不要重锚 | 复用市价的 `anchor_to_fill` | **限价不重锚**：挂单按挂单价成交，计划里的价位就是最终成交价，再算一遍只会引入取整噪声 |
| 16 | 配置期预览手数 | 做一个类似模版3 的试算助手 | **不做**：曾实现过一版（假设入场价 + 止损价 → 仓位分布），但手数本就由信号决定，配置期只能给假设值，与模版3「算完回填进配置」的助手不是一回事；维护一份与节点侧必须同步的重复算法，收益不抵成本。已连同 API 一并移除 |
| 17 | `order_type` 字段 | 用它决定开仓方式 | **只留痕不接线**：该字段自项目初期就解析但无消费方，开仓方式由策略配置决定更符合「开关在配置里」的诉求；接线会让同一份配置的行为随信号漂移 |

### 18.14 模版3 网格试算助手（2026-08-12）

模版3 规则新增一行「试算网格」开关（默认关闭）：把 ATR × 倍数折成格距推出网格数量，再按「满仓打止损」的最坏亏损反推每格手数。定位是**配置期助手**，不是新的运行期能力。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 自由度怎么定 | 只给止损价 + 最大亏损直接解 | **先由格距定格数，再由风险定手数**：两个未知数（格数、每格手数）只有一条风险约束，方程欠定，必须先固定一个自由度 |
| 2 | ATR 生效时机 | 规则存 `atr_mult`，启动时节点现算格距 | **配置期冻结**：算完落回 `grid_count` / `lot_per_grid`，运行期不读 assist 参数；否则网格线会随启动时刻的波动漂移，与「配置即区间/格数」的既有语义冲突，恢复逻辑也要跟着改 |
| 3 | 取数通道 | 新建一条 RPC / 后端接第三方行情 | **复用 `market_probe`**：ATR 只需 15 根已收盘 K 线，远低于探针根数上限，且要的就是该终端口径（含券商后缀与合约规格） |
| 4 | 合约规格从哪来 | 前端按用户手填点值估算 | **扩 `market_probe` 回包带 `spec`**（节点侧 `symbol_spec()` 已现成）：手数换算要 `tick_value / tick_size`，与节点侧 `risk_sizing` 同口径才不会预览能跑、下单被拒 |
| 5 | `spec` 读取失败 | 视作探针失败 | **只省略字段并降级为「只给格数」**：同一条 `fetch` 还担着分组趋势风控，那边探针失败是 fail-closed 拦开仓的，绝不能被规格读不到连带触发 |
| 6 | 算在哪 | 前端本地算 | **后端纯函数**（`grid_sizing.py` + `grid_sizing_api.py` 编排）：手数属交易决策，不进 Vue；ATR 与网格线切法分别对齐 `node_client/bar_metrics` 与 `grid_trading.build_levels` |
| 7 | 止损价来源 | 助手内单开一个止损输入 | **复用 `stop_lower` / `stop_upper`**：避免助手与规则两套止损打架，且规范化已保证它落在区间外 |
| 8 | 行情源节点 | 落进策略配置 | **只留在表单会话态**：策略绑的是分组，不该被某台节点绑死；离线时助手不可用，可手填格距降级 |
| 9 | assist 参数落库 | 纯前端不落库 / 只记事件 | **落规则但执行层忽略**：重开表单能还原当时输入，策略详情多一行「试算来源」留痕；`GridConfig.from_rule` 是白名单挑字段，天然读不到 |
| 10 | 建议值写回 | 试算即自动覆盖 | **手动「应用建议」**：不静默改用户手填的格数与手数；改动区间 / 止损 / 方向后结果标记过期，拦住应用 |
| 11 | 最坏亏损口径 | 一律按格线价求和 | **预填格按现价市价成本、其余按格线价**（与 `prefill_indices` 同判据）；`warnings` 明说软止损、`close_on_stop=false` 不封顶、开追踪后估算失效 |
| 12 | 校验时机 | 始终必填 | **仅开关开启时校验**：存量规则与不用助手的用户不受影响；但「开了助手却没设本方向止损」必须拦死，否则算出的手数是假的 |

### 18.13 本机运维面板与 loopback 状态口（2026-08-11）

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 健康检查深度 | 仅 OS 进程 / 改 node_client 加本地口 / 查中控 API | **进程级 + `node_client` loopback HTTP**（`/health` `/status` `POST /stop`）：可看 WS 态与 runners，不依赖后端可达 |
| 2 | 默认端口 | 固定端口常开 | **`LOCAL_STATUS_PORT=0` 默认关闭**；面板启动时注入空闲端口，兼容现网手动启动 |
| 3 | 绑定地址 | `0.0.0.0` | **仅 `127.0.0.1`**，无鉴权；文档禁止改绑公网 |
| 4 | 守护形态 | nssm / 面板内 watchdog | **面板内守护**（实例级开关）；关闭面板停止轮询但不强杀子进程 |
| 5 | UI | tkinter / Qt | **CustomTkinter**，可 PyInstaller 打成 GUI exe（`nodel_client_dashboard/`） |
| 6 | 与交易链路关系 | 面板内做下单决策 | **纯运维**：不碰 Dispatcher / group 规则；交易规则仍只在节点与后端 |

### 18.12 v0.6.7 分组趋势风控（2026-08-10）

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | 开关粒度 | 全局 / 策略级 | **分组级 `trend_risk_enabled`（默认关）**：与 strategy 分发单元一致，可按组试用 |
| 2 | 判定挂点 | `_candidate_groups` 组级一次 | **各节点 `_dispatch_node` 抢占位后、建子任务前**：用该节点终端行情，poll 被拦可顺延 |
| 3 | 放行口径 | 只拦逆向 / 顺势才放 | **顺势才放行**：BUY↔多头、SELL↔空头；中性与数据不足一律拦 |
| 4 | 算不出 | 放行 | **fail-closed 拦截**（探针失败 / 无 K 线 / `unknown`） |
| 5 | CLOSE | 同样过门禁 | **绕过**（对齐现有开仓过滤语义，终止指令必须可达） |
| 6 | 与趋势面板关系 | 另写一套指标 | **复用全局 `trend_config` + `market_probe` + `evaluate`**；面板仍只读，仅分组显式开启后才参与 strategy 开仓门禁 |
| 7 | 落库 | 新事件表 | 子任务 `skipped` + `skip_reason`（中文），复用分组信号页 |
| 8 | 零下发主任务原因 | 全部 `skipped` 一律写「均有进行中的任务」 | **按子任务 `reason` 汇总**（`group_rules.summarize_no_target`）：全忙仍用原句；全趋势抄/归并为趋势文案；混因分号拼接。分组页手动触发 toast 同步读 `tasks[].reason` |

### 18.11 v0.6.6 变更记录（2026-08-10）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 模版3 网格方向 | `long` / `short` / `follow`（跟随信号） | **仅 `long` / `short`**；配置侧不再提供「跟随信号」；规范化时历史 `follow` 回落为 `long`；节点侧仍可解析已下发快照中的 `follow` |

### 18.10 v0.6.5 趋势面板（2026-08-08）

节点详情新增「趋势面板」Tab：按币种/周期实时查看该节点终端上的 EMA + RSI 综合趋势得分。默认定位是**只读观测**；分组开启「趋势风控」后，同一套得分才参与 strategy 开仓门禁（见 §18.12）。

| # | 决策 | 备选 | 采用方案与理由 |
| --- | --- | --- | --- |
| 1 | K 线从哪来 | 后端接第三方行情源 | **向节点要**：面板要看的就是该节点终端上的口径（含券商后缀与自有历史），另接数据源会与实际成交环境脱节 |
| 2 | 取数机制 | 复用现有单向上报 | 新增最小 **req_id RPC**（`market_probe` / `market_probe_result`，`market_probe.py` 维护 `req_id → Future`），因为面板是按需拉取，塞进定时上报会白拉一堆无人看的品种 |
| 3 | 指标算在哪 | 节点端算完回传得分 | **后端纯函数算**（`trend_indicators.py`）：调参数时若 K 线仍在缓存内就无需再打扰终端，且规则与 I/O 分离便于单测 |
| 4 | 重复请求 | 每次请求直连节点 | **2s 结果缓存 + 同 key 请求合并**（`_inflight`）：多管理员同看一个品种、或滑动调参时只落一次终端查询 |
| 5 | 参数存哪 | 按节点 / 按币种 / 仅浏览器 | **落 `system_setting.trend_config` 全局一份**：全后台共用、不分节点与币种；查询参数可临时覆盖但不落库；旧 `nodes.trend_json` 仅作一次性迁移种子 |
| 6 | 参数校验 | 越界报错 | **规范化夹回合法区间**（`normalize_config`）：趋势参数只影响展示口径，夹回比让面板报错更顺手；EMA 权重定后 RSI 权重取 `1 - w`，保证满分恒为 ±100 |
| 7 | 得分口径 | EMA 直接产出 ±70 | **各指标先算 ±100 原始分再乘权重**：改权重时两指标贡献自动等比缩放，无需重标定每个指标的量程 |
| 8 | 数据不足 | 当成中性 0 分 | **单独的「数据不足」态**：K 线不够算 EMA/RSI 时中性与"算不出"必须可区分，否则会把空白误读成震荡 |
| 9 | 图表库 | 手写 SVG | **ECharts**（K 线主图 + EMA 叠加 + RSI 副图），并用 `defineAsyncComponent` 懒加载，避免图表库进节点详情首屏 chunk |

### 18.9 v0.6.4 变更记录（2026-08-07）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 模版1 加仓偏离基准 | 逆势 / 顺势均取**最近一笔**开仓价 | **顺势**仍取最近一笔；**逆势**取不利方向最深开仓价（无更深逆势仓时即首仓），避免顺势加仓后在其回撤点误加逆势 |

### 18.8 v0.6.3 变更记录（2026-08-07）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 网格预填判据 | 多头按「买线已跌破」等旧口径，空头对称写反 | **多头**：卖出价仍高于现价才预填；**空头**：平仓价仍低于现价才预填 |
| 2 | 触发价 | 易被理解成单侧比较 | **方向无关触及**：首报价仅等值算到价，之后靠穿越 |
| 3 | 模版选型 | tpl_3 无启用 type=4 可能回退加仓 | **fail-closed**；模版名「网络策略」→「网格策略」；`TEMPLATE_RULE_TYPES` 校验 |
| 4 | trailing 同 tick | 先平移可能吞掉 crossings | **先 crossings 再 trailing**；卖失败进重试队列 |
| 5 | 终态与残仓 | 未确认平干净也可能 `done` | `_close_all` 复查；残仓 / 崩溃 / `close_on_stop=false` → `stop_failed`/`faulted`/`detached`（STUCK，不释放占位） |
| 6 | 运行态恢复 | 平移量不落库，重连靠现价猜 | `runtime_json` + resume 透传；closing/`stop_requested_at` 重连改发 stop |
| 7 | CLOSE 候选 | 复用开仓 `_candidate_groups` | **`active_subtasks_overview`**：按活动子任务与快照筛，跳过 enabled/准入 |
| 8 | 互斥口径文档 | 误写 `(node_id, symbol)` | 纠正为 **`(group_id, node_id)`**（与实现一致） |
| 9 | 模版2 恢复已开档 | `total_orders = len(positions)` | 按 `R3B*` 最大批次 + 1；中间档止盈后不重开 |
| 10 | 模版2 开仓 burst | 分散仓循环不看 stop | 每笔前检查 `_stopping`，收到终止即停手再平仓 |

### 18.7 v0.6.2 变更记录（2026-08-06）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 网格止损/止盈文案与校验 | 一律按多头：`stop_lower`=止损须低于下限、`stop_upper`=止盈须高于上限 | **字段仍按价格上下沿存储**；前端文案/校验随 `grid_side` 切换（空头：上沿=止损、下沿=止盈）；运行时语义不变（§9.8） |

### 18.6 v0.6.1 变更记录（2026-08-05）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 模版2 分散仓止盈 | 全部挂同一盈亏比价 | **等分阶梯**：第 i 档 = 开仓价 + 止损距 × rr × i / N，末档吃满盈亏比（§9.8） |
| 2 | 模版2 止损距离 | 用开仓侧报价（BUY=ask） | 用**止损触发侧**（BUY=bid / SELL=ask）；阶梯仍锚开仓侧 |
| 3 | 模版2 手数切分 | 末笔吃余数 | 分散仓严格等手数，除不尽余量不开 |
| 4 | 任务收口原因 | 被动全平一律 `positions_cleared` | 查 MT5 出场成交 `DEAL_REASON`，写成「止损打掉 / 止盈兑完 / 混合…」中文原因 |

### 18.5 v0.6 变更记录（2026-08-04）

> 覆盖自提交 `fd138d6`（分组管理 + 策略信号分发）起至网格追踪与术语整理的实现。策略模版2 细节亦见 §18.4；本节补齐分组链路、模版3、风控与运维能力。

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | strategy 分发 | 无独立链路 | **节点分组** + `group_dispatcher`；与 normal 按币种链路规则/账本隔离（§9.8） |
| 2 | 策略绑定 | — | 分组与策略实例**一对一**（`node_group.strategy_id` UNIQUE）；未绑定不参与分发 |
| 3 | 任务与魔术号 | — | 主任务 `group_signal_task` + 节点子任务 `group_task_dispatch`；`magic = NODE_TASK_MAGIC_BASE + dispatch_id` |
| 4 | 节点并发 | — | `(group_id, node_id)` Redis 占位（`NODE_BUSY_TTL`）；同分组内节点互斥、跨分组可并行 |
| 5 | 模版1 分批方向 | 仅规则级 `action` | 新增 **`batch_action`**（`all`/`buy`/`sell`），分批监控方向可独立配置 |
| 6 | 策略模版3 | — | **网格交易**（`tpl_3`，规则 `type=4`）：等差/等比、方向、触发价、预填仓、止损止盈 |
| 7 | 网格追踪 | 固定区间 | 可选 **`trailing_up` / `trailing_max`**：突破外沿平移整网 |
| 8 | 网格收口 | 无持仓即结束 | **`hold_when_empty`**：空仓为常态，仅止损/止盈/CLOSE 结束 |
| 9 | 账户风控 | — | `nodes.risk_json` + 节点侧常驻规则（浮盈亏比、净值下限、品种盈亏/保护、手数分档）（§9.9） |
| 10 | 手动信号 | 仅 Webhook | 中控台 **`POST /api/console/manual-signal`** 对分组发 BUY/SELL/CLOSE |
| 11 | 交易日志清理 | — | **`POST /api/console/purge-trade-logs`**（确认短语）；清理相关表与 Redis，并尝试停策略 |
| 12 | 任务事件透明度 | 仅摘要字段 | `group_task_event.detail_json` 落计算依据；分组信号页可展开查看 |
| 13 | 管理端页面 | 无策略/分组 | 新增 `/strategies`、`/groups`、`/groups/:id/signals`；节点页可配风控 |

关键配置项（后端环境变量）：

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `NODE_TASK_MAGIC_BASE` | `900000000` | 节点子任务魔术号基数（兼容旧名 `GROUP_TASK_MAGIC_BASE`） |
| `NODE_BUSY_TTL` | `86400` | `(group_id, node_id)` 互斥占位 TTL 秒（兼容旧名 `GROUP_BUSY_TTL`） |
| `STRATEGY_REPORT_INTERVAL` | `5` | 策略运行上报间隔（秒） |

### 18.4 v0.5 变更记录（2026-08-03）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 策略模版 | 仅内置策略模版1（逆势 / 顺势加仓） | 新增 **策略模版2「以损定量趋势单」**（`tpl_2`，规则 `type=3`），见 9.8 |
| 2 | 规则规范化 | `normalize_rule` 硬编码 type 1/2，统一输出同一组字段 | 按 type 分派到独立 normalizer（`_RULE_NORMALIZERS`），各 type 只输出自己的字段 |
| 3 | 模版2 首单手数 | —— | 不使用信号 `volume`，由「风险金额 ÷ 止损距离」在**节点侧**反推 |
| 4 | 模版2 信号准入 | —— | 缺止损价 `sl` 时在分发前拒收（`group_rules.entry_reject_reason`），不影响模版1 |
| 5 | 模版2 节点 MT5 能力 | 只有 `symbol_point` | 新增 `symbol_spec`（合约与手数规格）、`modify_position_sl` / `modify_sl_by_magic`（保本改单） |

### 18.3 v0.4 变更记录（2026-07-06 21:10）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | Webhook 原始体 | 仅落库解析后字段 | **`signal_history.raw_payload`** 保存原始 JSON/文本（解析失败时亦记录） |
| 2 | 信号事件查询 | 仅在总览 WS 事件流 / 节点详情 Tab 查看 | 新增 **`GET /api/events/signals`** + 管理端 **「事件」页**（`/events`），分页展示原始参数与各节点处理明细 |
| 3 | 节点未配品种 | 回退节点级默认（`fixed` + `0.01`、参与 sync/poll） | **强制拒收**：`filters` 中无该品种条目 → `skipped`，`skip_reason` 为中文可读说明 |
| 4 | 自动注册 filters | `filters=None` | 从中控台已登记品种**批量生成**默认按币种条目（`DEFAULT_NODE_SYMBOL_RULE`） |
| 5 | 分发候选筛选 | 在线 + enabled + 参与模式 | 增加 **`node_has_symbol_config`** 前置校验（`rules.py`） |

### 18.2 v0.3 变更记录（2026-07-06）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 分发模式 / 持仓范围 | 全局 `config:dispatch` + `GET/PUT /api/config/dispatch` | **按品种**写入 `config:filters` 各条目（`dispatch_mode`、`position_scope`）；移除 dispatch API |
| 2 | Webhook 准入 | 任意可解析品种均可分发 | **须先在中控台登记且启用**；未登记 / `enabled=false` 返回 `status: rejected`（HTTP 200；含 CLOSE） |
| 3 | 节点交易配置 | 节点级 `lot_mode` / `follow_sync` / `follow_poll` / `poll_order` | 下沉到 `node.filters` **按币种**配置；节点详情 / 编辑表单维护 |
| 4 | 方向过滤 | 仅区间 `allow` 列表 | 增加 **`allow_buy` / `allow_sell` 方向总开关**（品种级） |
| 5 | 管理端 UI | 「配置」页含手数/过滤/分发 | 拆分为 **「中控台」**（`/console`：过滤 + 全局手数）与 **「配置」**（`/config`：令牌/密码/2FA） |
| 6 | 跳过原因 | 英文 reason 码 | 中文可读 `skip_reason`（持仓/区间/方向总开关），直接展示在后台 |

### 18.1 v0.2 变更记录（2026-06-29）

| # | 决策 | 原方案 | 新方案 |
| --- | --- | --- | --- |
| 1 | 节点鉴权令牌 | 每节点一份、明文仅创建时显示一次、DB 存 sha256 | **全局共享一份**、明文持久化于 `system_setting`、可在「账户设置」随时查看/重置 |
| 2 | 节点身份识别 | 后台用 `token_hash` 反查 node_id | **用 `mt5_login` 反查 node_id**（Redis `node:by_login:{login}` + DB 兜底） |
| 3 | 节点入库方式 | 仅管理员手动 `POST /api/nodes` | **自动注册（首次接入时按默认配置入库）+ 仍保留手动创建入口** |
| 4 | `mt5_login` 约束 | 可空、可重复 | **NOT NULL + 全局 UNIQUE**；创建后不可修改 |
| 5 | 自动注册默认值 | — | `name=node-{login}`, `enabled=False`；**`filters` 从中控台品种自动生成**（每品种 `follow_sync/poll=True`, `lot_mode=fixed`, `lot=0.01`, `poll_order=0`） |
| 6 | 接入握手首包 | `{type:auth, data:{token}}` | **`{type:auth, data:{token, mt5_login}}`** |
| 7 | 节点管理 API | `POST /api/nodes/{id}/rotate-token` 重置单节点令牌 | **移除**；改为 `GET/POST /api/config/node-token[/rotate]`（全局） |
| 8 | 重复登录策略 | 踢旧连保留新连 | **保留旧连，拒绝新连**（关闭码 `4409 already_online`）—— 避免互相踢线导致雪崩 |

> 迁移指引：开发环境直接清空 `nodes` 表（已通过 `_drop_legacy_nodes_table` 自动处理）；生产环境需要先记录现有节点的 `name/mt5_login/lot_*` 配置，再写一次性 Alembic 迁移：移除 `token_hash` 列 → 为 `mt5_login` 加 NOT NULL + UNIQUE → 创建 `system_setting` 表并插入新 token。所有节点 `.env` 中的旧 token 都需替换为新全局 token。

---

## 19. 测试方案

> 目标：核心交易/分发逻辑可重复验证；与参考库一致的解析逻辑用"黄金用例"锁定；实盘前用 demo 账户回归。

### 19.1 单元测试（pytest + pytest-asyncio）
- **解析器（对齐参考库）**：三类格式（结构化 JSON / 简化 JSON / 纯文本）、字段别名、动作关键字（含中文 `做多/做空/平仓`）、`CLOSE` 优先级、symbol 映射、`volume` 缺省 `0.1` 与上限 `MAX_LOT_SIZE` 截断、`sl/tp` 可选且 `>0`、`allow_position`、非法信号校验。以一组**黄金用例**固化"输入→输出"，与参考库保持一致。
- **纯函数**：`resolve_volume`（9.4）、`position_gate`（9.3）、`resolve_dispatch_config` / `interval_filter`（9.2，含 `allow_buy`/`allow_sell`）、retcode 分类（10.7）。
- **轮询轮转**：mock 多节点，验证单节点领取 + 消费者移到队尾循环轮转、队首离线/被过滤时顺延、无人可领取时标记 unconsumed。

### 19.2 集成测试
- **后台 + Redis**：用 `fakeredis` 或 testcontainers，跑通"全局 token 拉取 → WS 首包鉴权（含 mt5_login 自动注册分支）→ 心跳 → 上报 → 分发 → 回执"全链路。
- **MT5 客户端**：CI 中 `monkeypatch` 掉 `MetaTrader5` 模块做无终端测试；上线前在装有 MT5 的机器用 **demo 账户**真机验证 `buy/sell/close` 与 `resolve_symbol` 后缀解析。

### 19.3 端到端与故障演练
- **信号回放**：向 `/webhook` 注入历史/构造信号，校验各节点指令、`signal_dispatch` 落库与状态。
- **故障注入**：节点掉线重连不重复下单（9.7 幂等）、Redis 重启、单节点下单失败的轮询续推、全局 token 重置后所有旧连接被拒绝、`mt5_login` 重复创建返回 409、全局平仓二次确认。
- **幂等**：重复投递同一信号（webhook 重发）只产生一次下单。

### 19.4 安全测试
- webhook `Bearer`/IP 白名单、WS 非法全局 token 与缺失 mt5_login 拒绝（`4401`）、禁用节点拒绝（`4403`）、重复登录拒绝（`4409`）、首包超时（`4400`）、管理端 JWT + 节点令牌读取/重置鉴权与高危操作二次确认。

### 19.5 质量门禁
- 后端 `pytest` + coverage（核心模块 ≥ 80%）；前端 `vue-tsc --noEmit` 类型校验 + `vitest`；提交/合并前 CI 全量通过。

---

*（完）本方案在信号解析（第 8 章）与 MT5 下单/平仓（第 10 章）上严格对齐参考库 `mt5_python_connector` 的实现，其余为多节点分发、监控与管理的扩展设计。*