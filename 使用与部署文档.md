# MT5 多节点跟单系统 · 使用与部署文档

> 配套文档：架构与设计细节见 [`技术实现方案.md`](./技术实现方案.md)；Webhook 入参细则见 [`WEBHOOK.md`](./WEBHOOK.md)。
> 本文面向「部署 + 日常使用」，按本文操作即可把系统跑起来并完成一次完整跟单。
>
> **最后更新：2026-08-05**（分组列表进行中主任务数、策略信号 `status=active` 过滤）

---

## 1. 系统简介

一套「中心管理端 + 多 MT5 节点」的信号跟单系统：

- **管理端（后端 + Web）**：接收 TradingView Webhook 信号，按规则分发给各 MT5 节点；提供节点管理、手数/过滤/分发配置、**策略与分组**、**账户风控**、账户监控、远程平仓。
- **两条信号链路**：
  - **`model=normal`（默认）**：按中控台品种规则 → 节点按币种配置分发（同步 / 轮询）。
  - **`model=strategy`**：按**节点分组 + 绑定策略**分发；策略基于内置模版（逆势/顺势、以损定量、网格）。
- **节点端（MT5 Node）**：运行在装有 MT5 终端的 Windows 主机上，通过 WebSocket 长连接接收命令并执行下单/平仓/策略任务，同时上报账户与持仓。

```
TradingView ──HTTP Webhook──▶ 后端(FastAPI)
                                  │  ├─ Redis（实时态/队列/锁）
                                  │  └─ MySQL（节点账本/审计/历史/分组任务）
                                  │
                      ┌───────────┼───────────┐  WebSocket(wss)
                      ▼           ▼           ▼
                  MT5 节点A    MT5 节点B    MT5 节点C
                      ▲
   管理员 ──Web(Vue)──┘ （登录后通过 /ws/admin 实时看板 + 远程平仓）
```

技术栈：FastAPI + Redis + MySQL（后端），Vue3 + TypeScript（前端），`websockets` + MetaTrader5（节点端）。

---

## 2. 目录结构

```
robin-project-2026/
├─ backend/                # 管理端后端（FastAPI）
│  ├─ app/                 # 业务代码（已全量中文注释）
│  ├─ tests/               # 单元/集成/接口测试
│  ├─ alembic/             # 数据库迁移
│  ├─ requirements.txt     # 生产依赖
│  ├─ requirements-dev.txt # 测试依赖
│  ├─ Dockerfile
│  └─ .env.example
├─ node_client/            # MT5 节点端（Windows）
│  ├─ node_client.py       # 主程序
│  ├─ local_status.py      # 本机 loopback 健康/状态口（面板用）
│  ├─ mt5_client.py        # 真实 MT5 操作（对齐参考仓库）
│  ├─ mock_mt5.py          # 无 MT5 时的模拟器
│  ├─ version.py           # 版本号规则与运行时取版本
│  ├─ build_version.py     # 构建期版本戳（打包脚本调用）
│  ├─ build_package.py     # 构建期打安装包 zip（排除 .env，打包脚本调用）
│  ├─ build_exe.bat        # 打包 exe + 安装包（onefile；onedir 用 build_exe_onedir.bat）
│  ├─ run_node.bat         # Windows 一键启动
│  └─ .env.example
├─ nodel_client_dashboard/ # 本机运维面板（启停/守护/日志/导入/版本更新，可打包 exe）
│  ├─ version.py           # 面板自身版本号规则与运行时取版本
│  ├─ build_version.py     # 构建期版本戳（打包脚本调用）
│  ├─ build_package.py     # 构建期打分发压缩包（排除 panel_config.json / instances.json）
│  ├─ build_exe.bat        # 打包 exe + 分发压缩包
│  ├─ version_service.py   # 与后端的版本通道（发现后端、查版本、下载安装包、校验）
│  ├─ env_file.py          # node_client .env 读写与字段校验（纯函数）
│  ├─ connection_dialog.py # 后端 API 地址与节点令牌配置（含测试连接）
│  ├─ import_dialog.py     # 导入节点（选 MT5 目录 + 版本，部署并注册实例）
│  ├─ env_dialog.py        # 节点配置编辑（.env 表单 + 原始文本）
│  ├─ version_dialog.py    # 版本更新对话框（检测/批量更新/降级/回滚）
│  ├─ version_picker.py    # 目标版本选择（下拉后端版本清单，供「更新到指定版本」）
│  ├─ dialog_window.py     # 对话框构建期遮挡与居中显示（勿改回 withdraw，见方案 §18.18）
│  └─ client_deploy.py     # 版本读取、版本化备份与覆盖（纯函数）
├─ frontend/               # 管理端 Web（Vue3 + TS）
│  ├─ src/
│  └─ package.json
├─ deploy/nginx/           # Nginx 反代配置（含 HTTPS 模板）
├─ docker-compose.yml      # 一键部署（mysql + redis + backend + nginx）
├─ docker-compose.windows.yml # Windows 部署版（Docker Desktop，端口默认 8080/8443）
├─ .env.example            # docker-compose 用环境变量
├─ 使用与部署文档.md        # 部署与日常使用
├─ 技术实现方案.md          # 架构与设计细节
├─ WEBHOOK.md              # Webhook 入参细则
└─ 生产部署WSS操作文档.md   # HTTPS/WSS 升级指南
```

---

## 3. 环境要求

| 组件 | 版本 | 说明 |
| --- | --- | --- |
| Python | 3.10+（推荐 3.12） | 后端 + 节点端 |
| Node.js | 18+ | 前端构建 |
| Redis | 6+ | 实时态/队列/锁 |
| MySQL | 8.0 | 生产持久化（本地可用 SQLite 免装） |
| Docker / Compose | 最新稳定版 | 一键部署可选 |
| MetaTrader5 终端 | Windows | 仅节点端需要；`MetaTrader5` 包仅 Windows 可用 |

> 节点端必须运行在 **Windows**（MT5 官方 Python 包限制）。后端/前端可在任意平台开发。

---

## 4. 快速开始（本地开发，免 Docker / 免 MySQL / 免真实 MT5）

适合先把全链路跑通、做联调。后端用 SQLite + 本地 Redis，节点端用模拟 MT5。

### 4.1 启动 Redis

```bash
# 任选其一
docker run -d --name redis -p 6379:6379 redis:7-alpine
# 或本机已安装的 redis-server
```

### 4.2 启动后端

```bash
cd backend
python -m venv .venv && .venv\Scripts\activate     # Windows
# source .venv/bin/activate                          # macOS/Linux
pip install -r requirements.txt
copy .env.example .env                               # Windows；Linux 用 cp
# .env 默认即为：SQLite + 本地 Redis + 关闭鉴权，适合本地
uvicorn app.main:app --reload --port 8000
```

启动成功后：

- API 文档：<http://localhost:8000/docs>
- 健康检查：<http://localhost:8000/health>

### 4.3 启动前端

```bash
cd frontend
npm install
npm run dev        # 默认 http://localhost:5173，已配置代理到 :8000
```

浏览器打开 <http://localhost:5173>，用 `.env` 中的 `ADMIN_USER/ADMIN_PASSWORD`（默认 `admin / admin123`）登录。

### 4.4 拷贝全局令牌并启动「模拟」节点端

1. Web → 「配置」→「节点令牌 (NODE_TOKEN)」→ 点击「显示」并复制（首次启动后端时已自动生成）。
2. 配置并启动节点端（模拟模式）：

```bash
cd node_client
python -m venv .venv && .venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
# 编辑 .env：把 NODE_TOKEN 设为上一步复制的全局令牌；设 MT5_LOGIN=<任意正整数> 和 MT5_MOCK=true
python node_client.py
```

3. 节点首次连接时，后台按 `MT5_LOGIN` 在库中查找；若不存在则**按默认配置自动入库**（名称 `node-<login>`、**禁用**；**按币种配置**从中控台已登记品种自动生成，每个品种默认参与 sync/poll、固定手数 0.01、轮询序 0）。本轮鉴权会因禁用被拒（`4403`），需在「节点管理」启用后才会上线。
4. 在 Web 「节点管理」将该节点设为**启用**后，重启或等待 node_client 重连；回到「总览」应能看到该节点 **上线**、并周期上报模拟账户。

> 也可以反过来：先在「节点管理 → + 新建节点」里手动创建（填入 mt5_login；手动新建默认启用），再启动 node_client 接入。

### 4.5 在中控台登记信号品种

Webhook 只会处理**已在中控台登记**的品种；未登记的信号会返回 `status: rejected`（HTTP 200，但不分发）。

1. Web → 「中控台」→「多区间方向过滤」→ 点击「+ 添加品种」，填入 `XAUUSD`（或你测试用的品种）。
2. 保持默认即可（分发模式 `sync`、持仓范围 `symbol`、允许做多/做空），点击「保存过滤规则」。

### 4.6 触发一次信号

```bash
curl -X POST http://localhost:8000/webhook ^
  -H "Content-Type: application/json" ^
  -d "{\"action\":\"buy\",\"symbol\":\"XAUUSD\",\"volume\":0.1}"
```

响应应含 `"status":"accepted"`。总览页事件流会出现「分发 / 回报」记录，节点持仓 +1。至此本地全链路打通。

---

## 5. 生产部署（Docker Compose 一键）

服务端一条命令拉起 `mysql + redis + backend + nginx`，前端以静态资源由 Nginx 托管。

### 5.1 准备环境变量

```bash
cp .env.example .env
```

按 [§7 配置说明](#7-配置项说明) 修改 `.env`，**务必修改以下敏感项**：

```
MYSQL_ROOT_PASSWORD / MYSQL_PASSWORD   # 数据库密码
JWT_SECRET                              # 足够长的随机串
ADMIN_PASSWORD                          # 管理后台密码
ENABLE_AUTH=true / AUTH_TOKEN           # Webhook 鉴权
```

### 5.2 先构建前端静态资源

Nginx 通过挂载 `frontend/dist` 来托管前端，因此需先构建：

```bash
cd frontend
npm install
npm run build      # 产物输出到 frontend/dist
cd ..
```

### 5.3 启动全部服务

```bash
docker compose up -d --build
docker compose ps          # 确认 mysql/redis healthy，backend/nginx up
docker compose logs -f backend
```

访问入口（默认 80 端口，由 Nginx 统一转发）：

| 路径 | 说明 |
| --- | --- |
| `http://<服务器IP>/` | 管理后台（Web） |
| `http://<服务器IP>/webhook` | TradingView Webhook |
| `http://<服务器IP>/docs` | 后端 API 文档 |
| `http://<服务器IP>/health` | 健康检查 |
| `ws(s)://<服务器IP>/ws/node` | 节点接入 |
| `ws(s)://<服务器IP>/ws/admin` | 后台实时推送 |

### 5.4 数据库迁移（生产）

容器内已包含 Alembic。首次或升级时执行：

```bash
docker compose exec backend alembic upgrade head
```

> 说明：后端启动时也会自动 `create_all` 建表（便于快速启动）；生产环境**推荐以 Alembic 迁移为准**进行版本化管理。

### 5.5 启用 HTTPS（强烈建议）

1. 把证书放到 `deploy/nginx/certs/`（`fullchain.pem`、`privkey.pem`）。
2. 打开 `deploy/nginx/default.conf` 末尾被注释的 `443` server 段，填好 `server_name`。
3. `docker compose restart nginx`。
4. 节点端与 TradingView 改用 `wss://` / `https://`。

### 5.6 Windows 服务器部署（Docker Desktop）

仓库额外提供 `docker-compose.windows.yml`，与上面的 Linux 版**拓扑完全一致**（mysql + redis + backend + nginx 全在容器内，节点端在各自 Windows 主机原生运行）。容器内部都是 Linux 镜像，宿主是 Windows 不影响其行为；唯一面向 Windows 的差异是把 nginx 对外端口参数化——默认 `8080/8443`，绕开 Windows 上常被 IIS / HTTP.sys 占用的 `80/443`。

前置条件：

1. 安装 Docker Desktop（建议 WSL2 后端）。
2. 仍需先构建前端（§5.2）：`cd frontend; npm install; npm run build; cd ..`。
3. `copy .env.example .env`，按 [§7](#7-配置项说明) 修改敏感项；端口可用 `HTTP_PORT` / `HTTPS_PORT` 覆盖。

启动 / 运维（PowerShell 或 CMD）：

```powershell
docker compose -f docker-compose.windows.yml up -d --build
docker compose -f docker-compose.windows.yml ps
docker compose -f docker-compose.windows.yml logs -f backend
docker compose -f docker-compose.windows.yml exec backend alembic upgrade head
```

默认访问入口（端口由 `.env` 的 `HTTP_PORT/HTTPS_PORT` 决定）：

| 路径 | 说明 |
| --- | --- |
| `http://localhost:8080/` | 管理后台（Web） |
| `http://localhost:8080/webhook` | TradingView Webhook |
| `ws://<本机IP>:8080/ws/node` | 节点接入 |

Windows 常见问题：

| 现象 | 处理 |
| --- | --- |
| 端口被占用 | `netstat -ano \| findstr :8080` 查占用；或改 `.env` 的 `HTTP_PORT`。要用 80/443 需先确保未被 IIS/HTTP.sys 占用 |
| 绑定挂载失败 | WSL2 后端无需手动共享盘符；Hyper-V 后端需在 Docker Desktop → Settings → Resources → File Sharing 勾选项目所在盘符 |
| nginx 配置解析报错 | 把 `deploy/nginx/default.conf` 转为 LF 换行后 `docker compose -f docker-compose.windows.yml restart nginx` |
| 前端 404 | 是否已执行 `npm run build` 生成 `frontend/dist` |

---

## 6. 节点端部署（Windows + 真实 MT5）

> 在每台需要跟单的 Windows 主机上执行。一台主机一个 MT5 账户对应一个节点。MT5 账户登录号（`MT5_LOGIN`）是节点的业务唯一键。

1. 安装并登录好 MT5 终端，确保「算法交易/AutoTrading」已开启。
2. 在管理后台 → 「配置」→「节点令牌」复制全局 `NODE_TOKEN`（所有节点共享此令牌）。
3. 将节点客户端（打包 exe，或 `node_client/` 目录）放到 **MT5 安装目录**（与 `terminal64.exe` 同级），配置 `.env`：

```
MANAGER_WS_URL=wss://hub.example.com/ws/node   # 生产用 wss
NODE_TOKEN=<从管理后台复制的全局令牌>
MT5_MOCK=false
WATCH_SYMBOLS=XAUUSD,EURUSD,GBPUSD,USDJPY,US30,US100
```

> 账号：终端已登录则自动复用；未登录时启动交互输入。程序会自动发现同目录的 `terminal64.exe`（或 `terminal.exe`）；找不到会报错退出。

4. 双击 `run_node.bat`（会自动建虚拟环境、装依赖、启动），或手动：

```powershell
cd <MT5安装目录>
python -m venv .venv; .venv\Scripts\activate
pip install -r requirements.txt
pip install MetaTrader5      # 仅 Windows
python node_client.py
```

5. 后台「节点管理 / 总览」确认该节点出现；自动注册默认禁用，需启用后才会上线。若需要修改名称/手数策略/轮询顺序等，在「节点管理 → 编辑」里调整。

> 断线会自动重连（指数退避）；MT5 阻塞调用统一在线程池执行，不影响心跳与上报。重置全局 `NODE_TOKEN` 后，**所有节点的 `.env` 都需同步更新并重启**，否则会被拒绝接入。

### 6.1 打包 exe

在装有 MT5 的 Windows 机器上执行（脚本会自建 `.venv`、装依赖、装 PyInstaller）：

```bat
cd node_client
build_exe.bat
```

产物 `dist\node_client.exe`（onefile 单文件）。需要 onedir 形态（exe + `_internal\` 依赖树）时改用 `build_exe_onedir.bat`，产物为 `dist\node_client\` 目录。首次构建会把 `.env.example` 复制成产物目录的 `.env`，已存在则保留不动。

**版本号每次打包自动生成**，规则 `${数字版本号}-${年月日时分秒}`，例如 `1.1.0-20260813090507`：

- 数字版本号取 `node_client/version.py` 的 `BASE_VERSION`，只在发大版本时手工改；时间戳由构建脚本生成，所以同一天连打几次包也不会撞版本号，后端按数字段比较时晚构建的一定更新。
- 构建脚本先生成 `_build_info.py`（随 exe 一起冻结，是 exe 的内置版本来源，**不提交仓库**），打包完再写产物目录的 `version.txt`，两者必然是同一个版本号。
- 运行时取版本的顺序：旁路 `version.txt` → exe 内置版本 → 裸数字版本号。直接跑源码（`run_node.bat` / `python node_client.py`）没有经过构建，上报的是不带时间戳的 `BASE_VERSION`，后端会视为比任何构建产物都旧。
- 后端限定版本号不超过 32 字符，因此数字版本号最长 17 位，超出时构建会直接失败并提示。

**同时自动打出一份安装包 zip**，可直接上传到后台「客户端版本」页，不必手工压缩：

```
node_client\packages\node_client-1.1.0-20260813090507.zip
```

- 包内是**相对客户端目录的文件树**：onefile 形态为 `node_client.exe` + `version.txt`，onedir 形态为整棵目录树；另会附上 `.env.example`，面板导入新节点时用它作模板生成 `.env`。
- **`.env` 绝不入包**。安装包会上传到后端，任何持有节点令牌的机器都能下载它 —— 把带真实 `NODE_TOKEN` 的 `.env` 打进去等于泄露令牌。同时排除 `.backups/`（面板的版本化备份）、`*.bak`、`__pycache__` 与日志。
- 打包完会**自检**一次：必须含 `node_client.exe`、必须不含 `.env`，任一不满足就删掉产物并让构建失败，把打包失误挡在上传之前。
- 输出目录 `packages\` 刻意放在 `dist\` 之外：否则下一次构建会把上一次的 zip 当成产物文件收进新包。
- 上传时**版本号留空**即可，后端会读包内的 `version.txt`。

---

## 7. 配置项说明

### 7.1 后端 / docker-compose（`.env`）

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `REDIS_URL` | `redis://redis:6379/0` | Redis 连接 |
| `MYSQL_DSN` | 本地 SQLite | 生产由 compose 注入 `mysql+asyncmy://...` |
| `ENABLE_AUTH` | `false`（生产建议 `true`） | 是否校验 Webhook token |
| `AUTH_TOKEN` | — | Webhook 共享 token |
| `ENABLE_IP_WHITELIST` | `false` | 是否启用 IP 白名单 |
| `WHITELISTED_IPS` | TradingView 官方 IP | 逗号分隔；经 Nginx 时取 `X-Forwarded-For` |
| `JWT_SECRET` | `change_me` | **必须改**：后台登录令牌密钥 |
| `JWT_EXPIRE_MINUTES` | `720` | 登录有效期（分钟） |
| `ADMIN_USER` / `ADMIN_PASSWORD` | `admin / admin123` | **必须改**：后台账号 |
| `HEARTBEAT_INTERVAL` | `30` | 下发节点的心跳间隔（秒） |
| `ONLINE_TTL` | `90` | 在线标记 TTL，超时判离线 |
| `ACCOUNT_REPORT_INTERVAL` | `5` | 期望的账户上报间隔 |
| `AUTH_FIRST_PACKET_TIMEOUT` | `5` | 节点首包鉴权超时 |
| `DISPATCH_MODE` | `sync` | **回退默认**：品种在中控台未单独指定 `dispatch_mode` 时使用 |
| `POSITION_SCOPE` | `symbol` | **回退默认**：品种在中控台未单独指定 `position_scope` 时使用 |
| `POLL_ACK_TIMEOUT` | `15` | 轮询单节点回报超时 |
| `POLL_MAX_RETRY` | `3` | 轮询单节点最大重试 |
| `DEDUP_WINDOW` | `5` | 信号去重窗口（秒） |
| `NODE_TASK_MAGIC_BASE` | `900000000` | 策略子任务魔术号基数（兼容旧名 `GROUP_TASK_MAGIC_BASE`） |
| `NODE_BUSY_TTL` | `86400` | 同分组同节点策略互斥占位 TTL（秒）；键为 `(group_id, node_id)` |
| `STRATEGY_REPORT_INTERVAL` | `5` | 策略运行上报间隔（秒） |
| `CLIENT_PACKAGE_DIR` | `./data/client_packages` | 客户端安装包存放目录；Docker 下由 compose 注入 `/app/data/client_packages` 并挂 `backend_data` 卷 |
| `CLIENT_PACKAGE_MAX_MB` | `300` | 单个安装包大小上限；超出返回 413。改大时需同步放宽 Nginx 的 `client_max_body_size` |
| `LOG_LEVEL` | `INFO` | 日志级别 |

### 7.2 节点端（`node_client/.env`）

| 变量 | 说明 |
| --- | --- |
| `MANAGER_WS_URL` | 后端节点网关地址（生产 `wss://.../ws/node`） |
| `NODE_TOKEN` | 全局节点接入令牌（所有节点共享，从管理后台「配置 → 节点令牌」复制）|
| `MT5` 会话 | 客户端须放在 MT5 安装目录，自动发现 `terminal64.exe` / `terminal.exe`。终端**已登录**则复用当前账号（免输账号/密码/服务器）；未登录再交互输入。`MT5_LOGIN` 作为节点唯一标识 |
| `MT5_MOCK` | `true` 用内存模拟器（无需真实 MT5） |
| `HEARTBEAT_INTERVAL` / `ACCOUNT_REPORT_INTERVAL` | 心跳/上报间隔 |
| `RECONNECT_MIN` / `RECONNECT_MAX` | 断线重连退避区间 |
| `WATCH_SYMBOLS` | 本地额外观察列表；会与中控台已配置品种、当前持仓品种合并后上报报价（供区间过滤） |
| `DEFAULT_SLIPPAGE` / `DEFAULT_MAGIC` | 默认滑点 / 订单魔术号 |
| `LOCAL_STATUS_HOST` | 本机状态口绑定地址，**默认 `127.0.0.1`**（勿改公网） |
| `LOCAL_STATUS_PORT` | 本机状态口端口；**默认 `0`=关闭**。运维面板启动实例时会注入可用端口 |

### 7.3 本机运维面板（`nodel_client_dashboard/`）

用于在 Windows 上管理多个 `node_client.exe`：添加路径、启动/停止、守护自动拉起、轮询本地 `/health` `/status`、查看策略任务快照与日志。

**开发运行**

```bat
cd nodel_client_dashboard
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

**打包**

```bat
cd nodel_client_dashboard
build_exe.bat
```

产物两份：`dist\node_client_dashboard.exe`（`instances.json`、`panel_config.json` 与 `logs/` 运行时写在 exe 同目录），以及**分发压缩包** `packages\node_client_dashboard-1.0.0-20260813155913.zip`，拷到新机器解压即用。

- **版本号每次打包自动生成**，规则同 node_client 的 `${数字版本号}-${年月日时分秒}`（见 [§6.1](#61-打包-exe)）：数字版本号取 `version.py` 的 `BASE_VERSION`，只在发大版本时手工改。版本号一式两份 —— `_build_info.py` 冻结进 exe、`version.txt` 写在产物目录旁，**面板窗口标题会显示它**，排障时报版本不用再翻文件。
- 压缩包内部**带一层 `node_client_dashboard-<版本>\` 目录**，解压时不会把文件铺一地（面板没有自动安装通道，这个包是给人手工解压的，与 node_client 那种要被 `apply_package` 覆盖安装的平铺包不同）。
- **`panel_config.json` 与 `instances.json` 绝不入包**：前者存着后端地址与 `NODE_TOKEN`，后者记录本机每个实例的绝对路径。两者都写在 exe 同目录，只要在 `dist\` 里跑过一次面板就会生成，跟着打包等于把令牌和本机拓扑一起发出去。同时排除 `logs/`、`*.bak`、`__pycache__`。
- 打包完会**自检**一次：必须含 `node_client_dashboard.exe`、必须不含上述两个配置文件，任一不满足就删掉产物并让构建失败。
- 输出目录 `packages\` 刻意放在 `dist\` 之外：否则下一次构建会把上一次的 zip 当成产物文件收进新包。

**要点**

1. 「导入节点」选 MT5 安装目录 + 客户端版本，从后端下载部署并自动生成 `.env`，详见 §7.5；只有本机 exe 时用「手工添加」选文件（工作目录默认取其同目录，需已配置 `.env`）。
2. 「启动」注入 `LOCAL_STATUS_PORT`，**无窗口后台启动**（不弹控制台）；stdout/stderr **实时**写入面板运行目录 `logs/<节点标识符>/YYYY-MM-DD.log`（按天轮转）。
3. 「停止」优先 `POST http://127.0.0.1:<port>/stop`，超时再 terminate/kill。
4. 「守护进程」开启后，子进程退出会按间隔（默认 5s）自动拉起；左侧「全部开守护 / 全部关守护」可批量切换并写入 `instances.json`。关闭/最小化窗口会**进入系统托盘**（守护继续）；托盘右键「退出」才结束面板（仍不强杀已启动节点）。重新打开面板会按已保存的 `status_port` **自动接管**。
5. 顶栏右上角两个入口：「连接配置」填后端 API 地址与节点令牌（可「测试连接」验证），留空则自动读实例 `.env`；「版本更新」从后端拉取已发布的安装包并批量更新，支持指定版本与本机回滚，**后端有更新时按钮会变成橙色的「有新版 vX.Y.Z（待更新实例数）」**。两者详见 §7.4。
6. 「手工替换全部」选择本地新的 `node_client.exe`（建议同目录带 `version.txt`），确认后对**全部实例**（不能只选其中几个）：运行中先停 → 备份为 `.bak` → 覆盖 exe/`version.txt` → 自动再启。适用于没有后端通道、手上只有一个 exe 的场景；要挑实例更新或要能回滚到历史版本，走顶栏「版本更新」——它按版本号分目录备份，本地替换只有会被下次覆盖的单槽 `.bak`。
7. 面板**仅单实例运行**：重复启动会提示并尝试激活已有窗口。
8. **面板崩溃自恢复**：启动时带看门狗；UI 异常退出后默认间隔 5s 重启，最多 3 次（稳定运行 ≥60s 后崩溃计数清零）。监督日志：`logs/dashboard_supervisor.log`。环境变量：`DASHBOARD_RESTART_MAX`、`DASHBOARD_RESTART_DELAY_S`、`DASHBOARD_NO_SUPERVISOR=1`（关闭）。
9. 状态口仅 loopback、无鉴权——不要把 `LOCAL_STATUS_HOST` 改成公网地址。
10. 后台启动无法交互输入 MT5 账号：请先在终端登录（或 `MT5_MOCK=true`），再点启动。
11. 客户端版本：形如 `1.1.0-20260813090507`（打包时自动生成，见 [§6.1](#61-打包-exe)）。`node_client` 启动日志与 `/status` 的 `version` 字段可见；面板从状态口或旁路 `version.txt` 展示；节点也会在鉴权首包上报给后端，管理后台「客户端版本」页可看到全网分布。

### 7.4 客户端版本更新（面板「版本更新」）

面板作为更新代理执行文件替换 —— Windows 上正在运行的 exe 无法覆盖自己，而面板本就持有停止、备份、重启的完整能力。

**连接后端（顶栏「连接配置」）**

面板需要一个只读通道去查版本、下安装包，鉴权用的就是节点 `.env` 里那份 `NODE_TOKEN`。配置入口在**顶栏右上角「连接配置」**：

- 填 **API 地址**（如 `http://159.75.33.185` 或 `https://hub.example.com`）与 **节点令牌**（后台「配置 → 节点令牌」可复制），保存到 exe 同目录的 `panel_config.json`；
- **「测试连接」**会真的向后端要一次当前发布版本，当场告诉你地址与令牌通不通、后端发布的是哪个版本；
- 两项**留空时自动从各实例工作目录的 `node_client/.env` 推导**：`MANAGER_WS_URL=ws://host/ws/node` → `http://host`，令牌取 `NODE_TOKEN`。添加实例后打开该界面，**发现到的值会直接回显进输入框**（下方同时列出每个实例的发现结果），可直接「测试连接」或点「保存」固化；若想让面板继续跟随 `.env` 变化，则「清空」后再保存；
- 地址与令牌各自独立回落，例如只在面板改了后端域名、令牌仍沿用 `.env` 的那份；
- 「版本更新」对话框顶部也能就地改这两项（改完点「保存连接配置」），与顶栏入口共用同一份 `panel_config.json`；
- 该令牌只被后端授权访问「查当前发布版本」与「下载安装包」，**不能**用于上传、发布、降级、删除（那些需要管理员 JWT）。

**顶栏的更新提示**

入口在**顶栏右上角「版本更新」**，它同时兼作提示灯：面板启动 3 秒后自动查一次后端当前发布版本，之后每 30 分钟复检；装完新版、增删实例或改过连接配置也会立即重算。

- 有实例的版本低于后端发布版本时，按钮变成橙色的**「有新版 v1.2.0（2）」**，括号里是待更新的实例数；
- 全部已是最新、后端尚未发布、或后端连不上时，按钮保持常态的「版本更新」；
- **检测失败一律静默**（不弹窗、不写红字）—— 这只是个提示，本机运维不该被一个连不上的后端打断；
- 版本号读不出来的实例**不计入**待更新数：面板无从判断它是旧是新，算进去会让提示常亮，久而久之就没人看了。

**操作**

1. 「检测更新」拉取后端当前发布版本，实例清单逐行显示 `当前版本 → 目标版本` 与判定（需更新 / 已最新 / 将降级）。
2. 勾选实例后点「更新到发布版本」：下载 → 校验 sha256 → 解压 → 逐个实例「停 → 版本化备份 → 按文件树覆盖 → 自动再启」。
3. 「更新到指定版本」弹出版本选择窗口（居中于父窗口）：**下拉列出后端已上传的全部版本**（新版在前，默认选中当前发布版本），选中项旁显示包体大小、是否当前发布版本与更新说明，用于灰度或临时回退到某个具体版本。因为版本清单里带着 sha256，这条路径**同样会做完整性校验**。
4. 「回滚到本机备份」同样弹出版本选择窗口（居中于父窗口）：**下拉列出所选实例都存在的共同备份版本**，从 `.backups/<版本>/` 就地恢复，**不依赖网络**。因为候选就是共同备份，选出来的必然可用。

**要点**

- **`.env` 永不被覆盖**：它承载节点身份（`NODE_TOKEN` / MT5 账号），覆盖等于把节点搞下线。
- 备份按版本分目录存放在 exe 同级的 `.backups/<版本>/`，且 exe 与 `version.txt` **一起**备份 —— 只存 exe 的话，恢复后版本号会与实际运行的程序对不上。
- 目标版本低于当前版本时会追加一道「降级二次确认」，逐行列出受影响实例。
- 启停中（busy）的实例会被跳过，避免与启停抢同一个 exe 文件。
- 安装包是 zip，内部为**相对客户端目录的文件树**。**打包脚本会自动打好这份 zip**（见 [§6.1](#61-打包-exe)），直接上传即可，不必手工压缩。

### 7.5 导入节点与编辑节点配置

**导入节点（左栏「导入节点」）**

一次完成「部署客户端 + 注册实例 + 写好配置」，省掉手工拷 exe、手抄地址与令牌：

1. 选 **MT5 安装目录**。目录必须含 `terminal64.exe` 或 `terminal.exe`——客户端要与终端同目录才能发现 MT5，不合规会当场拦住。选好后会提示识别到的终端名，以及该目录是否已有客户端 / `.env`。
2. 选 **客户端版本**。列表从后端拉取（需先配好顶栏「连接配置」），**默认选中当前发布版本**，也可挑任意历史版本；旁边显示包体大小与更新说明。
3. 勾选项：
   - **自动写入 `.env`** —— 用面板连接配置里的 API 地址反推 `MANAGER_WS_URL`（`http://host` → `ws://host/ws/node`）、填入 `NODE_TOKEN`，其余项沿用包内 `.env.example` 的默认值与注释。**已存在的 `.env` 一律不覆盖**。
   - **导入后立即启动** —— MT5 未登录时节点需要交互输入账号，首次导入建议先不勾。
4. 确认框会逐项列出目标目录、版本、终端名与 `.env` 处理方式；执行流程是下载 → 校验 sha256 → 解压 → 覆盖部署（目录里已有旧客户端时先做版本化备份）→ 写 `.env` → 加入实例列表。

同一目录已注册过实例时会提示改用顶栏「版本更新」，避免重复添加。

**编辑节点配置（选中实例 → 「节点配置」）**

直接在面板里改该实例的 `node_client/.env`，不必再开记事本：

- **常用字段**页是表单：后端地址、节点令牌（掩码，可切换显示）、模拟 MT5 开关、观察列表、滑点、魔术号、各类间隔、日志级别等，带类型校验（整数/小数/枚举），`MANAGER_WS_URL` 会校验必须是 `ws://` 或 `wss://`——填成 `http://` 是最常见的错。
- **原始文本**页可直接改整份文件，应付表单未覆盖的键。**两页都改过时以原始文本为准**（无法合并，猜错会丢用户的编辑），保存后会提示这一点。
- 保存走就地合并：**注释、空行、顺序与自定义键全部保留**，只改动目标键那一行；表单里留空的可选项不会写进文件，沿用 node_client 的内置默认值。
- `.env` 不存在时保存会创建。
- 改完需**重启实例**才生效（node_client 的配置在进程内一次性加载）；若实例正在运行，保存后会询问是否立即重启。
- `LOCAL_STATUS_PORT` 刻意不在表单里：它由面板启动实例时用环境变量注入，写进 `.env` 只会造成误解。

---

## 8. 使用指南（管理后台）

导航栏：**总览** / **节点** / **趋势面板** / **分组管理** / **中控台** / **配置** / **客户端版本** / **事件** / **操作审计**。

### 8.1 中控台（`/console`）

系统级交易规则入口，包含：

- **多区间方向过滤（按品种）**：每个品种一条规则，也是 Webhook **`model=normal`** 信号的**准入清单**——未登记或取消「启用」的品种一律拒收（含平仓信号；后台手动平仓除外）。
- **全局手数**：开启后，手数策略为 `global` 的节点统一使用该手数。
- **手动发信号**：可对指定分组触发 BUY / SELL / CLOSE（走 `model=strategy` 分组链路，便于联调）；可勾选「策略模版定向」限定模版，也可在命中分组列表里直接勾选要发给哪几个分组。表单字段与 Webhook 同名字段一一对应，其中「入场价」对应 `limit_price`，只有配成限价开仓的模版2 会用到。
- **清理交易日志**：需输入确认短语「清空交易记录」；会尝试停止运行中的策略任务，并清理相关历史表与 Redis 运行态（**不可恢复，慎用**）。

单品种规则字段说明：

| 字段 | 说明 |
| --- | --- |
| `enabled` | 是否接收并分发该品种信号（`false` 时拒收全部信号，含 Webhook 平仓；后台手动平仓不受影响） |
| `allow_buy` / `allow_sell` | 方向总开关：关闭后该品种不再接收对应方向的信号 |
| `dispatch_mode` | `sync`（全员同步，所有节点并发下单）/ `poll`（轮询轮转，每条信号只由轮到的 1 个节点领取，之后该节点移到队尾循环轮转） |
| `position_scope` | `symbol`（同品种有持仓则拦截）/ `account`（账户有任意持仓即拦截） |
| `default_action` | 价格不在任何区间时：`block` 拦截 / `pass` 放行 |
| `intervals` | 价格区间及每个区间允许的方向 |

示例（`GET/PUT /api/config/filters` 存的就是此结构）：

```json
{
  "XAUUSD": {
    "enabled": true,
    "allow_buy": true,
    "allow_sell": true,
    "dispatch_mode": "sync",
    "position_scope": "symbol",
    "default_action": "block",
    "intervals": [
      { "low": 2300, "high": 2350, "allow": ["BUY"] },
      { "low": 2350, "high": 2400, "allow": ["SELL"] }
    ]
  }
}
```

含义：价格在 2300–2350 只允许 BUY，2350–2400 只允许 SELL，落在区间外按 `default_action` 处理。若 `allow_sell: false`，则无论价格区间如何，所有 SELL 信号都会被拦截。

### 8.2 策略（`/strategies`）

基于内置模版创建策略实例，再绑定到分组后才参与 `model=strategy` 信号：

| 模版 | ID | 规则 type | 要点 |
| --- | --- | --- | --- |
| 策略模版1 | `tpl_1` | 1 逆势 / 2 顺势 | 首单手数用信号 `volume`；可配分批档位与 `batch_action`；逆势锚首仓（或更深逆势仓），顺势锚最近一笔 |
| 策略模版2 | `tpl_2` | 3 以损定量 | 按风险金额反推手数；底仓无止盈，分散仓等手数、阶梯止盈；可切**市价 / 限价**开仓；信号**必须带 `sl`**（限价还要带入场价）；忽略信号 `volume` |
| 策略模版3 | `tpl_3` | 4 网格 | 区间/格数/每格手数来自规则；可开向上追踪；可用「试算网格」按 ATR 与风险预算推荐格数与手数；忽略信号 `volume`/`sl`/`tp` |

推荐流程：选模版 → 填名称与绑定品种 → 按需改规则参数 → 创建 → 到「分组」页绑定。

**模版2 的「开仓方式」（默认市价）**：决定信号到达后怎么进场。

| 开仓方式 | 行为 | 对信号的要求 |
| --- | --- | --- |
| 市价（默认） | 底仓与分散仓一次性全部市价打齐，各单开仓价相同 | 带 `sl` |
| 限价 | 底仓挂在信号给的入场价，分散仓在「入场价 → 止损价」之间等分挂阶梯限价，一直等到成交（GTC） | 带 `sl` **和入场价**（`limit_price` / `price`） |

切到限价前需要知道两件事：

- **同一份配置会开出更大的手数**。各档开仓价不同、止损价共用，止损距离成了一组递减值，手数改按仓位比例加权的平均止损距离反推；挂单价通常比现价有利，加权距离更小，仓位就更重。
- **风险金额变成上限而不是定值**。它对应「全部档位都成交后打到止损」的最坏情况；只成交了前几档就止损的话实际亏损更小。策略详情与运行事件里这一项会显示成「最坏亏损」。

限价挂单期间该节点该品种不接新信号（占位由子任务持有）；分组信号页的子任务表格有「挂单」列，能看出任务是在等成交还是空转。终止任务、发 `CLOSE`、账户风控清仓都会**先撤挂单再平持仓**，不会留下没人管的在途单。

**模版3 的「试算网格」（默认关闭）**：只在配置时算一次，帮你把「格距」和「最大可接受亏损」换成网格数量与每格手数。

- 展开后选一台**在线节点**作行情源（只用来读该终端的 K 线与合约规格，**不会**写进策略配置），填 K 线周期、ATR 倍数与最大可接受亏损，点「取行情并试算」。
- 算法：格距 = ATR × 倍数（算出后填进「格距」框，可手改；改完重新试算即以手改值为准）→ 网格数量 = 区间宽度 ÷ 格距（夹在 2–200）→ 每格手数 = 最大可接受亏损 ÷ 满仓打止损的每手亏损（向下取整到品种手数步长）。
- 结果只是建议，点**「应用建议」**才写入上方的「网格数量」与「每格手数」；之后仍可手改。改了区间 / 止损 / 方向等参数后结果会标为过期，需重新试算才能应用。
- 前置条件：必须先设好本方向的止损价（多头看区间下沿、空头看区间上沿），否则无从反推手数。
- **网格运行时不读这组参数**，改动它们不影响已在跑的任务；策略详情里的「试算来源」只是留痕。
- 提示区会说明口径与风险：止损是节点侧软止损（网格单不带 `sl`），节点掉线或跳空时实际亏损可能超出预算；`终止时清仓`关闭时亏损不封顶；开启向上追踪后最坏亏损估算不再成立。

> 创建时规则会复制到实例；之后改实例不影响模版。已触发的任务使用触发时快照，不受后续编辑影响。

> 同品种下挂了多个分组时，信号默认发给全部命中分组；若只想让一部分接收，在信号里带上 `template_ids`（按模版收窄，如 `["tpl_3"]`）或 `group_ids`（按分组 ID 点名），详见 `WEBHOOK.md` §4.5。

### 8.3 分组（`/groups`）

- 创建分组并选择**分发模式**（组内 `sync` / `poll`）与**成员节点**。
- **一对一绑定策略**：一个策略只能挂到一个分组；未绑定策略的分组不会处理 strategy 信号。
- **趋势风控**（默认关闭）：开启后，开仓信号进入各节点前会按「趋势面板」**全局参数**计算该节点上信号品种的趋势——`BUY` 仅多头放行、`SELL` 仅空头放行；中性、数据不足或行情读取失败一律拦截，子任务记为 `skipped` 并写中文 `skip_reason`。`CLOSE` / 停策略不受影响。轮询模式下被拦节点会顺延下一节点。
- **分组 ID**：形如 `grp_` + 16 位十六进制，可在分组列表或 `GET /api/groups` 取到；信号带 `group_ids` 时用它点名接收方。
- 列表「信号」列为该分组主任务总数；「进行中」列为 `pending/dispatching/running` 主任务数（有值时可点进信号页，默认仅筛进行中）。
- 进入分组信号页（`/groups/:id/signals`，可选 `?status=active`）可查看主任务、各节点子任务状态，以及开仓/加仓/网格平移等事件（含计算明细）；页内可在「全部 / 仅进行中」间切换。

### 8.4 配置（`/config`）

账户与安全设置，不含交易规则：

- **节点令牌 (NODE_TOKEN)**：查看 / 复制 / 重置全局接入令牌（所有 node_client 共享）。
- **修改密码**、**双因素认证 (2FA)**。

### 8.4.1 客户端版本（`/client-versions`）

客户端安装包的上传与发布，配合本机运维面板完成节点更新（面板侧操作见 §7.4）。

- **上传安装包**：zip，内部为相对客户端目录的文件树，至少包含 `node_client.exe`，建议附 `version.txt`。版本号留空时读包内 `version.txt`，读不到则要求手工填写。上传时按分块落盘并计算 sha256，校验通过才登记；同版本号重复上传返回 409。
- **设为发布版本**：写入发布指针；各节点的运维面板「检测更新」时据此判断要不要更新。目标版本低于当前发布版本即判定为**降级**，需输入「确认降级」二次确认。
- **回滚**：把发布指针退回上一个发布过的版本，需输入「确认回滚」。删除某版本时若它正是回滚落点，落点会一并清空（否则回滚必然失败）。
- **删除**：当前发布版本不可删除，需先切到其它版本。
- **节点版本分布**：版本由节点在鉴权首包上报，反映各节点实际运行的程序版本。旧客户端不上报版本号，会计入「未上报」。

存储与部署注意：安装包落在 `CLIENT_PACKAGE_DIR`，Docker 下必须挂 `backend_data` 卷（compose 已配置），否则重建镜像后已上传的包会全部丢失；Nginx 对 `/api/client-versions` 单独放宽了请求体上限与代理超时。

### 8.5 节点管理

- **自动注册（推荐）**：node_client 用任意尚未入库的 `MT5_LOGIN` 首次连接时，后台自动按默认配置入库（名称 `node-<login>`、**禁用**）。**按币种配置**会从中控台已登记品种批量生成（每个品种默认参与 sync/poll、`fixed` + `0.01`、轮询序 0）。入库后须在「节点管理」启用，客户端才会真正接入在线。
- **新建节点（可选）**：在「节点 → + 新建节点」里手动填表。`mt5_login` 全局唯一，重复创建会返回 409。
- **按币种分发配置**：在节点详情或编辑表单中，为每个品种单独设置：
  - `follow_sync` / `follow_poll`：是否参与该品种在对应分发模式下的分发；
  - `lot_mode`：`global`（跟随中控台全局手数）/ `fixed`（本节点该品种固定手数）/ `signal`（跟随信号手数）；
  - `lot`：`fixed` 模式下的手数；
  - `poll_order`：该品种在轮询轮转中的**初始排队顺序**（越小越先轮到；之后按领取情况动态轮转）。
- **账户级风控**：在节点编辑中配置（存 `risk_json`，登录/保存时同步到节点本地执行），包括：
  - 浮盈/浮亏相对余额比例清仓；
  - 净值下限清仓；
  - 品种盈亏 + 订单数条件平仓；
  - 品种浮盈亏保护；
  - 按持仓手数分档 + 盈亏金额平仓。
- **品种须双重登记**（仅 `model=normal`）：信号品种须先在中控台登记；节点 `filters` 中也须存在该品种的按币种条目，否则该节点拒收（事件流 / 事件页会显示中文 `skip_reason`）。
- **编辑/启停**：可随时停用节点（停用后不参与分发，也无法接入）。`mt5_login` 创建后不可修改。
- **趋势面板**：节点详情的「趋势面板」Tab，按币种查看该节点终端上的实时趋势（只读观测，不参与下单与过滤）。详见 §8.9。

### 8.6 事件（`/events`）

集中查看 Webhook 信号历史（持久化于 MySQL，非仅实时 WS 流）：

- **原始请求体**：每条信号保存 Webhook 收到的 JSON 或纯文本（`raw_payload`），便于对照 TradingView 告警原文。
- **解析字段**：动作、品种、手数、止盈止损、来源 IP、分发模式、信号状态等。
- **节点明细**：展开后可看各节点的下发手数、跳过原因、执行状态、MT5 回报（`retcode` / 订单号 / 成交价 / 错误信息）。
- **分页**：默认每页 20 条，可调整页码与每页条数；不含手动平仓记录。

> 分组策略任务请优先在「分组 → 信号」页查看；总览页 WS 事件流适合实时监控，事件页适合 normal 链路事后追溯。

### 8.7 监控与远程平仓

- 「总览」实时展示各节点净值、保证金、持仓与盈亏，以及分发/回报事件流。
- 单个持仓可一键平；也可对单节点/全体广播「全平 / 按品种 / 按订单」平仓（全平有二次确认）。

### 8.8 策略信号快速上手

1. 在「策略」创建实例（例如模版1 绑 `XAUUSD`）。
2. 在「分组」创建分组，加入在线节点，绑定上一步策略。
3. 用 Webhook 或中控台手动信号发送：

```json
{"model": "strategy", "action": "buy", "symbol": "XAUUSD", "volume": 0.1}
```

模版2 须带 `sl`，配成限价开仓时还须带入场价；模版3 可不带 `volume`/`sl`。细则见 [`WEBHOOK.md`](./WEBHOOK.md) §4.5、§4.6。

```json
{"model": "strategy", "action": "buy", "symbol": "XAUUSD", "sl": 2380.0, "limit_price": 2390.0}
```

走中控台「手动发信号」触发限价开仓时，填法一一对应：**止损价** = `sl`、**入场价（限价开仓）** = `limit_price`。两者缺一，信号都会被拒收并在落选原因里写明。

### 8.9 趋势面板（顶栏 `/trend` · 节点详情 Tab）

按币种 / 周期实时查看节点 MT5 终端上的趋势强弱，属于只读观测，不参与下单与过滤决策。入口有两处，共用同一套面板与接口：

- **顶栏「趋势面板」**（`/trend`）：跨节点查看。进入时默认选中**第一个在线节点**、默认品种 **XAUUSD**；右上角可切换观察节点。
- **节点详情 →「趋势面板」Tab**：固定看当前节点。

数据链路：面板轮询后端 → 后端向该节点下发行情探针（`market_probe`）→ 节点回传已收盘 K 线、当前报价与品种合约规格 → 后端算指标与得分。**节点必须在线**，否则提示「节点当前离线，无法读取行情」。同一条探针也供模版3 的网格试算取 ATR 与合约规格。

- **选品种**：输入框可直接填任意品种代码（回车或失焦生效，自动转大写；券商后缀由节点侧解析）；下方快选来自该节点的报价品种、当前持仓品种与按币种配置的并集（顶栏页始终包含 XAUUSD）。选中的品种记在浏览器本地，下次打开该节点自动沿用。
- **选周期**：`M1 / M5 / M15 / M30 / H1 / H4 / D1 / W1 / MN`，默认 `M15`。
- **自动刷新**：3 / 5 / 10 / 30 秒或暂停，默认 3 秒；切走该 Tab 即停止轮询。
- **图表**：K 线主图叠加 EMA 线，副图画 RSI 与超买/超卖参考线，主副图联动，可在图内滚轮/拖拽缩放。

**得分构成**：两个指标各自先算出 -100 ~ +100 的原始分，再乘权重相加，因此单指标最高贡献 = 权重 × 100。

| 指标 | 默认权重 | 作用 | 得分口径 |
|------|---------|------|---------|
| EMA | 70% | 方向 | 当前价在 EMA 上方为正、下方为负；偏离达到「满分偏离 %」即触顶（±70） |
| RSI | 30% | 动能 | 高于「偏多起始」为正、低于「偏空起始」为负，两线之间为 0（±30） |

**趋势判定**：得分 > 多头阈值（默认 +20）判多头；< 空头阈值（默认 -20）判空头；之间为中性。K 线不足以算出指标时显示「数据不足」，不会当成中性。

**可调参数**：

| 参数 | 默认 | 说明 |
|------|------|------|
| EMA 周期 / RSI 周期 | 50 / 14 | 范围 2~400 / 2~100 |
| EMA 权重 | 0.7 | RSI 权重自动取 `1 - EMA 权重` |
| 满分偏离 % | 0.2 | 价格偏离 EMA 达到该百分比即按方向满分计；品种波动差异大时按需调整 |
| 多头 / 空头阈值 | 20 / -20 | 同时决定得分刻度条上中性区的宽度 |
| RSI 偏多 / 偏空起始 | 60 / 40 | 两线之间为中性区，RSI 落在此区间不贡献得分 |
| RSI 超买 / 超卖 | 70 / 30 | 只影响状态文案与 RSI 副图参考线 |
| 显示 K 线根数 | 120 | 范围 30~500；实际取数根数由后端按指标周期自动放大；**月线最多 80 根、周线最多 150 根**（高周期历史下载会卡住 MT5 终端，再多会把节点拖离线）|

参数改动**即时生效于本次查看**（后端行情缓存会拦掉重复的终端查询，因此调参数几乎无延迟）；点「保存」写入**全局配置**（`system_setting.trend_config`，全后台共用、不分节点/币种），下次打开与其他管理员、任意节点面板都沿用，越界值会被夹回合法区间。「恢复默认」只重置表单，仍需保存才落库。

> `MT5_MOCK=true` 的模拟节点回传的是合成 K 线，得分参考意义有限，需接真实终端验证实际走势。

---

## 9. TradingView Webhook 接入

在 TradingView 警报的「通知 → Webhook URL」填：

```
https://hub.example.com/webhook
```

消息体支持 JSON 或纯文本，示例（JSON）：

```json
{
  "action": "buy",
  "symbol": "XAUUSD",
  "volume": 0.1,
  "sl": 2320.5,
  "tp": 2360.0,
  "token": "<AUTH_TOKEN>"
}
```

- `action` 支持 `buy/sell/close`（含中文「做多/做空/平仓」等关键字）。
- 开启 `ENABLE_AUTH` 后必须带 token，可放在 `token` 字段、`X-Auth-Token` 头或 `?token=`。
- **`model=normal`**：品种须先在中控台登记且启用，否则返回 `status: rejected`，不会分发。
- **`model=strategy`**：不走中控台品种准入；需有启用的分组且绑定了匹配品种的策略。详见 [`WEBHOOK.md`](./WEBHOOK.md) §4.5。
- 平仓信号（`close`）在 normal 链路会广播给在线节点；strategy 链路则结束对应分组策略任务。
- `DEDUP_WINDOW` 秒内相同信号（动作/品种/手数/止盈止损一致）会被去重。

未登记品种时的响应示例：

```json
{
  "status": "rejected",
  "signal_id": "sig_...",
  "action": "BUY",
  "symbol": "EURUSD",
  "volume": 0.1,
  "mode": "rejected",
  "targets": 0,
  "reason": "品种未配置：EURUSD未在中控台配置，信号拒收"
}
```

> 字段解析规则与参考仓库 `Robin3910/mt5_python_connector` 完全一致，详见 `backend/app/parser.py` 与 `backend/tests/test_parser.py`。

---

## 10. 接口与协议速查

### HTTP（均需 `Authorization: Bearer <JWT>`，登录接口除外）

> 例外：下表标注「节点令牌鉴权」的接口用全局 `NODE_TOKEN`（请求头 `X-Node-Token: <NODE_TOKEN>`，也接受 `Authorization: Bearer <NODE_TOKEN>`），供节点端与本机运维面板查版本、下载安装包。该令牌全局共享且明文分发到每台节点机，因此**只授权这几个只读接口**，上传 / 发布 / 降级 / 删除一律仍需管理员 JWT。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/login` | 登录，返回 JWT |
| GET/POST | `/api/nodes` | 列表 / 新建节点（`mt5_login` 重复返回 409）|
| GET/PATCH/DELETE | `/api/nodes/{id}` | 查询 / 更新 / 删除 |
| GET | `/api/nodes/{id}/trend` | 趋势面板数据（`symbol` 必填；`timeframe` 与各参数可临时覆盖已保存配置，不落库）|
| GET | `/api/nodes/{id}/grid_sizing` | 模版3 网格试算（`symbol` 必填；按区间 / 止损 / ATR 倍数 / 最大亏损给出建议格数与每格手数，只读不落库）|
| GET | `/api/config/node-token` | 获取全局节点接入令牌（明文） |
| POST | `/api/config/node-token/rotate` | 重置全局节点接入令牌（旧令牌立即失效）|
| GET/PUT | `/api/config/lot` | 全局手数 |
| GET/PUT | `/api/config/filters` | 按品种规则（准入清单 + 方向总开关 + 分发模式/持仓范围 + 区间过滤） |
| GET/PUT | `/api/config/trend` | 趋势面板全局参数（EMA/RSI 周期、权重、阈值等；不分节点/币种） |
| GET | `/api/events/signals` | Webhook 信号事件分页（`page`/`page_size`；含 `raw_payload` 与各节点 `dispatches`） |
| GET/POST | `/api/strategies` · `/api/strategies/templates` | 策略实例 / 内置模版 |
| GET/PATCH/DELETE | `/api/strategies/{id}` | 查询 / 更新 / 删除策略 |
| GET/POST | `/api/groups` | 分组列表 / 创建（列表项含 `signal_count`、`active_task_count`） |
| GET/PATCH/DELETE | `/api/groups/{id}` | 查询 / 更新 / 删除分组 |
| GET | `/api/groups/{id}/signals` | 分组策略信号分页（可选 `status=active` 仅进行中主任务） |
| GET | `/api/groups/{id}/dispatches/{dispatch_id}/events` | 子任务事件流 |
| POST | `/api/console/manual-signal` | 手动对分组发 BUY/SELL/CLOSE |
| POST | `/api/console/purge-trade-logs` | 清理交易日志（需确认短语） |
| GET/POST | `/api/client-versions` | 客户端版本清单（含发布指针与节点版本分布）/ 上传安装包（multipart：`file` 必填，`version`、`notes` 可选） |
| POST | `/api/client-versions/{version}/release` | 设为当前发布版本；降级需 `{"confirm_downgrade":true}`，否则 409 |
| POST | `/api/client-versions/rollback` | 发布指针回退到上一个发布过的版本 |
| DELETE | `/api/client-versions/{version}` | 删除安装包；当前发布版本返回 409 |
| GET | `/api/client-versions/current` | **节点令牌鉴权**：当前应安装的版本（未发布返回 204） |
| GET | `/api/client-versions/available` | **节点令牌鉴权**：可下载版本清单（新版在前，含 `current`）；面板「导入节点」选版本用 |
| GET | `/api/client-versions/{version}/download` | **节点令牌鉴权**：下载安装包 |
| GET | `/api/accounts` | 全部账户快照 |
| POST | `/api/nodes/{id}/close` · `/api/close-all` · `/api/close-batch` | 单节点 / 全局 / 多节点平仓 |
| POST | `/webhook` | TradingView 信号入口（`model=normal` 或 `model=strategy`） |

### WebSocket

- `/ws/node`：节点接入。**首包必须**为 `{"type":"auth","data":{"token":"<NODE_TOKEN>","mt5_login":<int>,"client_version":"<可选>"}}`（`client_version` 用于后台展示各节点运行版本，旧客户端不带该字段时按「未知」处理，不影响接入），后台依次校验：① `token` 是否与全局 `NODE_TOKEN` 一致（常量时间比较）② `mt5_login` 是否对应已注册节点（未注册则自动入库）③ 节点是否启用 ④ 是否重复在线。通过即回 `auth_ok` 并开始心跳/上报；命令 `open`/`close`/`market_probe` 下行，结果 `trade_result`/`market_probe_result` 上行。鉴权失败的关闭码：`4400` 首包超时/格式错误、`4401` 鉴权失败（`invalid_token`/`missing_mt5_login`）、`4403` 节点被禁用、`4409` 重复在线。
- `/ws/admin?token=<JWT>`：后台实时推送 `snapshot/node_status/node_registered/node_rejected/account/dispatch/trade_result`（其中 `node_registered` 为自动注册事件）。

---

## 11. 测试

```bash
# 后端（含 WS 鉴权、自动注册、全局令牌重置等用例）
cd backend
pip install -r requirements-dev.txt
python -m pytest -q

# 节点端（强制模拟模式，无需真实 MT5）
cd node_client
python -m pytest -q

# 本机运维面板
cd nodel_client_dashboard
python -m pytest -q

# 前端类型检查 + 构建
cd frontend
npm run build
```

测试均使用 `fakeredis` 与模拟 MT5，无需真实中间件即可运行。

---

## 12. 故障排查（FAQ）

| 现象 | 排查方向 |
| --- | --- |
| 节点一直离线 | 全局 `NODE_TOKEN` 是否与「配置 → 节点令牌」一致（被重置过吗？）；`MANAGER_WS_URL` 是否可达；Nginx 是否放行 `/ws/`（已配置 Upgrade）|
| 节点被拒绝（`invalid_token`）| 全局令牌已变更，重新从「配置 → 节点令牌」复制并更新该节点 `.env` 后重启 |
| 节点被拒绝（`missing_mt5_login`）| `.env` 缺少 `MT5_LOGIN` 或值不为正整数 |
| 节点被拒绝（`disabled`）| 节点已在管理后台被禁用，到「节点管理」启用即可 |
| 节点被拒绝（`already_online`）| 同一 `MT5_LOGIN` 已有在线连接（仅允许一个同时在线）；等旧连接超时或显式下线 |
| Webhook 返回 401 | `ENABLE_AUTH=true` 时是否带对 `AUTH_TOKEN` |
| Webhook 返回 403 | IP 白名单是否开启；经 Nginx 时是否正确透传 `X-Forwarded-For` |
| Webhook 返回 400 | 信号无法解析：检查 `action`/`symbol` 是否齐全（参考 §9 示例） |
| Webhook 返回 `status: rejected` | normal 链路：品种未在中控台登记；到「中控台」添加该品种并保存 |
| strategy 信号无响应 | 分组是否启用、是否绑定策略、策略品种是否与信号一致、策略/规则是否启用；信号若带 `template_ids` / `group_ids`，分组是否在定向范围内（响应 `reason` 会写明「指定的分组不存在」「分组已禁用」等）；模版2 是否缺 `sl`（限价开仓还要入场价，且必须落在止损价的盈利侧）；模版3 方向是否与 `grid_side` 相容 |
| 限价单挂上了但一直不成交 | 正常现象：GTC 会一直等，期间该节点该品种不接新信号。要提前收手就对该子任务发 `CLOSE` 或点「平仓」，系统会撤掉挂单并收口 |
| 限价单下单报 `10016` | 挂单价距现价太近，低于券商要求的最小距离（`trade_stops_level`）；把入场价挂远一些 |
| 同分组同节点策略不下发 | 该分组内该节点可能仍有未结束子任务（互斥占位 `(group_id, node_id)`）；看分组信号页子任务状态，或等任务结束 / 发 CLOSE |
| 信号被「跳过」 | 命中方向总开关、持仓过滤、区间过滤，或**节点未配该品种**（事件页 / 节点详情会显示中文 `skip_reason`） |
| 下单报 `10030` | 填充模式不支持——节点端已自动切换 FOK/IOC/RETURN，仍失败请检查券商品种设置 |
| 节点端 `MetaTrader5 not available` | 该包仅 Windows；非 Windows 调试请设 `MT5_MOCK=true` |
| 后端起不来连不上 DB | 生产确认 `MYSQL_DSN`、容器 `mysql` 健康；本地默认 SQLite 无需 DB |
| Redis 不可用 | 后端会降级启动并告警，但实时态/队列/锁不可用，请尽快恢复 Redis |

查看日志：`docker compose logs -f backend`、节点端控制台输出、Nginx 容器日志。

---

## 13. 安全与运维清单

- [ ] 修改 `JWT_SECRET`、`ADMIN_PASSWORD`、数据库密码为强随机值。
- [ ] 生产开启 `ENABLE_AUTH=true` 并设置 `AUTH_TOKEN`；按需开启 IP 白名单。
- [ ] 启用 HTTPS/WSS（见 §5.5），节点与 TradingView 全部走加密通道。
- [ ] 数据库定期备份：`docker compose exec mysql mysqldump -u root -p mt5hub > backup.sql`。
- [ ] Redis 已开启 AOF 持久化（compose 默认 `--appendonly yes`）。
- [ ] 升级流程：构建前端 → `docker compose up -d --build` → `alembic upgrade head`。
- [ ] 单实例部署下 WebSocket 连接在进程内；如需多实例高可用，按技术方案第 5 章用 Redis Pub/Sub 路由命令。

---

如需进一步定制（多实例 HA、报警通知、更多品种映射等），参见 [`技术实现方案.md`](./技术实现方案.md) 对应章节。
