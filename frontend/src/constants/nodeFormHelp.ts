/** 节点新建/编辑表单字段说明 */
export const NODE_FORM_FIELD_HELP = {
  name: '节点在管理后台中的显示名称，用于区分不同 MT5 终端或 VPS。留空时自动生成「node-{mt5_login}」。仅用于展示与识别，不影响交易逻辑。',
  mt5_login:
    '绑定的 MT5 账户登录号（整数），节点的业务唯一键，创建后不可修改。node_client 用同一登录号首次连接时若库中尚不存在会自动注册；接入鉴权统一使用「账户设置 → 节点令牌」中的全局 NODE_TOKEN。',
  filters:
    '按币种配置该节点的分发参与、手数策略与轮询顺序。信号品种须先在中控台配置；节点未单独配置的币种信号将被拒收并记入本节点信号日志。自动注册时会按中控台已有品种生成默认配置（参与 sync/poll、固定手数 0.01、轮询序 0）。',
  enabled:
    '禁用后节点无法通过 WebSocket 鉴权接入，也不会参与任何信号分发；已在线连接不会被立刻踢下线，但重连会被拒绝。自动注册入库默认为禁用，需管理员手动启用。启用后恢复正常跟单。',
} as const
