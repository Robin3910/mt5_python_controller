/** 节点新建/编辑表单字段说明 */
export const NODE_FORM_FIELD_HELP = {
  name: '节点在管理后台中的显示名称，用于区分不同 MT5 终端或 VPS。留空时自动生成「node-{mt5_login}」。仅用于展示与识别，不影响交易逻辑。',
  mt5_login:
    '绑定的 MT5 账户登录号（整数），节点的业务唯一键，创建后不可修改。node_client 用同一登录号首次连接时若库中尚不存在会自动注册；接入鉴权统一使用「账户设置 → 节点令牌」中的全局 NODE_TOKEN。',
  lot_mode:
    '决定该节点每次开仓使用的手数来源：跟随全局 = 使用「系统配置」中的全局手数；固定手数 = 使用下方填写的固定值；跟随信号 = 使用 Webhook 信号里携带的手数。',
  lot: '仅当手数策略为「固定手数」时生效。该节点每次开仓统一使用此处填写的手数，不受全局配置或信号手数影响。',
  follow_sync:
    '当系统分发模式为「全员同步」时，是否参与执行。关闭后，该节点不会收到同步模式下的开仓指令，但仍可参与轮询（若开启跟随轮询）。',
  follow_poll:
    '当系统分发模式为「轮询领取」时，是否参与轮询队列。关闭后，该节点不会被分配轮询任务，但仍可参与同步分发（若开启跟随同步）。',
  poll_order:
    '仅在轮询模式下有意义。数值越小越先领取信号执行，相同数值按节点 ID 排序。例如 0 最先、1 次之。',
  enabled:
    '禁用后节点无法通过 WebSocket 鉴权接入，也不会参与任何信号分发；已连接会话会被拒绝。启用后恢复正常跟单。',
} as const
